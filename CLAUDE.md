# Microgrid Project — Authoritative Context

> **Resume protocol for new sessions:** read this file first. The spec is the authoritative design; the plan is the authoritative TDD task list.

## Research question

Can a population of LLM agents, one per household, negotiate peer-to-peer to allocate scarce energy during a grid outage in a way that is **(a)** fair across heterogeneous households, **(b)** robust to incomplete or incorrect information, and **(c)** explainable to residents?

The contribution is on the **CS/ML axis** (natural-language agent coordination, robustness, explainability), not the power-systems axis. Classical optimization handles (a) under strong assumptions, struggles with (b), and does not attempt (c). That gap is the paper.

## Timeline + venue

- Started: 2026-05-14. Runway: summer + potentially the following school year (no fixed deadline).
- Target venues (per advisor 2026-07-15, supersedes the 2026-05-26 list): **CHI, ICLR, or WWW — main conference or a workshop under it** (workshop deadlines ~3–4 months after main); track via mlciv.com AI-conference-deadlines. Decision after Phase 3 results, with the advisor.

## Four-phase plan

| Phase | Weeks | Status | What it builds |
|-------|-------|--------|----------------|
| 1 — Simulator | 1-4 | **complete** | Deterministic discrete-time microgrid sim, no agents |
| 1.6 — Pre-Phase-2 hardening | — | **complete** | Ownership/management comm-graph overlays, stress scenarios that break round-robin, centralized LP upper-bound baseline, gap-closed reporting (cold/hot ResStock regions pending data fetch) |
| 2 — LLM agent layer | 5-8 | **complete** | Per-household LLM agent, natural-language P2P messaging |
| 2.9 — Correctness hardening | — | **complete** | Review-driven fixes: energy conservation, solar timezone, LP slack/bounds, zero-LLM control, seed sweeps, provenance (18 tasks, 2026-07-07) |
| 3 — Benchmark & experiments | 9-14 | **in progress** (spec/plan 2026-07-07) | Scenario suite + baselines + metrics (Gini, served-critical-load, explanation quality) |
| 3.1 — Pre-live hardening | — | **complete** (tag `phase3.1-complete` 2026-07-12) | 55-finding review fixes; **gates ALL live spend** |
| 3.2 — Live runs | — | **complete** (tag `phase3.2-live-complete` 2026-07-16) | Live Haiku clean cell (3 seeds) + 3 failure cells; Stage 3 Sonnet ablation + Stage 4 explanation judging remain optional, budget-gated |
| 4 — Web demo & paper writeup | 15-20 | not started | SvelteKit/Next.js visualization on Vercel + paper |

> **Phase 1.6 is a hard gate on Phase 2**, inserted by advisor (Yongfeng) on 2026-05-26. Rationale: the current 12h-July-overnight scenario has a 98.5% no-coordination ceiling, leaving only ~0.5% headroom for the LLM layer — Phase 2 would be a null result by construction. Must establish a regime where round-robin visibly fails before building LLM agents.

Each phase has its own spec + implementation plan in `docs/superpowers/`.

## Phase 2 status

- **Spec:** `docs/superpowers/specs/2026-06-13-phase2-llm-agent-design.md`
- **Plan:** `docs/superpowers/plans/2026-06-13-phase2-llm-agent.md` (26 tasks, TDD)
- **Approved:** 2026-06-13
- **Execution mode:** Inline (Claude executes tasks in-session, batched ~3-5 at a time with check-ins).
- **Status:** ✅ **complete; tags `phase2-complete` (2026-06-13), `phase2.5-complete` (2026-06-14), `phase2.6-complete` (2026-06-14), `phase2.7-complete` (2026-06-17), `phase2.8-complete` (2026-06-17)**. 188 tests pass; ruff + mypy --strict clean. **Headline:** live Haiku 0.513 vs round_robin 0.525 (1.2-point gap, down from 6.5 in Phase 2v0). **Phase 2.8 finding:** adding peer state to the plan prompt + LLM-controlled share fraction changed NO macro metric (served/gini/transfers identical) while messages rose 66% — the clean cell has hit this architecture's ceiling; the Phase 3 hypothesis is that the LLM advantage lives in the failure cells. Reference run in `reference_runs/haves_havenots__llm/llm_agent/clean/` is the Phase 2.8 architecture (served=0.513). **Deferred follow-ups:** (a) live reference runs for `__defectors` / `__noise` / `__comm` / `__all` failure cells + a `long_outage_72h__llm` YAML (mock-LLM tests cover the first four; the 72h LLM variant was never created); (b) `defector_realization: prompt` live run (`wrapper` is the shipped-cell default; prompt realization is wired and is the config default); (c) peer state via INFORM-only (currently the engine's ground truth — a validity prerequisite for the noise/defector cells, see Phase 2.9 spec deferred table); (d) prompt engineering to outperform round_robin (Phase 3 work).

## Phase 3 status

- **Spec:** `docs/superpowers/specs/2026-07-07-phase3-benchmark-design.md`
- **Plan:** `docs/superpowers/plans/2026-07-07-phase3-benchmark.md` (11 tasks, TDD)
- **Status:** ✅ **infrastructure complete; tag `phase3-infra-complete` (2026-07-07)**. 267 tests (the previously recorded '274' was a miscount — verified 2026-07-12). Shipped: message-borne peer beliefs (INFORM-only) + noised act() + binding negotiation (commitments); failure axes causally couple (test-enforced); needs-aware fairness metrics; explanation provenance + rubric judge; --set overrides + sweep driver; process-stable RNG seeding; **solar showcase `haves_havenots_solar` (no_coord 0.4280 / control 0.6269 / rr 0.7666 / LP 0.9711 — 20.4-pt rr→LP gap; control re-pinned P3.1 T21, was 0.6910 pre-hardening)**; mock dose-response matrix in `docs/phase3_mock_sweep.md` (regenerated P3.1 T21: rr flat on every axis = structural immunity; comm budget 1 collapses belief-driven coordination to 0.440/gini 0.524; noise mildly HELPS served while defector rows are now FLAT after the T5 gating fix — the honest floor for live runs).
- **Live runs (Phase 3.2):** ✅ **complete; tag `phase3.2-live-complete` (2026-07-16).** **Stage 1 (clean Haiku, 3 seeds 23/1/7): beats the zero-LLM control on 3/3 — +5.8 ± 1.0 pts, closing 29.0% ± 2.9 of the control→LP gap; vs round_robin +2.0 ± 1.1 but decaying (2.8→2.3→0.8), so the claimable result is the *control* comparison, not round_robin.** First replicated live evidence the LLM layer adds value (P2.8 live Haiku *lost* to rr; P2.9 control *tied* it — the info-flow rework changed the answer); no efficiency–equity tradeoff (gini + Jain improve alongside served on all 3 seeds). **Stage 2 (failure cells, each vs its own same-seed control, all n=1):** `defectors`@7 (33.6% dose) 0.7564, **+2.3** vs control — retains 89.1% of coordination value under defection; `noise`@23 0.6486, **+0.87** vs control — tolerates dual-channel belief noise, margin narrows (clean +4.6 → +0.87) but ordering holds; `comm`@23 0.4565, **−4.6** vs control — the one loss, a **bandwidth-cost-of-negotiation** finding measuring hardcoded send-order plumbing, NOT LLM reasoning (`all`@7 deferred — its comm component is confounded). **Total live spend ≈ $34.51** (Stage 1 $17.45 + Stage 2 $17.06) of the $100 budget, + ~$0.75 sunk on the aborted inert defectors@23 cell.
- **Remaining:** **Stage 3** (Sonnet capability ablation) and **Stage 4** (live explanation judging, `--rubric-variant` consistency instrument shipped) stay OPTIONAL and budget-gated — advisor's 2026-07-15 priority is scenario design over model comparisons, and both run against already-committed run dirs whenever authorized. Then VT/AZ data + analysis (**Phase 3.3**: dose-response with live cells, fairness panel, explanation-quality table — see roadmap); **advisor sign-off received 2026-07-15 (see "Advisor sign-off 2026-07-15" below)**; venue decision at 3.3 exit.

## Phase 3.1 status

- **Spec:** `docs/superpowers/specs/2026-07-12-phase3.1-prelive-hardening.md` (locked decisions — do not re-litigate in execution)
- **Plan:** `docs/superpowers/plans/2026-07-12-phase3.1-prelive-hardening.md` (23 tasks, TDD, fully code-specified so a lighter-model executor can run it verbatim)
- **Why:** 2026-07-12 adversarially-verified full-repo review at `phase3-infra-complete` — 55 confirmed findings; 3 critical (defector-realization conflation, state-blind binding reacts, dead reference-cache fallback). Verified healthy: energy conservation, LP-ceiling validity, RNG determinism, zero-LLM-control isolation.
- **Status:** ✅ **complete; tag `phase3.1-complete` (2026-07-12). 333 tests; ruff + mypy --strict clean; clean-install dry-run green.** All 23 tasks done — 3 criticals fixed (T5 defector-realization gating, T7 state-blind reacts, T15 dead reference-cache). Live spend now unblocked; next is Phase 3.2.
- **Execution mode:** inline serial TDD, batched 3-5 tasks with check-ins. Everything after: `docs/superpowers/specs/2026-07-12-remaining-phases-roadmap.md`.

## Phase 2.9 status

- **Spec:** `docs/superpowers/specs/2026-07-06-phase2.9-correctness-hardening.md`
- **Plan:** `docs/superpowers/plans/2026-07-06-phase2.9-correctness-hardening.md` (18 tasks, TDD)
- **Status:** ✅ **complete; tag `phase2.9-complete` (2026-07-07)**. 243 tests; ruff + mypy --strict clean; clean-install dry-run green. Driven by a 2026-07-06 full-repo adversarial review (65 verified findings).
- **Corrected headline numbers (supersede everything below):** haves_havenots `no_coord 0.4560 / rr 0.5194 / lp 0.5294` (honest rr→LP gap 1.0 pt; old rr 0.5250 included phantom energy from the sender-cap bug). Real-data 24h_resstock_outage `no_coord 0.8425 / rr 0.8582 / lp 0.9111` (solar-tz fix; rr saves a real 21.4 kWh, not 35.7). **Zero-LLM control (`llm_fallback`): 0.518 on the showcase clean cell — within 0.15 pts of round_robin and above the Phase 2.8 live Haiku 0.513 (old physics). The tuned executor, not the LLM, carries clean-cell performance; every Phase 3 comparison must beat this control.**
- **The Phase 2.8 live reference run is old-physics.** A live re-run (and any live failure-cell run) must wait for the Phase 3 information-flow fixes below, or it measures plumbing, not robustness.
- **Deferred to Phase 3 spec (do NOT silently drop):** (1) INFORM-only peer state + noised act() + binding negotiation (REQUEST sized by real deficit, ACCEPT-gated transfers) — validity prerequisite for the failure cells; (2) needs-weighted welfare substrate + min-served/Jain metrics; (3) explainability instrument (advisor sign-off needed: rubric vs LLM-judge); (4) showcase-scenario redesign for larger rr→LP headroom; (5) live re-runs incl. a Sonnet capability ablation (~$22 batched); (6) repo visibility — **RESOLVED 2026-07-12: stays PUBLIC** (Leo's decision; flag to the advisor in the next update).

## Phase 1 status

- **Spec:** `docs/superpowers/specs/2026-05-14-phase1-simulator-design.md`
- **Plan:** `docs/superpowers/plans/2026-05-14-phase1-simulator.md` (26 tasks, TDD)
- **Approved:** 2026-05-14
- **Execution mode:** Inline (Claude executes tasks in-session, batched ~5 at a time with check-ins). Not subagent-driven — per project conventions, every line must be understood by the student.
- **Started executing:** 2026-05-14
- **Current position:** ✅ **Phase 1 complete + Phase 1.5 NREL ResStock integration done.** 70 tests pass; mypy + ruff clean. Three data sources work: synthetic (toy), pecan_street (requires university affiliation — not used), resstock (public, no signup). On real Texas ResStock load + 12h overnight outage: no_coord serves 81.3% with 254 kWh unmet; round_robin serves 83.9% with 218 kWh unmet. Round-robin saves **35.7 kWh of real residential load** and reduces welfare Gini by 15%. _[CORRECTED 2026-07-07: computed on solar shifted +6 h (UTC bug) AND pre-conservation-fix physics; honest figures: no_coord 0.8425 / rr 0.8582 (21.4 kWh saved, gini 0.0623→0.0530) / LP ceiling 0.9111 — see Phase 2.9 status.]_ The advisor email is drafted asking whether to use ResStock primary or have him sponsor Pecan Street access. Tagged `phase1-complete`. **Advisor approved Phase 1 on 2026-05-26 but gated Phase 2 behind a Phase 1.6** (ownership/management comm-graph overlays + round-robin-breaking stress scenarios + centralized LP upper-bound baseline + 1-2 more public datasets — see "Locked Phase 1.6 design decisions"). **Phase 1.6 shipped 2026-05-29** (96 tests, tagged `phase1.6-complete`); only the cold/hot-climate ResStock scenario YAMLs remain, pending data fetch. Next (historical — long since done; see the phase table).

### Progress log

Update this after every committed task. Newest entries on top.

| Date | Task | Commit | Tests | Note |
|------|------|--------|-------|------|
| 2026-07-16 | **P3.3 Task 6 — results-section outline (`docs/phase3_results.md`)** | _(this commit)_ | 346 ✓ | The paper's empirical story with real numbers inlined: headline (control-comparison claim, clean 3-seed +5.8±1.0 / 29.0%±2.9, NOT round_robin), failure-axis story (defectors +2.3/89.1% retained, noise +0.9, comm −4.6 bandwidth-cost), fairness + no-tradeoff finding, negotiation instrumentation, honest-negative clean-cell context (P2.8 0.513<0.525, P2.9 tie → P3 rework flipped it), and limitations (n=1 failure cells, degenerate Rawlsian floor, single dataset family, LLM-judge caveat, no-deployment claim). **Factual-accuracy pass caught + fixed one error:** cited 0.408 (the 3-seed no-coord MEAN) in the seed-23 row; corrected to per-seed 0.334/0.461/0.428. Every number re-verified against a fresh `collect_cell` dump (committed live + regenerated baselines). References all four figures + the tables doc by path. |
| 2026-07-16 | **P3.3 Task 5 — negotiation table + explanation-quality stub** | _(this commit)_ | 346 ✓ | `render_tables` (mpl-free, reads committed summaries only — $0) → `docs/phase3_tables.md`. **Negotiation instrumentation** per live run: commitments made/expired (+%), defaulted amount, react skipped/stale, plan parse-fails, msg sent/delivered/dropped. Cross-checks: clean@23 27.3% expiry (5.6%/9.0% at seeds 1/7 — prose fixed to give the range, not just seed 23), noise@23 5.7%, defectors@7 17.8%, comm@23 only 4260 of 17647 messages delivered (11887 budget-dropped) — the message-budget devastation, zero drops elsewhere. **Explanation-quality table = "pending Stage 4" stub** (money-gated Sonnet judging not authorized); auto-populates from `explanations_eval*.json` once committed. CLI `--tables`. ruff clean; no-mpl guard green (tables path included). |
| 2026-07-16 | **P3.3 Task 4 — failure-axis delta figure** | _(this commit)_ | 346 ✓ | `failure_axis_deltas` + `render_failure_axis` → `docs/figures/phase3_failure_axis.png`: per axis (defectors@7 / noise@23 / comm@23), the causal SAME-SEED decomposition — control's clean→stress drop (orange) + live−control under stress (blue) — on served (left) and Jain (right). Chose the same-seed control-drop over the cross-seed mock as the bar reference (honest, causal); mock 5-seed drop from `docs/phase3_mock_sweep.md` overlaid as grey diamonds for corroboration. **Every value matches:** defectors control-drop 0.0 (structurally immune to prompt defection) / live +0.023; noise control +0.013 (the "noise mildly helps the fixed policy" floor finding) / live +0.009; comm control −0.124 / live −0.046; diamonds land on the same-seed bars. Fixed a RUF001 (Unicode minus in a legend label → ASCII). CLI `--failure-axis`. ruff clean; no-mpl guard green. |
| 2026-07-16 | **P3.3 Task 3 — fairness panel + efficiency-vs-equity scatter** | _(this commit)_ | 346 ✓ | `cell_metrics` (mpl-free, per-method mean of every metric over a cell's seeds; LP's Jain/min/critical come back `None` and are skipped, not invented). **`docs/figures/phase3_fairness.png`** — 2×2 (Gini/Jain/min-house/critical), grouped bars per method per cell; visibly confirms two caveats: the min-house Rawlsian floor is degenerate (bars near-identical across strategies) and the comm loss (live Gini spikes 0.48, Jain drops 0.56 while rr holds). **`docs/figures/phase3_efficiency_equity.png`** (advisor's dedicated tradeoff figure) — served vs Gini and served vs Jain scatter, colour=method / marker=cell; both panels show fairness *improving* with served-load (no efficiency–equity tradeoff), comm the up-left outlier. LP appears only in the Gini panel (no Jain defined). CLI `--fairness`/`--scatter`. ruff clean; CI-parity (no-mpl import + tests) reconfirmed. |
| 2026-07-16 | **P3.3 Task 2 — headline figure** | _(this commit)_ | 346 ✓ | `render_headline` (matplotlib imported lazily) + `cell_series` (mpl-free per-method mean/std over a cell's committed seeds) → `docs/figures/phase3_headline.png`: 2×2 small-multiples, one per cell (clean/defectors/noise/comm), 5 bars each (no-coord/control/round-robin/live-Haiku/LP) with gap-closed(control→LP) annotated over the live bar. Clean cell shows mean±spread over seeds {1,7,23} (spread = error bars); failure cells single-seed, labelled n=1. **Eyeballed against the Slack scorecard — every bar matches:** clean gap-closed **+29%**, defectors **+10%**, noise **+7%**, comm **−17%**; control<live everywhere except comm (the known bandwidth-cost loss). CLI `--headline`/`--all`. Rendered in 4.4s via `.venv`. CI-parity reconfirmed: `scripts.figures` imports and `tests/test_figures.py` passes with matplotlib forcibly blocked; ruff clean. |
| 2026-07-16 | **P3.3 Task 1 — `scripts/figures.py` data-assembly layer (+ tests)** | _(this commit)_ | 346 ✓ | Matplotlib-free data layer: `read_live_summary` loads the committed `llm_agent` summary.json (live numbers, never re-run); `regen_baselines` regenerates the $0 baselines (`no_coordination`/`round_robin`/`llm_fallback` control) + `lp_optimal` ceiling via the engine, deterministic and seeded; `collect_cell` merges them into `{method: metrics}`. `LIVE_CELLS` pins the four committed cells (clean 23/1/7, comm 23, defectors 7, noise 23; `all` excluded — no live run). 5 new tests (`tests/test_figures.py`) assert the exact committed clean-seed23 served (0.6725764138021589), the 5-method merge, LP ≥ live, gap_closed>0, and KeyError on absent runs. **Verified the module-state trap is a non-issue for us:** `llm_agent.prepare` returns a closure with its OWN registry (Phase 2.9 T16), and I read only engine-computed physics from baselines + committed JSON for live, so in-process baseline sweeps don't cross-contaminate. **CI-parity proven:** suite green with matplotlib forcibly blocked (CI installs `.[dev,data]`, no viz) — the data layer never imports it. ruff clean. Rendering lands in Tasks 2-5 (matplotlib imported lazily there). |
| 2026-07-16 | **P3.3 — spec + TDD plan written (analysis & results, $0)** | _(this commit)_ | 341 ✓ | Phase 3.2 live runs are done; Leo chose Phase 3.3 (analysis + figures, $0) over the money-gated Stage 3/4 add-ons. Spec `specs/2026-07-16-phase3.3-analysis-design.md` + plan `plans/2026-07-16-phase3.3-analysis.md`. **Scope (all $0):** `scripts/figures.py` (deterministic, one command) → headline multi-seed bars, fairness panel, efficiency-vs-equity scatter (advisor's dedicated tradeoff figure), failure-axis delta, negotiation-instrumentation table, `docs/phase3_results.md` outline. Live numbers are READ from committed `llm_agent` summaries; $0 baselines regenerated via the engine (`.gitignore` keeps them out of the repo). **Explanation-quality table BLOCKED on Stage 4** (money, not authorized) → stubbed "pending Stage 4"; **VT/AZ cross-climate cells DEFERRED** (optional per roadmap). Key CI constraint recorded: matplotlib is the `viz` optional extra, absent in CI → rendering imports it lazily, tests stay mpl-free; verify in a fresh 3.12 venv (repo `.venv` is 3.14/missing pulp, not CI-parity). |
| 2026-07-16 | **P3.2 Stage 5 — wrap: docs synced to live numbers; tagged `phase3.2-live-complete`** ✅ | _(this commit)_ | 341 ✓ | Docs-only. Phase 3.2 live runs are DONE and the story is stable, so this syncs the status surfaces (per the docs-sync-at-phase-boundaries rule): phase table gains a 3.2 row; the Phase 3 status block gains a live-results bullet (Stage 1 3/3 vs control +5.8 ± 1.0 / 29% gap-closed; Stage 2 defectors +2.3 / noise +0.87 / comm −4.6, all n=1); README status line leads with the headline. **Total live spend ≈ $34.51** (Stage 1 $17.45 + Stage 2 $17.06: comm $4.03 + defectors $6.07 + noise $6.96) of $100, + ~$0.75 sunk on the aborted inert defectors@23. `/security-review` + secrets grep over the full `phase3.1-complete..HEAD` diff — clean (no key material; live artifacts are simulation JSONL + cache only). **Stage 3 (Sonnet ablation) and Stage 4 (explanation judging, `--rubric-variant` instrument already shipped) remain OPTIONAL and budget-gated** — advisor priority is scenario design over model comparisons; both run against committed run dirs on a fresh go. `all`@7 stays deferred. Advisor repo-public flag still needs confirming in the next update (non-blocking). |
| 2026-07-16 | **P3.2 Stage 2 — `noise` @ seed 23: coordination tolerates dual-channel belief noise; margin narrows but ordering holds** ✅ | _(this commit)_ | 341 ✓ | **served 0.6486 / gini 0.2794 / Jain 0.7872 / $6.96.** Beats the zero-LLM control (0.6399, **+0.87**, the mandatory bar) AND round_robin (0.6444, **+0.42**); gap_closed control→LP **+6.8%**, rr→LP +3.4%. **Pre-registered 0.65, band [0.62,0.69] → HIT** — the first Stage 2 cell to land where predicted, essentially on the point estimate. **The finding is the narrowed margin, not the win:** clean live @ 23 = 0.6726 → noise 0.6486 (−2.4 pts), so the LLM's edge over the control collapses from clean's **+4.6 to +0.87 pts** — noisy beliefs (SoC 10% + load 15%) cost the reasoning arm several times more than the fixed heuristic that barely reads the perturbed channel. Exactly the pre-registered "reasoning tolerates noise but pays for it" outcome: ordering preserved (live > rr > control), advantage compressed. Noise did NOT accidentally help (>0.69) nor break coordination (<0.62) — mechanism behaved as measured. Counter signal: **commitments_expired 149/2606 = 5.7%**, far below clean seed-23's 27.3% (914/3343) — under noise agents committed *less* and fulfilled *more* reliably (plausibly smaller, more-serviceable promises from clamped beliefs; n=1, unverified). parse failures 3.7% (<5%); react_refusals 0; zero comm/budget drops. Fairness moves with efficiency (Jain 0.7872, gini 0.2794, served-critical 0.8347). n=1 seed. **This completes the three authorized Stage 2 cells (defectors ✓ / comm ❗ / noise ✓); `all` @ 7 remains deferred (its comm component is confounded plumbing).** |
| 2026-07-16 | **P3.2 Stage 2 — `defectors` @ seed 7: coordination absorbs 33.6% defection, keeps 89% of its value** ✅ | _(this commit)_ | 341 ✓ | **served 0.7564 / gini 0.2002 / Jain 0.8658 / $6.07.** Beats the zero-LLM control (0.7337, **+2.3**, the mandatory bar) and loses to round_robin (0.7885, **−3.2**); gap_closed control→LP **+9.7%**. **Beating the control is strong here specifically because the control is *structurally immune* to prompt-realized defection (P3.1 T5 — no LLM reads the selfish prompt), yet live cleared it while carrying the full handicap** — the "interesting outcome" the pre-registration named. Honest headline is **"retains 89.1% of coordination value under 33.6% defection,"** NOT a baseline-beat claim (rr/control never touch the corrupted channel): coordination worth +0.3684 clean → +0.3284 here = 89.1% retained; naive proportional predicted 66.4%. **Pre-registered 0.71, band [0.68,0.75] → MISSED HIGH** — I under-modeled how well the 8 loyal haves absorb the withheld share. **Investigated the doubled commitment-expiry (9.0%→17.8%): it is NOT defectors reneging.** Defectors defect by ACCEPTing 59% less up front (have-defector commitments 396→162); the engine ships commitments in `act()` *before* the below-mean-SoC filter (`agent.py:759-797`) so no agent can renege at act-time; have-defectors make only 6.9% of all commitments (≤38% of expiries even if all expired) and hoard (high SoC → ample headroom), so their promises are the *least* likely to expire. The extra expiry is **honest haves over-promising against batteries depleted by the missing 33.6% supply** — a scarcity artifact. Fairness moves *with* efficiency (gini 0.2002, Jain 0.8658, served-critical 0.8805). n=1 seed; realized dose 33.6% pinned by `scripts/dose_check.py`. **noise@23 and all@7 deferred to a spend decision (below).** |
| 2026-07-16 | **P3.2 Stage 2 — `comm` @ seed 23: the LLM is WORSE than the zero-LLM control** ❗ | `c9b8b0d6` `cf8dbca7` | 341 ✓ | **served 0.4565 / gini 0.4803 / Jain 0.5665 / $4.03.** Stage 1: LLM **+5.8** over control; here **−4.6** (gap_closed control→LP −17.4%). Pre-registered 0.53 band [0.50,0.57] → **MISSED LOW: I predicted the advantage would vanish (live ≈ control); it inverted.** **Mechanism (measured, after two refuted hypotheses): negotiation has a bandwidth cost that exceeds its value under a per-tick message budget.** The LLM spends **824 delivered messages (19%) on ACCEPT/COUNTER/REJECT replies the control never sends**, and replies emit *first* (`llm_agent.py:299-303`), displacing the OFFER/REQUEST that move energy → control delivers **60% more** energy-moving traffic (2639 vs 1651) and serves more. Refuted: (a) "expired promises crowd out alternatives" — no such channel exists (recipients never parse ACCEPT; expiry ships/reserves nothing); (b) "LLM starves its own INFORMs" — it gets *more* INFORMs through (1823 vs 1467). **Scope: NOT "LLM coordination is fragile"** — send order is hardcoded plumbing the LLM can't control; the defensible claim is **"a negotiation protocol costs bandwidth that under scarcity can exceed the coordination it buys."** Follow-up bugs for 3.3: commitments appended before bus send (ship against dropped ACCEPTs); `agent.py:797-798` exports promised energy while below own threshold (~15% loss). n=1 seed. _(row back-filled 2026-07-16 — the comm commits omitted it.)_ |
| 2026-07-16 | **P3.2 Stage 1 — LIVE seed 1: the control win replicates (2/2)** ✅ | _(this commit)_ | 334 ✓ | **served 0.8248 / gini 0.1432 / $5.95.** Beats the zero-LLM control 0.7607 by **+6.4 pts** (seed 23: +4.6) and round_robin 0.8023 by **+2.3 pts** (seed 23: +2.8); gap_closed 11.9% rr→LP, 27.8% control→LP (LP 0.9914). **I pre-registered a prediction (~0.807, and warned the rr win might not replicate) — the result beat it on both counts.** Two seeds now clear both bars, on seeds whose round_robin scores differ by 16 pts (0.6444 vs 0.8023) — that spread is exactly why one seed was never claimable. Fairness again moves with efficiency, not against it: gini 0.1432 (control 0.1822), Jain 0.9015, served-critical 0.9193. All 6 checks pass; sanity gate OK (0.7107 ≤ 0.8248 ≤ 0.9914). **Ops: zero stalls, 22.6 calls/min — exactly seed 23's healthy-window rate, i.e. seed 23 minus the hangs. The 60s timeout remains UNPROVEN (no hang occurred to exercise it); do not credit it yet.** Note: the `end-of-file-fixer` hook resets cache-file mtimes at commit, so per-cell timing forensics must be measured BEFORE committing. |
| 2026-07-16 | **P3.2 Stage 2 — HALTED and redesigned: the defectors cell was a null by construction** 🛑 | `5c3d334c` `846a3206` `423c780e` | 341 ✓ | **Killed the running defectors cell at 502 paid calls (~$0.75, cached → $0 resume) after checking WHICH houses defect: at seed 23 all 6 land on have-nots (0 kW PV, 2–4 kWh batteries) → 0.0% of generation withheld.** The agents told to hoard had nothing to hoard. It would have returned ≈clean and the obvious write-up — *"robust to 20% defectors"* — would have been **false**: no defector was *capable* of defecting. A null that reads as good news is the worst kind to ship. `P = C(21,6)/C(30,6) = 9.1%` — a rare draw that landed on the one seed Stage 2 was built on; seeds 1/7/2/3/11/42 give a real 16–34% dose, so the design is sound and seed 23 is not. `all` is compromised there too (defector component = 0 → really just noise+comm). **Root cause is scenario design (advisor's 2026-07-15 point, with a number):** defectors are drawn uniformly over ALL households but only haves can withhold, so ~60% of every draw is inert by construction and the have-side dose swings 0–34% by seed — **any defector result must report its realized dose**. Shipped `scripts/dose_check.py` (exit 2 on inert) + 3 tests pinning inert@23 / potent@7 (33.6%) / none-in-clean. Existing failure-cell tests only asserted cells *parse* — configuration is not dose. **Leo's call: defector cells → seed 7; noise/comm stay at 23** (each cell vs its own same-seed control, never across cells). Provenance gap that hid it: `assign_defectors` returns a set and never writes back, so `defector_house_ids` stays `[]` and no artifact records who defected. |
| 2026-07-16 | **P3.2 Stage 2 preflight ($0) — the baseline matrix says round_robin cannot be the comparator** | `0294de71` `cd862f15` `6f817a48` | 341 ✓ | Ran $0 baselines for all four failure cells before spending. **round_robin and lp_optimal are BIT-IDENTICAL across clean/defectors/noise/comm/all — spread `0.000000` on both.** Only the control moves: comm −12.4, all −16.9, defectors +0.0, noise +1.3. Code trace: `failure_modes` has **exactly one application site** (`llm_agent.prepare`, which `llm_fallback` routes through); round_robin gets `message_bus=None` + reads engine ground truth, lp_optimal is a ground-truth oracle. The five YAMLs differ *only* in the `failure_modes` block, so for those two strategies all five cells are literally the same simulation. **This confirms Stage 1's comparator choice structurally, from a second direction.** Also: **reversed the preflight advice to skip `comm`** — with the control falling 12.4 pts there's a real hole for live to fall into or survive, making it a causal test of whether the P3 info-flow rework produced the Stage 1 win. **Retracted my own "noise helps" claim**: measured both channels — load-clamp bias is **+0.048%** (binds at z<−6.67, p≈1e-11) and the SoC clamp is *mean-reverting* (full battery reads 9.60, empty reads 0.40 → both REDUCE sharing). Nothing can systematically help; +1.3 is realization luck inside a ~16-pt spread. **`summary.json`'s `failure_modes_active` means CONFIGURED, not applied** — a round_robin comm-cell summary advertises `per_tick_budget: 2` while no comm code ran; caveat now in `sim/logging.py`. `.gitignore` now excludes $0 baselines (~40 MB of derivable JSONL was untracked inside a tracked tree, one `git add -A` from a public commit). |
| 2026-07-16 | **P3.2 ops + Stage 4 instrument ($0)** | `c70a9961` `7dc5d243` | 341 ✓ | **`max_retries=0` on the SDK client (TDD):** its default 2 nested inside this module's own 5 → up to 15 attempts at a known-dead endpoint, with the inner ones invisible to `base_backoff_s`. Stage 1 lost **82 of 228 min (36%)** to stalls, all the same signature (pooled connection to one edge IP wedged in an SSL read); the 60 s timeout bounded each at 16–32 min, this bounds them at ~5. Done *before* Stage 2 because 4 cells × that rate is most of a day. **`--rubric-variant` (TDD, advisor 2026-07-15 judge-consistency ask):** 3 paraphrases of ONE rubric (default/terse/roleplay) with axis names + the 1–5 scale deliberately fixed, so a score shift measures the judge's stability rather than the rewrite. Non-default variants write `explanations_eval__<variant>.json` — a second variant overwriting the first would destroy *paid* results (the paid-artifact overwrite from the 2026-07-12 pre-live review, now a test). Mock-verified end-to-end on the Stage 1 clean cell: 3 separate artifacts, same 5 messages sampled (pairing is what makes agreement computable — also pinned). Stage 4 was blocked on this; it isn't now. |
| 2026-07-16 | **P3.2 Stage 1 COMPLETE — 3/3 seeds beat the zero-LLM control** ✅ | `a094d425` | 334 ✓ | **served 0.7964 / gini 0.1597 / $5.91 (seed 7).** 3-seed aggregate: **vs control +5.8 ± 1.0 pts (3/3, tight); vs round_robin +2.0 ± 1.1 but decaying 2.8 → 2.3 → 0.8.** gap_closed control→LP **29.0% ± 2.9**; rr→LP 13.0% ± **9.2** (5× the spread). **The claimable result is "LLM coordination closes ~29% of the control→LP gap, consistently across seeds" — NOT a round_robin claim, which is seed-dependent and wouldn't survive added seeds.** Seed 7 was pre-registered (~0.79, "most likely to beat control but tie/miss rr") → actual 0.7964, +0.8 vs rr. **First replicated live evidence the LLM layer adds value** (P2.8 live Haiku LOST to rr 0.513/0.525; P2.9 control TIED it — the info-flow rework changed the answer). **No efficiency–equity tradeoff: gini and Jain improve alongside served-load on all 3 seeds** — the advisor expected the tradeoff to be "more interesting than the absolute numbers"; the finding is it doesn't appear. Caveats: n=3 is small; `min_house_served_fraction` degenerate (identical across strategies) so the Rawlsian floor contributes nothing. **Total Stage 1 spend $17.45.** |
| 2026-07-15 | **P3.2 ops fix — pin a 60 s LLM request timeout (TDD)** ✅ | _(this commit)_ | 334 ✓ | Stage 1 lost **35 min of its 213 min (16%) to 3 hung HTTPS reads**, one costing 23 min alone. Root cause: `AnthropicLLMClient` built the SDK client with defaults — **`read=600 s` and the SDK's own `max_retries=2`** — so one wedged connection burns 30 min before this module's retry loop (`max_retries=5`) even sees an exception; the two retry layers compound. Now pins `httpx.Timeout(60.0, connect=5.0)` on both auth paths (api-key and OAuth): worst-case hang per app-attempt drops 30 min → 3 min. 60 s is ~10x the p99 for an 800-token Haiku call, so it can't abandon a healthy request — which matters because a client-side abort still bills server-side, making 'err long' the safe direction. **Safe mid-campaign: a timeout touches neither prompts nor cache keys, so cached responses stay valid and cross-cell comparability is untouched** (verified: Stage 1's 4,461 cached responses still replay). Also declares `httpx` in `pyproject.toml` — `sim/agents/llm.py` now imports it directly and it was only a transitive `anthropic` dep, the same packaging trap that shipped red CI in Tasks 11–14. Clean-install dry-run in a fresh venv green. |
| 2026-07-15 | **P3.2 Stage 1 — LIVE clean cell (Haiku, seed 23): the LLM layer beats the zero-dollar control for the first time** ✅ | _(this commit)_ | 333 ✓ (unchanged) | **served 0.6726 / gini 0.2704 / $5.59.** Beats the pinned zero-LLM control (0.6269, **+4.6 pts**) AND round_robin (0.6444, **+2.8 pts**); **closes 22.7% of the rr→LP gap** (LP 0.7684), 32.3% of control→LP. **This is the first live result where LLM reasoning demonstrably adds value** — Phase 2.8's live Haiku *lost* to rr (0.513 vs 0.525) and the P2.9 control *tied* it (0.518 vs 0.5194), which is what motivated the whole P3 information-flow rework. Fairness moves the same direction (never Gini alone): gini 0.2704 and Jain 0.7936 are both best-of-three; served-critical 0.8347 (non-vacuous, P3.1 T19 substrate working). **`min_house_served_fraction` = 0.0357 identical across live/rr/control → the Rawlsian floor is degenerate here and cannot discriminate strategies; the 3.3 fairness panel rests on gini + Jain (advisor flag).** Verification: all 6 playbook checks PASS; sanity gate OK; 18/665 plan-parse failures (2.7% < 5% bar); 3,343 commitments made / 914 expired. **⚠️ NOT CLAIMABLE YET — single seed, and seed 23 is the *hardest* seed (its rr 0.6444 is the exact min of the mock sweep's [0.6444, 0.8023] range); cross-seed spread ~16–20 pts dwarfs the +2.8-pt delta. Seeds 1+7 gate (~$11 at real cost) is required for a defensible claim.** Ops: 3.3 h wall, **35 min (16%) lost to 3 hung-connection stalls** (SDK default 10-min timeout × max_retries=2; one stall cost 23 min) → 60s-timeout fix proposed pre-Stage-2 (doesn't touch prompts/cache keys, so comparability is unaffected). Two interruptions cost **$0** — the stable `--reference-cell` resume dir replayed every paid call (P3.1 T15 validated in the field). Artifacts 33 MB; `messages.jsonl` 9.5 MB — would have breached the old 5000 kB pre-commit limit raised earlier today. |
| 2026-07-15 | **P3.2 preflight ($0): reasoning-advantage mock pass + large-file limit raised** ✅ | _(this commit)_ | 333 ✓ (unchanged) | **Candidate "reasoning-advantage" cell LOST — no new YAML; `__comm` stays as shipped.** Control (5 seeds; harness validated — reproduces the committed matrix exactly at clean 0.6993 / budget-2 0.5524). **`__comm`'s 16.0-pt damage = budget 14.7 + drops −0.1**: per-circle drops are INERT (even a 90% geographic drop costs 0.4 pts — messages are re-sent every tick), and routing headroom under a generous bound is only +1.2 pts. **Mechanism:** the budget kills 83% of INFORMs (emitted last) so only 46% of the 126 (sender,peer) pairs ever exchange one → 68 permanently invisible → unknown peers are ineligible recipients. Damage is **eligibility, not staleness**; `emit_informs()` is not policy-controlled, so **no LLM policy can fix it — `__comm` measures plumbing, not reasoning (expect live ≈ control)**. Compounding: fallback policy is geographic-only (`policy.py:83`), only 22.2% of pairs are overlay-routable, and the prompt exposes circle membership but never reliability while steering away from circle routing (`agent.py:41-42`). **Trust-circle routing has no substrate in this architecture** → post-3.2 prompt/mechanism work; raise with Yongfeng. Also: `check-added-large-files` 5000 → 15000 kB (Leo's call) — the 24 h `messages.jsonl` measures **6.4 MB at $0 on the control**, so Stage 1's commit would have failed AFTER the spend; real per-cell artifact cost is ~42 MB, not the playbook's 5–15. **Live spend still blocked: `ANTHROPIC_API_KEY` is not set anywhere.** |
| 2026-07-15 | **Advisor Phase-3 sign-off recorded (docs only)** ✅ | _(this commit)_ | 333 ✓ (unchanged) | Yongfeng answered the 3.2 preflight items: defector realization, fairness metrics, and LLM-judge all approved as shipped → **Stages 2+4 unblocked (budget gates remain)**. Doc updates: playbook (gate checked; Stage 3 Sonnet ablation now optional/last; Stage 4 gains a rubric-variant consistency sub-step, judging budget ~$3–7; $0 reasoning-advantage mock pass added to preflight); roadmap (3.3 efficiency-vs-equity tradeoff figure; venues → CHI/ICLR/WWW main-or-workshop via mlciv.com, supersedes 2026-05-26 list); Phase 3 spec [ADVISOR] flags annotated confirmed; CLAUDE.md venue + sign-off section. Repo-public flag not acknowledged in the reply — confirm with advisor; non-blocking. |
| 2026-07-12 | **P3.1 Task 23 — wrap-up: full verify, clean-install dry-run, security pass; tagged `phase3.1-complete`** ✅ | _(this commit)_ | 333 ✓ | **Phase 3.1 pre-live hardening COMPLETE (23/23 tasks; 3 criticals fixed).** 333 tests; ruff + mypy --strict clean; **clean-install dry-run in a fresh venv green** (packaging clean after the reflection deletion). Headline 3-seed comparison for the record: **haves_havenots_solar** no_coord 0.4604 / rr 0.8090 / rr_overlay 0.8160 / LP 0.9870 (~18-pt rr→LP gap); **haves_havenots** no_coord 0.4875 / rr 0.5582 / LP 0.5692. Security pass clean (no secrets in the phase diff; `yaml.safe_load`, argv-list subprocess, user-controlled local paths). Live spend unblocked → Phase 3.2. |
| 2026-07-12 | **P3.1 Task 22 — README reflects the Phase 3 codebase; dead Reflection module removed** ✅ | _(this commit)_ | 333 ✓ | `Reflection` was dead production code (only its own test imported it; the real reflection lives in `LLMAgent.plan()`) — deleted `sim/agents/reflection.py` + `tests/test_reflection.py` + the `__init__` docstring entry (F46). README overhauled: status → Phases 1–3.1 complete; real `sim/`/`sim/agents/`/`sim/adapters/`/`sim/strategies/`/`scripts/` architecture tree; output path `<strategy>/<ts>-<pid>/` + `messages.jsonl` row; unclamped gap_closed table (no_coord −639.62%); replay quickstart replaced with the tag-pinned, non-destructive version; "run live" now points at the Phase 3.2 playbook (F29/F30/F5). |
| 2026-07-12 | **P3.1 Task 21 — golden pins for the zero-LLM control; mock dose-response matrix regenerated under gated defectors** ✅ | _(this commit)_ | 336 ✓ | The mandatory control baseline had no pin (tests asserted only `0<served<1`); the mock matrix was stale (F25/F32/F3-fallout). Pinned `llm_fallback` control to 6-dp: **haves_havenots__llm 0.519612 / gini 0.318512 / 263 transfers; haves_havenots_solar__llm 0.626872 (was 0.6910 pre-hardening) / gini 0.287016 / 966 transfers** — the drop is the T1/T7–T9/T19 behavior changes, within the [0.60,0.74] sanity band. Regenerated `docs/phase3_mock_sweep.md`: **defector rows now FLAT (T5 fix confirmed — the old moving rows were the ungated-wrapper artifact)**, rr flat everywhere, comm still devastating (0.699→0.440). Updated README + CLAUDE.md control number 0.6910 → 0.6269. |
| 2026-07-12 | **P3.1 Task 20 — showcase failure-cell + Sonnet-ablation YAMLs; current Opus pricing** ✅ | _(this commit)_ | 334 ✓ | The committed failure cells targeted the superseded 12h scenario with wrapper realization; the Sonnet model id was pinned nowhere; the Opus price row was 3× stale (F6/F31/F50c). Created 5 solar-showcase cells (`__defectors`/`__noise`/`__comm`/`__all` — **defectors use `prompt` realization per the mock-sweep conclusion + advisor mandate; wrapper reachable via `--set`** — plus `__llm_sonnet` pinning `claude-sonnet-5`); all load under the T3 strict loop. `_PRICE_PER_MTOK` opus row corrected $15/$75 → $5/$25 (Opus ≥4.5), pinned by a cost test. |
| 2026-07-12 | **P3.1 Task 19 — live-run YAML samples `critical_load_frac`; fairness metrics validated + pinned** ✅ | _(this commit)_ | 328 ✓ | Every planned live cell recorded `served_critical_load_fraction = 1.0` vacuously — the needs-aware fairness pillar had zero substrate in any real experiment (F8); `min_house_served_fraction` had no test (F19). `sample_households` now hard-rejects a malformed `critical_load_frac` (scalar → ValueError, not a downstream TypeError); `haves_havenots_solar__llm.yaml` gains `critical_load_frac: [0.2, 0.6]` (live family only — the extra draw shifts that YAML's household sequence; base goldens untouched, verified). Pinned the Rawlsian floor end-to-end + the live YAML's non-vacuous served-critical fraction. |
| 2026-07-12 | **P3.1 Task 18 — LP: honest discharge bound (rate·√η); gini counts zero-load houses; partial-island coverage** ✅ | _(this commit)_ | 324 ✓ | `dis ≤ rate` let the LP serve own load at bus-side `rate` while the engine tops out at `rate·√η` — needlessly loose exactly where winter/heatwave scenarios will live (F51); LP gini excluded zero-load houses while the engine counts them at 1.0 (F53); the partial-island constraint paths had never executed (F52). Tightened the discharge upper bound to `rate*se` (golden LP pins byte-identical — never binding on shipped scenarios); gini now scores zero-load houses 1.0 to match `sim/logging.py`; added a partial-island test (3 & 5 islanded of 6, LP ≥ round_robin) and a zero-load gini-parity test. |
| 2026-07-12 | **P3.1 Task 17 — explanation judge: OAuth-correct client + decision-time state pairing** ✅ | _(this commit)_ | 322 ✓ | The judge 401'd under this project's OAuth workflow (client built with `api_key=""` bypassed the `sk-ant-oat` → `Authorization: Bearer` routing), and it graded rationales against the POST-step state row — systematically penalizing honest explanations (F11). Default client now reads `ANTHROPIC_API_KEY` (mirrors `_make_llm_client`); `evaluate_run` pairs each message with the sender's PRIOR-tick (decision-time) state and excludes first-tick messages (`n_first_tick_excluded` reported); prompt/rubric relabeled to "decision-time state". Existing single-tick fixture gained a prior tick. |
| 2026-07-12 | **P3.1 Task 16 — compare.py persists run artifacts; n/a gap without round_robin** ✅ | _(this commit)_ | 320 ✓ | `--strategies llm_agent` cached PAID responses into a `TemporaryDirectory` deleted on exit (F10); `gap_closed` silently substituted rr=0.0 when round_robin wasn't in the strategy list, printing served/lp as if it were the paper's metric (F36). `_collect` now takes a required `out_root` and persists per-strategy run dirs (`runs/compare/<ts>-<pid>/seed<N>/` by default; `--out-dir` override); `format_table`/`format_aggregate` default rr/lp to NaN so `gap_closed` reports **n/a** whenever the baseline or ceiling is absent. |
| 2026-07-12 | **P3.1 Task 15 — reference prompt-cache fallback resolves (CRITICAL)** ✅ | _(this commit)_ | 318 ✓ | `_reference_cache_dir` walked one level short (`run_dir.parent.parent.parent` = `runs/`, not the repo root) and returned None for EVERY real layout — the two-tier cache's reference tier had never worked, so a mid-run crash on a paid live run would re-pay everything the relaunch couldn't find locally (F2). Now resolves the run dir and takes the repo root as `parents[3]`; both `runs/…/<ts>` and `reference_runs/…/<cell>` layouts resolve; shallow paths return None without crashing. **All 3 critical findings (T5, T7, T15) now fixed.** |
| 2026-07-12 | **P3.1 Task 14 — sweep tolerates failed cells; writes the report incrementally** ✅ | _(this commit)_ | 317 ✓ | One failed subprocess aborted the whole grid and discarded every completed cell's tabulation; the docstring promised per-cell `summary.json` copies that don't exist (F37, F13-doc). `run_grid` now wraps each cell in try/except (records `FAILED: <msg>` in-place and keeps sweeping) and rewrites `report.md` after every axis; docstring corrected to describe the real `cells/` layout. |
| 2026-07-12 | **P3.1 Task 13 — control runs log messages + counters; run dirs unique per seed & PID** ✅ | _(this commit)_ | 316 ✓ | `llm_fallback` runs wrote no `messages.jsonl` and reported all-zero `message_counts` (the control's provenance lied; `eval_explanations` crashed on it); `--reference-cell` ignored `--seed` so seed replicates overwrote each other's PAID artifacts (F12); 1-second timestamps collided under the sweep (F13). Widened both `llm_agent`-only gates in `run.py` to include `llm_fallback`; reference-cell dirs gain a `__seed<N>` suffix; timestamped run dirs gain a `-<pid>` suffix. |
| 2026-07-12 | **P3.1 Task 12 — bus latency wired to `dt_hours`; end-of-run queue logged; real bus-neutrality test** ✅ | _(this commit)_ | 313 ✓ | Both `MessageBus` construction sites (`llm_agent.prepare`, `scripts/run.py`) used the hardcoded 15-min default, breaking the one-tick contract for other tick lengths (F43); the final tick's accepted messages vanished from `messages.jsonl` (F44/45); the bus-neutrality test never passed a bus (F55). Both sites now pass `dt=timedelta(hours=scenario.dt_hours)`; new `MessageBus.flush_undelivered()` logs still-queued messages as `pending_at_end` (counted by `phase2_message_counts`); the neutrality test now runs round_robin *with* a bus and asserts `messages.jsonl` exists. |
| 2026-07-12 | **P3.1 Task 11 — pin commitment partial-fulfillment/ordering/expiry; scope `_make_llm_client` patches** ✅ | _(this commit)_ | 310 ✓ | Test-only. The budget-capped partial serve (THE new Phase 3 mechanism) had ZERO assertions — a `* dt_hours` mutation survived the suite (F24). Added 3 pins (carry-across-ticks, list-order competition, partial-serve-then-expiry) + verified the mutation now fails and reverts clean. Hygiene: replaced 4 bare `llm_strat._make_llm_client = <fake>` leaks with `monkeypatch.setattr` (threaded through `_run` + 3 tests in failure_axes). |
| 2026-07-12 | **P3.1 Task 10 — negotiation counters reach summary.json; react-skip counted once; unparsed-react counter** ✅ | _(this commit)_ | 307 ✓ | `n_commitments_made/expired` + `n_react_amount_defaulted` were incremented but never aggregated (spec A3's "logged counter" silently dropped); `n_react_skipped` recounted the same waiting message every tick; malformed react replies vanished (F22/F23/F38). Added `n_react_unparsed`, fixed the skip recount (count each deferred message once at its arrival tick), and surfaced all four counters via `current_call_counts` → `summary.json`'s `llm_call_counts_detailed`. |
| 2026-07-12 | **P3.1 Task 9 — REQUEST fan-out splits the need across deduped recipients** ✅ | _(this commit)_ | 304 ✓ | `_emit_requests` sent the FULL next-tick deficit to each of 3 peers every tick (~6× over-commitment), and a peer sharing two trust circles could get two copies (F17). Now dedups recipients (highest-weight circle wins, deterministic tiebreak by weight desc / id asc) and divides `need_kwh` evenly across the deduped top-3. |
| 2026-07-12 | **P3.1 Task 8 — only REQUESTs consume react LLM calls** ✅ | _(this commit)_ | 302 ✓ | OFFER replies are pure chatter (only REQUEST replies create commitments; senders never parse ACCEPT/REJECT), yet each OFFER queued for react was a PAID LLM call that could starve genuine REQUESTs under `react_max_per_tick` (F14). `observe()` now queues only REQUESTs; OFFERs stay memory-only (still logged as importance-6 msg_recv). |
| 2026-07-12 | **P3.1 Task 7 — react prompt carries own state + open commitments (CRITICAL); TTL wording** ✅ | _(this commit)_ | 301 ✓ | Binding ACCEPT/COUNTER replies were built with ZERO own-state in the prompt — live cells would measure prompt blindness, not reasoning (F1). `_react_to_message` now prefixes the react prompt with the household's noised self-view (SoC, headroom above DoD floor, load, solar, max discharge) + total open-commitment kWh. Also fixed the prompt's TTL wording ("binding for the next 2 ticks" → "3 serviceable ticks (this one and the next two)") to match the ledger's real behavior (F39) — wording only, behavior unchanged. |
| 2026-07-12 | **P3.1 Task 6 — plan prompt renders belief age; observe() snapshots post-ingestion beliefs** ✅ | _(this commit)_ | 300 ✓ | `observe()` folded INFORMs AFTER snapshotting peer beliefs, so plan prompts lagged `act()` by one tick (F16/F49), and spec A1's "reported N ticks ago" staleness was never rendered (F15). Reordered `observe()` to fold INFORMs first, then snapshot beliefs with an `age_ticks` field; `_peers_summary` now prints "reported N tick(s) ago". Dropped the redundant `peer_states` param (agent derives its own snapshot) — updated 32 call sites + the facade. |
| 2026-07-12 | **P3.1 Task 5 — DefectorWrapper gated on `defector_realization` (CRITICAL)** ✅ | _(this commit)_ | 299 ✓ | `prepare()` applied wrapper channel-corruption to every defector regardless of realization, conflating the paper's two defector conditions — the mock sweep's llm_fallback defector dose-response existed ONLY because of this bug (F3). Wrapper now fires only for `wrapper`/`both`; `prompt` cells get an empty defector set (`maybe_corrupt` = identity) so they measure LLM-driven defection, not line noise. Verified: the zero-LLM-control prompt-defector cell is now byte-identical to clean (was served 0.7477 vs 0.6907). **`docs/phase3_mock_sweep.md` defector rows are now KNOWN-STALE — regenerated in T21, not here.** |
| 2026-07-12 | **P3.1 Task 4 — Policy validation upper bounds (API tool schemas are advisory)** ✅ | _(this commit)_ | 297 ✓ | The Anthropic API does not enforce tool-schema min/max, so a live Haiku emitting `share_min_soc_frac: 30` (percent confusion) silently disabled all discretionary sharing for a whole cell — and the selfish-defector prompt pushes that direction (F18). `_validate` now bounds `share_min_soc_frac`∈[0,1], `max_share_kw_per_tick`∈[0,100], `share_fraction_per_tick`∈[0,1]; an out-of-range live policy takes the parse-failure path (agent keeps prior policy, `n_plan_parse_failures`++). |
| 2026-07-12 | **P3.1 Task 3 — hard-error on unknown scenario/failure-mode/llm YAML keys; drop dead solar-forecast channel** ✅ | _(this commit)_ | 289 ✓ | `load_scenario` now rejects unknown top-level + `llm` keys and `FailureModeConfig.from_dict` rejects unknown `failure_modes`/`obs_noise`/`comm` keys — a typo like `failure_mode:` or `modle:` silently ran the wrong experiment (bit us twice via `react_max_per_tick`/`solar_forecast_*`, F41/F7). Deleted the parsed-but-unused `solar_forecast_*` fields (no consumer) from `ObsNoiseConfig` + 2 YAMLs; `household_sampling` left unchecked (mode-dependent keys). Also cleaned stale dead `policy_refresh_every_ticks`/`require_rationale` from a test fixture. |
| 2026-07-12 | **P3.1 Task 2 — validate bus params + defector enums at load time** ✅ | _(this commit)_ | 271 ✓ | `Scenario.__post_init__` now rejects `bus_loss_factor` outside [0,1) (silently produced negative received energy) and non-positive `bus_max_kw` (F48); `FailureModeConfig.from_dict` hard-errors on unknown `defector_assignment` / `defector_realization` values and drops the never-implemented `by_circle` (was accepted but silently ran `random`, F42). No shipped YAML used `by_circle`; all still load. |
| 2026-07-12 | **P3.1 Task 1 — receiver transfer cap subtracts the receiver's own solar surplus** ✅ | _(this commit)_ | 269 ✓ | Engine receiver cap admitted peer energy that `household.step()` then curtailed whenever the receiver's own PV surplus already filled its battery intake (review F20) — anti-coordination in direction (can't flatter the LLM) but it inflated daylight wasted-energy accounting. Cap now subtracts `own_surplus_kw`; base goldens byte-identical (the bug never fires on the no_coord/rr pins), only daytime PV-receiver cells move. Also corrected the `wasted_kwh` field comment (F47). |
| 2026-07-12 | **P3.1 Task 0 — verified review + full planning package (no code changes)** ✅ | _(this commit)_ | 267 ✓ | 9-reviewer adversarially-verified repo review at `phase3-infra-complete`: 55 confirmed findings (3 critical: defector-realization conflation, state-blind binding reacts, dead reference-cache fallback); core physics/LP/determinism/control verified healthy. Shipped: Phase 3.1 spec + 23-task fully-specified plan, Phase 3.2 live-run playbook (honest budget ~$92-220 sync), remaining-phases roadmap. CLAUDE.md: repo-public decision recorded, security-check + push rules added, test counts corrected 274→267, rows ≤2026-06-17 archived, stale pointers fixed. Phase 3 spec/plan stale anchors annotated. |
| 2026-07-07 | **P3 Task 11 — mock benchmark matrix + Phase 3 infra wrap** ✅ | _(this commit)_ | 267 ✓ | Full mock dose-response sweep (2 strategies × 3 axes × 4 values × 5 seeds = 120 subprocess cells) on the solar showcase → `docs/phase3_mock_sweep.md`. **round_robin FLAT on every axis (structural immunity, now empirical); comm budget is the devastating axis (0.702→0.460 served, gini 0.230→0.504); wrapper-corruption/noise mildly HELP served under fixed policies (symmetric belief corruption reads as neediness → more sharing) — so live LLM runs must beat accidental gains, and the real defector test is prompt-realized selfishness.** README Phase 3 section + CLAUDE.md status. Tagged `phase3-infra-complete`. |
| 2026-07-07 | **P3 Task 10 — solar showcase scenario: rr→LP gap 20.4 pts** | _(this commit)_ | 267 ✓ | Scans showed bus-tightening SHRINKS the gap (proportional scaling trims rr's over-sharing); the winning lever is TIMING: PV [5,7] kW on the haves over a 24 h outage. `haves_havenots_solar.yaml` (+ `__llm` variant): no_coord 0.4280 / rr 0.7666 / **LP 0.9711** — energy abundant but misplaced, coordination quality is the whole game, round_robin visibly fails (advisor's Phase 1.6 bar, finally met with 20× the old headroom). **Zero-LLM control: 0.6910, 7.6 pts BELOW rr — the control is no longer saturated, so a live LLM has 28 pts of demonstrable room.** Goldens pinned. |
| 2026-07-07 | P3 Task 9 — scenario overrides + sweep driver | _(this commit)_ | 268 ✓ | `run.py --set dotted.key=value` via new `sim/overrides.apply_overrides` (walks Scenario fields, nested frozen dataclasses, and dicts; YAML-coerced values; typo'd keys are HARD errors so a mistyped axis can't silently report the base scenario as treated). `scripts/sweep.py` runs grid YAMLs (base + one override at a time × strategies × seeds), subprocess-per-cell, emits per-axis dose-response markdown with mean [min,max] served + gini. `configs/sweeps/phase3_grid.yaml` ships the spec's default grid. Gotcha fixed en route: YAML parses bare `off` as boolean False — the messaging flag now accepts both. |
| 2026-07-07 | P3 Task 8 — explanation provenance + rubric judge harness | _(this commit)_ | 263 ✓ | Every message row now carries `templated: true/false` (react replies with parsed LLM rationales are false; f-string OFFER/REQUEST/INFORM rationales true) — the paper can finally distinguish LLM explanations from template text. `scripts/eval_explanations.py`: deterministic-sampled 3-axis rubric judge (state_accuracy / actionability / consistency, 1-5) pairing each rationale with the sender's LOGGED state at that tick; PromptCache-backed; --mock stub for tests. [ADVISOR] method sign-off flagged in the docstring. |
| 2026-07-07 | P3 Task 7 — needs-aware fairness metrics | _(this commit)_ | 260 ✓ | `HouseholdProfile.critical_load_frac` sampled from `household_sampling.critical_load_frac: [lo,hi]` (only when configured — existing scenarios stay byte-identical). Summary gains `min_house_served_fraction` (Rawlsian floor), `jains_index`, and `served_critical_load_fraction` under the documented rule that unmet energy hits flexible load first. Hand-computed pins for all three. The fairness pillar finally has substrate beyond Gini. |
| 2026-07-07 | **P3 Task 6 — failure-axis effect sizes measured (mock, canned-policy floor)** | _(this commit)_ | 257 ✓ | served / gini / transfers / delivery vs clean (0.5178 / 0.1735 / 560 / 100%): defectors-wrapper −0.12 pts, gini 0.172; noise −0.13 pts, gini 0.202; comm(budget 2) −0.17 pts, gini 0.301, delivery 21%, transfers 295; all-combined −0.49 pts, gini 0.330. **Headline: failure modes hurt FAIRNESS far more than efficiency in the fixed-policy regime — comm scarcity doubles gini. This is the floor live LLM reasoning must improve on, and the axis (equity under degraded information) where it plausibly can.** |
| 2026-07-07 | **P3 Task 5 — binding negotiation + process-stable RNG seeding** | _(this commit)_ | 257 ✓ | REQUESTs now ask for the genuine next-tick shortfall from the noised view ((load−solar)·dt − battery deliverable; no deficit → no request), replacing the hardcoded 0.5 kWh. React replies parse `ACCEPT <kwh>` / `COUNTER <kwh>` into a binding `Commitment` ledger (TTL 2 ticks, bare-ACCEPT defaults counted); `act()` serves commitments FIRST, bypassing the below-mean filter (a promise is a promise), bounded by the policy power cap, with discretionary sharing drawing on the residual. llm_fallback control unchanged by design (no LLM → no commitments): re-verified 0.520/0.319/263. **Bonus bug found by a per-process-flaky test: ALL agent-layer RNG seeds used Python hash() on strings → salted per process (PYTHONHASHSEED), so defector assignment, noise draws, bus drops, and agent RNGs differed across processes — same disease as e56941d. New `sim/agents/seeding.stable_seed()` (sha256) everywhere + a subprocess test pinning identity across three hash seeds.** |
| 2026-07-07 | P3 Task 4 — act() decides on the noised self-view | _(this commit)_ | 247 ✓ | The share-vs-request branch, headroom math, and request sizing now consume `last_visible_own` (the observe()-time noised snapshot); the engine still settles against TRUE state, so imperfect self-knowledge causes over-promising/under-sharing exactly as the robustness axis intends. Branch-revealing test pins which value act() consults. |
| 2026-07-07 | **P3 Task 3 — ground-truth peer feed CUT; failure axes now causally couple** ✅ | _(this commit)_ | 246 ✓ | Facade no longer passes engine states as peer knowledge; `act()`'s below-mean filter and the plan prompt's peers section consume `peer_beliefs` (INFORM-borne, with age annotation) — unknown peers are ineligible recipients (never share blind). **The three failure-axis tests now assert (and pass!) real settled-outcome deltas vs clean: noise perturbs beliefs→routing, wrapper corruption of defector INFORMs lands on consumers, comm drops destroy the knowledge sharing depends on. messaging-off now collapses transfers to 0 — communication finally causally matters (pre-P3 it changed nothing, proving messages were decorative).** Zero-LLM control re-derived under new architecture: served 0.520 / gini 0.319 / transfers 263 (was 0.518/0.315/268). |
| 2026-07-07 | P3 Task 2 — message-borne peer beliefs (PeerBelief store + INFORM emission) | _(this commit)_ | 246 ✓ | `PeerBelief` (soc/capacity/t_idx_reported) fed ONLY by received INFORMs (newest wins); `emit_informs()` broadcasts the agent's NOISED self-view to union-neighbors every tick, pure Python, sent LAST in the facade tick (replies > need signals > status). DefectorWrapper's existing INFORM `soc_kwh` corruption now has a consumer. Ground-truth feed still present — cut in T3. |
| 2026-07-07 | **P3 Task 1 — Phase 3 spec + plan committed** | _(this commit)_ | 243 ✓ | Spec: information-flow rework (INFORM-only beliefs, noised act(), binding negotiation) as the validity gate for failure cells; needs-aware fairness metrics; explanation rubric judge; --set overrides + sweep driver; showcase-tight scenario. 11 tasks. Live runs budget-gated (Part E). [ADVISOR] flags: defector realizations, fairness framing, judge method. |
| 2026-07-07 | P2.9 follow-up — LP gini is solver-version-dependent (degenerate optima); golden pin loosened | _(this commit)_ | 243 ✓ | First post-2.9 CI run failed: LP gini 0.36198 on CI vs 0.36170 locally — different scipy/HiGHS versions pick different optimal VERTICES with identical objectives. Only objective-derived LP values (served, unmet total) are cross-platform stable; the LP's per-house distribution (hence gini) is not. Golden test now pins served/unmet tightly and bounds gini to [0.30, 0.42] with an explanatory comment. **Paper rule: never quote LP gini beyond ~2 decimals; a fairness tiebreak in the LP objective would pin it properly (Phase 3 candidate).** |
| 2026-07-07 | **P2.9 Task 18 — wrap-up: corrected real-data numbers + figure + docs + tag** ✅ | _(this commit)_ | 243 ✓ | Clean-install dry-run in fresh venv green (243 tests). **Corrected real-data result (solar-tz + conservation fixes): 24h_resstock_outage no_coord 0.8425 / rr 0.8582 / LP 0.9111 — rr saves a REAL 21.4 kWh (old claim 35.7 was computed on inverted day/night solar) and the LP shows a genuine 5.3-pt coordination gap on real data.** Figure regenerated. README gains a Phase 2.9 section; CLAUDE.md gains a Phase 2.9 status block + corrected-numbers annotations + phase-table row. Tagged `phase2.9-complete`. |
| 2026-07-07 | P2.9 Task 17 — tool-use path test parity | _(this commit)_ | 243 ✓ | Every e2e mock test exercised the deprecated YAML-fence parse path while live runs use Anthropic tool-use — byte-identical replay determinism was only proven for a branch the paper's runs never take. Replay test now parametrized over text AND tool_input modes; malformed-tool_input unit tests cover the failure branch behind the policy_parse_failures statistic (1 failure keeps policy, 3 strikes → round-robin fallback). Integration test renamed `test_llm_agent_pipeline_integrates_...` — its old name claimed ‘beats round_robin’ while asserting only ≥0.9×rr. |
| 2026-07-07 | P2.9 Task 16 — llm_agent counters travel with the prepared closure | _(this commit)_ | 239 ✓ | `prepare()` now returns a closure bound to ITS OWN registry (exposed as `.registry`); `current_call_counts(registry=)` / `update_summary_with_counts(run_dir, registry=)` accept it. The module global remains as last-prepared convenience for scripts/run.py, but an in-process multi-cell sweep can no longer write cell B's LLM counters into cell A's summary. Subprocess-per-cell still documented as the preferred sweep pattern. |
| 2026-07-07 | P2.9 Task 15 — react queue survives across ticks (aging instead of silent clobber) | _(this commit)_ | 238 ✓ | `observe()` REPLACED `pending_react` every tick, destroying every deferred REQUEST/OFFER one tick later (19.5% of inbound negotiation traffic in the reference run, concentrated on the most-solicited donors at peak scarcity) while the docstring claimed re-queueing. Queue entries now carry their arrival tick, EXTEND across ticks, and age out after `react_stale_after_ticks` (default 2) into a counted `n_react_dropped_stale`. Deferred messages are provably answered next tick (new test). |
| 2026-07-07 | P2.9 Task 14 — wire llm knobs + run provenance + real cost accounting | _(this commit)_ | 236 ✓ | `react_max_per_tick` now actually read from the scenario llm block (was silently dead — a Phase 3 sweep varying it would have changed nothing); dead keys `policy_refresh_every_ticks`/`require_rationale` deleted from all YAMLs. config.json now records llm + failure_modes + solar_tz_offset_hours; summary.json `failure_modes_active` filled from the scenario (was hardcoded {}). `LLMClient` counts fresh-call tokens (cache hits free) and `update_summary_with_counts` writes a real `llm_cost_usd_estimated` at per-model $/MTok rates (was hardcoded 0.0 while actual recorded spend was ~$11.6). |
| 2026-07-07 | **P2.9 Task 13 — zero-LLM control strategy + messaging-off flag; CONTROL RESULT** ✅ | _(this commit)_ | 231 ✓ | New `sim/strategies/llm_fallback.py`: identical llm_agent machinery with the LLM permanently disabled (agents keep the tuned initial fallback policy; zero API calls, $0). Plus `llm: messaging: off` ablation flag (suppresses ALL bus traffic; mock-tested). **Control run on haves_havenots__llm: served 0.518, gini 0.315, transfers 268 — within 0.15 pts of round_robin (0.5194) and ABOVE the Phase 2.8 live Haiku run (0.513, old physics). The hand-tuned executor, not LLM reasoning, accounts for essentially all clean-cell performance. This is the mandatory baseline the paper's Phase 3 comparisons must beat.** |
| 2026-07-07 | P2.9 Task 12 — multi-seed compare + seed override + unclamped gap_closed | _(this commit)_ | 228 ✓ | `run.py --seed N`; `compare.py --seeds a,b,c` prints per-seed tables + a mean/min/max aggregate with PAIRED per-seed gap_closed (all strategies share each seed's household draw). gap_closed is now honest: negative when below round_robin (old clamp hid ‘worse than baseline’ — the live LLM cell's raw value was -273% on old numbers), n/a when lp≈rr (old clamp reported a zero denominator as 0% progress). `--strategies` extension point for the T13 control. |
| 2026-07-06 | P2.9 Task 11 — golden-number regression pins | _(this commit)_ | 224 ✓ | `tests/test_golden_numbers.py` pins haves_havenots served/unmet/gini for no_coord (0.455996/195.84/0.485104), round_robin (0.519448/173.00/0.224442) and the LP ceiling (0.529368/169.43/0.361703) to 6+ dp. Byte-determinism makes exact pins free; a physics regression that shifts all strategies together is no longer invisible. |
| 2026-07-06 | P2.9 Task 10 — Gini + baseline numeric pins | _(this commit)_ | 221 ✓ | `_gini` gains 6 hand-computed unit pins ([0,1]→0.5, [0,0,1]→2/3, 4-value case — previously only presence-checked). round_robin pinned numerically: exactly 2 transfers of (13.0-1.35)·0.05/0.25/2 = 1.165 kW each, above-mean houses receive nothing, empty when nobody islanded; overlay pinned to the identical share formula (2.0 kW across an owner edge). The paper's reference baseline can no longer drift silently. |
| 2026-07-06 | P2.9 Task 9 — outage validation at scenario load | _(this commit)_ | 212 ✓ | `load_scenario` now hard-rejects outage windows referencing unknown house ids (a typo silently exempted the house from the outage) and windows entirely outside [start, end) (silently inert — scenario runs grid-up and reports great numbers). All 13 shipped YAMLs verified to still load. |
| 2026-07-06 | P2.9 Task 8 — ResStock native-interval conversion + period-ending realignment | _(this commit)_ | 210 ✓ | kWh→kW now divides by the DATA's inferred native interval (15 min), not scenario dt — a dt=0.5 scenario silently halved every real load. Real ResStock timestamps are period-ENDING (first row 00:15); adapter now shifts them back one interval when detected, so loads no longer lag one tick and year-start scenarios don't crash. New `resstock_period_ending_sample.csv` fixture pins agreement with the period-beginning twin. |
| 2026-07-06 | P2.9 Task 7 — NREL solar timezone alignment + machine-independent noise seed | _(this commit)_ | 208 ✓ | **All pre-P2.9 real-data numbers were computed on a solar curve shifted ~+6 h**: fetch_data requests NSRDB with utc=true but the sim read timestamps as naive local (austin_2018.csv GHI peaks at 18:00—19:00 “local”; the 12 h ‘overnight’ outage actually contained MORE sun than the day). Fix: `NRELSolar(tz_offset_hours=)` + new `Scenario.solar_tz_offset_hours` (set to -6 in all three resstock/real YAMLs) + loud load-time validation (mean-GHI-by-hour peak must fall 10:00-16:00, else ValueError naming the UTC pitfall). Noise seed now derives from calendar fields instead of naive `datetime.timestamp()` — the old seeding gave DIFFERENT results per machine TZ (test proves it via tzset); cross-platform constant pinned. Real-data numbers re-derived in Task 18. |
| 2026-07-06 | P2.9 Task 6 — settlement duplicate-pair aggregation, LP self-pair renormalize, t=0 outage event | _(this commit)_ | 204 ✓ | `settle_transfers` kept only the LAST of duplicate (sender,receiver) transfers (dict-comprehension clobber) while scaling against the full total — now aggregates pairs before cap scaling. LP `_schedule_from_solution` silently dropped a house’s self-pair share when it both sent and received in one tick — now excludes self and renormalizes over the remaining pool (gross conserved, unit-tested). Engine transition tracker starts at True so outages active at t=0 (the whole haves_havenots family) emit OUTAGE_STARTED. |
| 2026-07-06 | P2.9 Task 5 — LP curtailment slack + engine-feasibility bounds | _(this commit)_ | 201 ✓ | The LP power balance was a strict equality — any islanded solar surplus beyond total absorption made the WHOLE program infeasible (hard crash), and the LP had never been solved with nonzero PV. Added a zero-cost `curt` variable (mirrors engine wasted-energy semantics) + per-tick bounds `send ≤ min(bus, solar+rate·√η+grid)`, `recv ≤ min(bus, load+rate)` — pure relaxations of the engine caps, so the LP stays a valid ceiling. Showcase ceiling pinned by regression test at 0.529368385 (verified identical pre/post). Unblocks Phase 3's winter/heatwave solar scenarios. |
| 2026-07-06 | **P2.9 Tasks 3+4 — energy-conservation fix (THE bug) + re-derived numbers** ✅ | _(this commit)_ | 199 ✓ | New `engine._transfer_caps()`: sender cap is now load/solar-aware with √η on the whole battery term (`max(0,(solar−load)+min(rate,avail/dt)·√η [+grid])`); receiver cap gains the DC-bypass load-serving term (`max(0,load−solar)+absorption`). Strict mode now asserts `achieved_net_export_kw == desired` every tick — the invariant that would have caught this on day one. **Re-derived numbers:** haves_havenots rr 0.5250→**0.5194** (0.56 pts were phantom), honest rr→LP gap now **1.0 pt** (was 0.44); overnight_outage_hard rr==nc exactly at 0.8300 (the "30 kWh saving" was 100% phantom; LP 0.8642 shows 73.8 kWh of REAL headroom rr can't reach); synthetic_lp_smoke rr 0.759→**0.8375** (receiver-cap fix unblocked real load-serving transfers). Integration test rewritten: conservation regression on overnight_outage_hard + genuine-help test on lp_smoke. LLM reference cell (0.513) is now old-physics; live re-run = Phase 3 deliverable. Tasks 3+4 share one commit so no red-suite commit exists. |
| 2026-07-06 | P2.9 Task 2 — household physics test hardening | _(this commit)_ | 190 ✓ | `test_rt_efficiency_full_cycle` was a tautology (asserted test-side arithmetic; a broken discharge-leg √η passed it). Rewritten to pin the discharge step's actual soc/unmet/wasted. Added the first-ever tests of the receive leg (negative `desired_net_export_kw`) and the export-shortfall safety-net branch (`household.py:116-123`) — the code paths every P2P transfer relies on. |
| 2026-07-06 | **P2.9 Task 1 — docs catch-up + Phase 2.9 spec/plan** ✅ | _(this commit)_ | 188 ✓ | Full-repo review (two adversarially-verified passes, 65 findings) kicked off Phase 2.9 correctness hardening: spec + 18-task plan in `docs/superpowers/`. This commit back-fills the three missing Phase 2.8 progress rows, updates README/CLAUDE.md status to phase2.8-complete, and fixes the haves_havenots header comment (24 h → 12 h). |
| _2026-05-14 → 2026-06-17_ | _Phases 1 → 2.8: 76 rows archived_ | — | — | See [docs/progress_log_archive.md](docs/progress_log_archive.md) — moved 2026-07-12 to halve the resume-read cost; the append-every-task rule is unchanged (new rows go here). |

## Locked Phase 1 design decisions

These are decisions you should NOT re-litigate without explicit user re-approval:

- **Physics:** energy model + basic constraints (battery rate, RT efficiency 0.9, DoD floor 0.1, bus transit loss 0.05). No AC power flow.
- **Temporal:** 15-min ticks for both physics and decisions in v1. Architecture supports evolving to 5-min physics / 15-min decisions in Phase 2.
- **Neighborhood:** 30 households on a 5×6 grid. 4-neighbor communication graph + shared physical bus (50 kW default). Configurable.
- **Data:** NREL ResStock primary (advisor confirmed 2026-05-26 — public reproducibility is a paper strength; Pecan Street dropped, can't get HS-student affiliation) + NREL NSRDB for solar irradiance. Adapter pattern in `sim/adapters/`. **Phase 1.6 adds 1-2 more public datasets** for cross-dataset robustness (another ResStock state, NREL ComStock, or EULP).
- **Outage model:** schedule of `(start, end, affected_houses)` tuples. Partial-island supported but **no wheeling** (grid-connected houses can't pass grid energy through the bus to islanded neighbors).
- **Coordination plug-point:** `decide_transfers(t, states, households, solar, load, grid, neighborhood, dt_hours) -> list[Transfer]`. Phase 2 LLM agents plug in here without modifying the simulator core.
- **Output:** state.jsonl + events.jsonl + summary.json under `runs/<scenario_id>/<timestamp>/`. `messages.jsonl` reserved for Phase 2.

## Locked Phase 1.6 design decisions (advisor 2026-05-26)

These are advisor-mandated and gate Phase 2. Do NOT skip or re-scope without re-approval.

- **Communication graph — ownership/management overlays.** 4-neighbor geographic adjacency is insufficient on its own. Add cross-cutting edges that follow real coordination relationships: single owner of multiple properties, property-management companies, HOAs, demand-response aggregators. Agents end up in multiple *partially overlapping trust circles* (geographic neighbors, same owner, same manager). This is the structure where natural-language negotiation should outperform fixed protocols — it's the core research setting, not a nice-to-have.
- **Stress scenarios that break round-robin.** Current 12h-July-overnight (98.5% no-coord ceiling) is too forgiving. Build scenarios where round-robin leaves *substantial* unmet demand and unequal welfare. Directions: 24-72h or repeated outages with no recovery window, winter morning peak with low solar, heterogeneous "have" vs "have-not" battery/solar capacity, heatwave + AC load. Target a regime with meaningful unmet kWh and Gini well above zero after round-robin.
- **Centralized LP upper-bound baseline.** Add a centralized linear-program allocator with full information as a third strategy. It defines the achievable ceiling. Reframe all results as **"LLM coordination closes X% of the gap between round-robin and LP-optimal"** rather than "beats round-robin by Y%".
- **Welfare measure:** served-load fraction confirmed fine for Phase 1; needs-weighting (medical loads, thermal comfort thresholds) in Phase 3 confirmed as the right trajectory.

## Locked Phase 2 design decisions (advisor 2026-05-26)

- **Failure modes the LLM layer must be tested against** (advisor's top three):
  1. *Strategic / selfish agents* — some agents misreport state or refuse to share. Does language-based negotiation surface and route around defectors better than a fixed protocol?
  2. *Noisy / faulty observations* — imperfect knowledge of own SoC or load forecast. Memory + reflection should help in ways a rule-based protocol can't.
  3. *Communication constraints* — limited message budget, partial link failures. Agents must reason about which trust circle to route through (ties back to the ownership/management overlays).
- **Park et al., "Generative Agents" (arXiv:2304.03442):** advisor-recommended. Decision: do NOT fork the codebase (their world is a 2D social town, fundamentally different from a physics-based microgrid). Treat as a Phase 2 reference for agent architecture patterns (memory stream, reflection, natural-language messaging) which we reimplement adapted to microgrid coordination and cite prominently.

## Advisor sign-off 2026-07-15 (Phase 3)

Yongfeng's email answering the five Phase 3.2 preflight items. Approves the Phase 3 direction and the shipped choices; four adjustments recorded in the docs on 2026-07-15:

- **Defectors:** prompt-driven selfishness confirmed correct; wrapper corruption stays a separate failure mode (matches P3.1 T5/T20 as shipped).
- **Fairness:** metric selection approved; NEW — efficiency-vs-equity tradeoff gets its own 3.3 figure + discussion ("may ultimately be more interesting than the absolute performance numbers").
- **Explanation judging:** LLM-judge approved; NEW — transparent rubric + inter-prompt/repeated-evaluation consistency reporting (playbook Stage 4 sub-step); explanation quality stays secondary.
- **Scenario design > model comparison:** find operating conditions where richer reasoning measurably helps; Sonnet ablation (playbook Stage 3) now explicitly optional/last; $0 mock pass on a "reasoning-advantage" cell added to the playbook preflight.
- **Venues:** CHI / ICLR / WWW main-or-workshop (see Timeline). **Caveat:** the reply did not acknowledge the repo-public flag (item 4) — Leo should confirm it was communicated; non-blocking.

## User context

- High school student, comfortable with Python, new to power systems / energy / multi-agent systems.
- Working on this as a research project under mentor/advisor guidance. Advisor framing is in the kickoff message in session history. No fixed deadline — summer + potentially next school year.
- Capable and engaged — don't be condescending, but do explain power-systems terminology when it first comes up (DoD, RT efficiency, distribution bus, etc.). The student is here to learn the material: when subagents produce code, summarize what it does and why it's correct (updated 2026-07-11 — subagent execution now allowed, see Workflow preferences).

## Workflow preferences (durable)

These are the user's explicit preferences for how Claude should operate on this project. They override default Claude Code behavior where they conflict.

- **Simplicity above all.** Every task and code change as simple as possible. Every change should impact as little code as possible. No massive or complex changes — split them. No speculative abstraction.
- **Serial focused work by default; worktree subagents for independent tracks.** Tasks execute in the active session, batched ~3-5 at a time with check-ins between. For genuinely independent tracks, Claude spawns worktree-isolated subagents itself rather than asking Leo to open parallel sessions. (Changed 2026-07-11 from inline-only, to match campus-talent.) Subagent-produced changes still get explained — see User context.
- **No Claude attribution in commits, anywhere.** No Co-Authored-By trailer, no "Generated with Claude Code" lines, no tool mentions — Claude must not appear in commit messages in any form. All commits attributed solely to the user. (Also enforced harness-wide via `attribution: {commit: "", pr: ""}` in `myproject/.claude/settings.json`.)
- **Update `CLAUDE.md` progress log after every completed task**, in the *same* commit as the task's code changes — never a separate "docs" commit.
- **Keep README + `CLAUDE.md` status in sync at every phase boundary.** On finishing any phase or hardening sub-phase (1.6, 2.9, 3.1, …), that phase's wrap-up commit MUST also update `README.md` (status line + the phase's section/numbers) and flip the `CLAUDE.md` phase-table row to *complete* + refresh its status block (tag, final test count) — on top of the per-task progress-log row above. Never tag `phaseX-complete` until both docs reflect the phase. This is a followed rule, not a harness hook (composing the doc content needs judgment a shell command can't do). (Added 2026-07-12 at Leo's request; already practiced via each plan's wrap-up tasks — now an explicit standing rule.)
- **After completing each task, preview the next one** — end the response with a 1-2 sentence summary of what's coming next.
- **Continuous execution: do NOT pause between tasks to ask "want me to continue?"** — just keep going. Stop only on real blockers: ambiguous spec, failing test you can't fix, decision the user hasn't authorized, or anything affecting the public surface area of the repo. The user explicitly opted into "fire-and-forget" execution on 2026-05-14.
- **Verify CI-equivalently before committing.** Local `pytest` against the long-lived venv is necessary but not sufficient — the local venv masks packaging bugs that fail on `pip install -e .` from scratch. On 2026-05-14 four commits (Tasks 11-14) shipped with red CI because `configs/` broke setuptools auto-discovery and my local venv had been installed before `configs/` existed. **The rule:** for any change touching `pyproject.toml`, `.github/workflows/`, `.pre-commit-config.yaml`, or any new top-level directory / package root, run a clean-install dry-run in `/tmp/microgrid_ci_check` (recipe in `~/.claude/projects/.../memory/feedback_microgrid_verify_before_commit.md`). After every push, glance at `gh run list --limit 1`. Don't let a red streak develop — fix before shipping the next task.
- **Mark plan checkboxes as `- [x]` after each task**, in the same commit. The plan file is the source of truth for what's done; `/nextask` reads the first `- [ ]`.
- **Repo visibility: PUBLIC — settled.** github.com/leochang017/microgrid-llm-coordination has been public from day 1 and stays public (Leo's decision, 2026-07-12; supersedes the advisor's earlier private-until-draft note under Important warnings — mention the decision in the next advisor update). MIT licensed.
- **Security checks (public repo).** Never commit secrets — credentials live only in env vars or the macOS Keychain, never in a file, YAML, or commit. Before every push, scan the staged diff for **real key material**:
  ```bash
  git diff --cached | grep -cE 'sk-ant-(api|oat)[0-9a-zA-Z_-]{15,}'   # must be 0
  ```
  **Match key-shaped strings, not the bare prefix.** The old rule grepped `sk-ant|bearer `, which is unsatisfiable: this repo legitimately documents the `sk-ant-oat`/`sk-ant-api` prefixes (playbook credential step, `sim/agents/llm.py` routing comments + `startswith` check, `sk-ant-oat01-FAKE` test fixture), so it fired on every diff touching those lines and trained the reader to wave it through. A check that always cries wolf is worse than no check. (Corrected 2026-07-15 after it false-positived on the P3.2 preflight commit; verified 0 real key-shaped strings across all 7,544 tracked files, including a live cell's 7,537-file `llm_cache`.)

  Run `/security-review` on the pending diff before tagging any phase. New code touching subprocess calls, YAML loading, or file paths gets a quick injection/path-traversal look. Live-run artifacts under `reference_runs/` are published to the public repo — verify they contain no credentials before committing (the cache stores prompt/response/model/token-counts only; auth never enters it, since the cache key derives from request content). **Never paste a key into the session transcript** — it persists as plaintext JSONL on disk; store it in the Keychain (`security add-generic-password -a "$USER" -s anthropic-api-key -w`) and export via `$(security find-generic-password -a "$USER" -s anthropic-api-key -w)`. (Added 2026-07-12; expanded 2026-07-15.)
- **Commit and push without asking.** Leo relaxed both gates (commit 2026-07-11, push 2026-07-12). Pre-commit hooks still gate content; CI still gates main; microgrid pushes deploy nothing.
- **Pre-commit hooks gate every commit.** If a hook reformats files, re-stage and retry — never bypass with `--no-verify`.
- **Explain power-systems jargon when it first appears**, but don't be condescending — the student is capable, just new to the domain.

## Project skills (invoke with `/<name>`)

Project-specific Claude Code skills authored for this project, all at `~/.claude/skills/<name>/SKILL.md`. Available in every session:

- **`/readclaude`** — Load this CLAUDE.md into working context at the start of a session.
- **`/nextask`** — Find the next unchecked step in the plan and report what it will do. Waits for go-ahead.
- **`/simtest`** — Run `pytest + ruff + mypy` and report a one-line summary (or first failure).
- **`/explainphysics`** — Plain-language walkthrough of a `sim/` file with hand-traced numeric examples.
- **`/advisormeeting`** — Draft a 2-paragraph status update for an advisor meeting using the progress log + git history.
- **`/sweep`** — (Phase 3) Run scenarios × strategies × seeds and tabulate summary metrics.

## Claude Code efficiency setup (added 2026-07-11, mirrors apps/campus-talent/CLAUDE.md)

**Preconfigured — nothing for Leo to manage:** permissions run at `bypassPermissions` with a safe-command allowlist (pytest/ruff/mypy/uv/manage.py/read-only git) in `myproject/.claude/settings.json`; a PostToolUse hook auto-runs `ruff check --fix` on every edited `.py` under this repo (and under campus-talent); the VS Code Claude Code extension is installed — terminal drives, VS Code reviews diffs and per-message rewind.

**Claude self-serves, unprompted:** plan mode before any multi-file feature (the existing brainstorm → spec → plan flow already enforces this); cheaper models assigned to mechanical subagent chores; for genuinely independent tracks, worktree-isolated subagents spawned by Claude itself — same rule as campus-talent (changed from inline-only on 2026-07-11).

**Leo's cheat sheet (user-side, optional):** Shift+Tab cycles permission modes · `/effort` (session-only; default for routine work, max for hard problems) · `/rewind` checkpoints · `/resume` past sessions · `/clear` between unrelated tasks · `@path/file` references a file · `! cmd` runs shell inline · `/usage` shows token spend.

**Working two projects at once:** one terminal + `claude` session per project (this repo and campus-talent). Separate repos mean zero file conflicts and no worktrees needed; each session gets its own browser instance. Both sessions share the same subscription rate limits — two heavy sessions drain quota faster. Keep each session in its own project lane, and don't let both edit shared files (myproject settings, memory) at the same moment.

## Conventions (project-wide)

- Python 3.12, `python3 -m venv .venv` + pip (no `uv` — see the 2026-05-14 Task 0 archive row), `pytest`, `ruff`, `mypy --strict` on `sim/`.
- **TDD always.** Every task: write failing test → run it red → minimal impl → run green → commit. Do not skip the red step.
- **Conventional commits** (`feat:`, `test:`, `chore:`, `docs:`).
- **No global state** in `sim/`. Engine owns the RNG. Pass it explicitly.
- **Pure functions** in `household.py` and `network.py`. The engine is the only stateful glue.
- **Determinism is mandatory.** Two runs of the same scenario YAML must produce byte-identical state logs.
- **Strict-mode invariants** (SoC bounds, non-negative wasted/unmet) asserted every tick. Disabled only via `--no-strict` for hacking.

## Critical files

| Path | Purpose |
|------|---------|
| `docs/superpowers/plans/2026-07-12-phase3.1-prelive-hardening.md` | **ACTIVE plan — Phase 3.1 (start here)** |
| `docs/superpowers/specs/2026-07-12-phase3.1-prelive-hardening.md` | Phase 3.1 spec (locked decisions) |
| `docs/superpowers/plans/2026-07-12-phase3.2-live-runs.md` | Phase 3.2 live-run playbook (budget-gated) |
| `docs/superpowers/specs/2026-07-12-remaining-phases-roadmap.md` | Roadmap: Phase 3.3 analysis → Phase 4 paper+demo |
| `docs/superpowers/` (specs/ + plans/) | Earlier phase specs & plans (1, 1.6, 2, 2.9, 3) |
| `docs/progress_log_archive.md` | Progress-log rows ≤ 2026-06-17 (Phases 1 → 2.8) |
| `CLAUDE.md` | This file |
| `sim/` | Simulator package (Phase 1 build target) |
| `tests/` | Test suite |
| `configs/scenarios/` | Scenario YAML files |
| `runs/` | Output directory (gitignored) |
| `data/` | Cached raw data (gitignored, see Task 24) |

## Data access

- **NREL NSRDB:** free API key at https://developer.nrel.gov/signup/. Env vars: `NREL_API_KEY`, `NREL_EMAIL`.
- **Pecan Street Dataport:** DROPPED 2026-05-26 (no HS-student affiliation path; ResStock is primary and public). The adapter + CSV fixtures remain for reference only.

## Important warnings (from advisor)

- Do NOT claim the system is ready for real deployment. It isn't, nobody's is.
- Be careful with the equity framing. Read Sovacool's energy-justice work before writing the fairness section.
- The LLM-agents-lie failure mode is interesting; study it but do NOT oversell as an adversarial security contribution.
- ~~Open-source release planned alongside the paper. GitHub repo is **private until paper draft is solid**.~~ _Superseded 2026-07-12: Leo decided the repo stays public (it has been public from day 1). Tell the advisor in the next update._

## What to do at the start of a new session

1. Read this file (`/readclaude` does this for you).
2. Read the current phase's spec.
3. Read the current phase's plan.
4. `git status` + `git log -10` to see where execution left off.
5. Find the first unchecked `- [ ]` step in the plan and proceed.
