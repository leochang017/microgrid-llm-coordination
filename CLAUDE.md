# Microgrid Project — Authoritative Context

> **Resume protocol for new sessions:** read this file first. The spec is the authoritative design; the plan is the authoritative TDD task list.

## Research question

Can a population of LLM agents, one per household, negotiate peer-to-peer to allocate scarce energy during a grid outage in a way that is **(a)** fair across heterogeneous households, **(b)** robust to incomplete or incorrect information, and **(c)** explainable to residents?

The contribution is on the **CS/ML axis** (natural-language agent coordination, robustness, explainability), not the power-systems axis. Classical optimization handles (a) under strong assumptions, struggles with (b), and does not attempt (c). That gap is the paper.

## Timeline + venue

- Started: 2026-05-14. Runway: summer + potentially the following school year (no fixed deadline).
- Target venues (per advisor 2026-07-15, supersedes the 2026-05-26 list): **CHI, ICLR, or WWW — main conference or a workshop under it** (workshop deadlines ~3–4 months after main); track via mlciv.com AI-conference-deadlines. Decision after Phase 3 results, with the advisor.

## Four-phase plan

| Phase | Weeks | Status | What it builds |
|-------|-------|--------|----------------|
| 1 — Simulator | 1-4 | **complete** | Deterministic discrete-time microgrid sim, no agents |
| 1.6 — Pre-Phase-2 hardening | — | **complete** | Ownership/management comm-graph overlays, stress scenarios that break round-robin, centralized LP upper-bound baseline, gap-closed reporting (cold/hot ResStock regions pending data fetch) |
| 2 — LLM agent layer | 5-8 | **complete** | Per-household LLM agent, natural-language P2P messaging |
| 2.9 — Correctness hardening | — | **complete** | Review-driven fixes: energy conservation, solar timezone, LP slack/bounds, zero-LLM control, seed sweeps, provenance (18 tasks, 2026-07-07) |
| 3 — Benchmark & experiments | 9-14 | **complete** (benchmark + live runs + analysis) | Scenario suite + baselines + metrics (Gini, served-critical-load, explanation quality) |
| 3.1 — Pre-live hardening | — | **complete** (tag `phase3.1-complete` 2026-07-12) | 55-finding review fixes; **gates ALL live spend** |
| 3.2 — Live runs | — | **complete** (tag `phase3.2-live-complete` 2026-07-16); **Stage 4 judging done 2026-07-17; Stage 3 Sonnet ablation done 2026-07-18** | Live Haiku clean cell (3 seeds) + 3 failure cells; **Stage 4 explanation judging COMPLETE** (Sonnet judge); **Stage 3 Sonnet capability ablation COMPLETE** — Sonnet ties Haiku in the clean cell (0.6685 vs 0.6726), model capability does not move coordination quality. All playbook stages now run. |
| 3.3 — Analysis & results | — | **complete** (tag `phase3.3-complete` 2026-07-16) | Deterministic `scripts/figures.py --all` → headline / fairness / efficiency-equity / failure-axis figures + negotiation table + `docs/phase3_results.md` outline; explanation-quality table populated (Stage 4 done 2026-07-17); VT/AZ cross-climate cells deferred |
| 4 — Web demo & paper writeup | 15-20 | **planned** (spec + plans 2026-07-20) | Spec `docs/superpowers/specs/2026-07-20-phase4-paper-demo-design.md`; plans 4a (paper draft, Markdown-first) + 4b (SvelteKit static demo on Vercel) in `docs/superpowers/plans/` |

> **Phase 1.6 is a hard gate on Phase 2**, inserted by advisor (Yongfeng) on 2026-05-26. Rationale: the current 12h-July-overnight scenario has a 98.5% no-coordination ceiling, leaving only ~0.5% headroom for the LLM layer — Phase 2 would be a null result by construction. Must establish a regime where round-robin visibly fails before building LLM agents.

Completed-phase specs + plans live in `docs/superpowers/archive/`; Phase 4 planning starts from `docs/superpowers/specs/2026-07-12-remaining-phases-roadmap.md`.

## Status

**All phases through 3.3 are complete** — tags `phase1-complete`, `phase1.6-complete`, `phase2-complete` … `phase2.9-complete`, `phase3-infra-complete`, `phase3.1-complete`, `phase3.2-live-complete`, `phase3.3-complete` (see the phase table above). **Phase 4 (paper + web demo) is planned, not started** — spec + both executor plans written 2026-07-20; execution kicks off via `docs/superpowers/plans/2026-07-20-phase4-kickoff-prompts.md`.

**Headline result (Phase 3, solar showcase `haves_havenots_solar`):** live Haiku coordination beats the mandatory zero-LLM control on the clean cell across 3 seeds — **+5.8 ± 1.0 pts served, closing 29.0% ± 2.9 of the control→LP gap** (vs round_robin +2.0 ± 1.1, but seed-dependent — the claimable comparison is the *control*, not round_robin). Fairness improves *with* served-load (no efficiency–equity tradeoff; the Rawlsian min-house floor is degenerate, so fairness rests on Gini + Jain). This is the first replicated live evidence the LLM layer adds value — Phase 2.8 live Haiku *lost* to round_robin (0.513 vs 0.525) and the Phase 2.9 zero-LLM control *tied* it; the Phase 3 information-flow rework (INFORM-only beliefs, noised `act()`, binding negotiation) flipped the answer.

**Failure cells** (each n=1, vs its own same-seed control): **defectors +2.3** (retains 89.1% of coordination value under 33.6% realized defection), **noise +0.87** (tolerates dual-channel belief noise; margin narrows from clean's +4.6 but ordering holds), **comm −0.86** (after the C1/C2 commitment-integrity fixes; was −4.6 pre-fix — most of that loss was measurement bugs, and the residual is the hardcoded send-order bandwidth channel, not LLM reasoning). `all`@7 deferred (its comm component is confounded).

**Sonnet capability ablation (Stage 3):** clean@23 served 0.6685 vs Haiku 0.6726 — a statistical tie; a stronger model does not move clean-cell coordination quality (corroborates the P2.8 architecture ceiling and the advisor's scenario-design-over-model-comparison priority). **Explanation judging (Stage 4, Sonnet judge ≠ Haiku authors):** clean 3.03 / 4.05 / 4.46, defectors 3.00 / 3.97 / 4.52 (state_accuracy / actionability / consistency) — rationales are actionable + consistent, `state_accuracy` weakest (self-reported numbers drift from logged state); selfish prompting doesn't degrade quality. **Total live spend ≈ $38.47** of the $100 budget.

**Next:** execute Phase 4a (paper draft, `docs/superpowers/plans/2026-07-20-phase4a-paper-draft.md`) then 4b (web demo, `...phase4b-web-demo.md`) — one fresh Opus session each, kickoff blocks in `docs/superpowers/plans/2026-07-20-phase4-kickoff-prompts.md`. Venue decision (CHI / ICLR / WWW main-or-workshop) with the advisor after the 4a draft (email drafted at 4a wrap); `paper-v1` tag deferred to submission. Also flag the repo-public decision to the advisor.

Detail pointers: **full results story** → `docs/phase3_results.md`; **figures + tables** → `docs/figures/` + `docs/phase3_tables.md` (regenerate with `python -m scripts.figures --all`); **completed-phase specs/plans** → `docs/superpowers/archive/`; **row-level history** → `docs/progress_log_archive.md` (≤ 2026-07-12) + the Progress log below (≥ 2026-07-15).

### Progress log

Update this after every committed task. Newest entries on top.

| Date | Task | Commit | Tests | Note |
|------|------|--------|-------|------|
| 2026-07-20 | **Phase 4b Task 1 — `scripts/export_demo_data.py`: compact per-cell JSON for the web demo (TDD, $0)** | _(this commit)_ | 416 ✓ (+9) | First execution task of Plan 4b (`docs/superpowers/plans/2026-07-20-phase4b-web-demo.md`). New `scripts/export_demo_data.py` turns each of the four committed live runs (`DEMO_CELLS` = clean@23 / defectors@7 / noise@23 / comm@23) into `{meta,ticks,messages,explanations}.json` the SvelteKit app will fetch. **Data layer only — no `web/` dir, no CLI run against the repo, no `sim/` change.** Shape: `build_houses` re-derives the household population deterministically from the seed via `sim.engine.sample_households` + `sim.agents.failure_modes.assign_defectors` (not read from any artifact) and **cross-checks it against `state.jsonl` as a drift tripwire** — raises if any logged `soc_kwh` exceeds the re-derived battery capacity, or if a re-derived `pv_kw_peak > 0` disagrees with whether that house ever generated; `build_ticks` emits columnar `[tick][house]` `socFrac`/`solarKw`/`loadKw`/`unmetKwh` (3dp), cumulative `servedFracCum` (4dp), per-tick `transfers` (from `transfer_executed`), non-transfer/non-outage `events` (`sender_dod_floor`/`receiver_full`), and the per-tick INFORM `{sent,delivered,dropped}` aggregate; `keep_message` downsamples on the planned rule (keep iff `perf != INFORM or not templated`) so the negotiation traffic ships in full while the templated INFORM broadcast survives *losslessly as that aggregate* — which is precisely the comm-cell story. `build_meta` copies `live` and `baselines` **unrounded** from the committed `summary.json` + `figures.collect_cell` (which is also what regenerates the $0 baselines and runs the C6-3 frozen-config drift guard — not reimplemented), so the demo can never disagree with `figures.EXPECTED_LIVE`. `export_cell` asserts the served-load identity (state.jsonl physics vs `summary.json`) to `< 1e-9` **before** any rounding, and `_write` refuses any file ≥ 15 MB and appends exactly one trailing newline (pre-commit `end-of-file-fixer` parity). **`llm_cache/` and `judge_cache/` are never opened** — the export reads only the three JSONL files, `summary.json`, and the optional `explanations_eval.json`; no key material or cache path can reach the output (security grep on the staged diff → 0). **TDD red step recorded:** `tests/test_export_demo_data.py` was written first and run before the script existed → `ModuleNotFoundError: No module named 'scripts.export_demo_data'`, collection interrupted; green after implementation, 9 ✓. **Every pinned constant in the plan brief PASSED as written — none needed re-derivation:** kept-message count 12258 (`clean__seed23`), comm drop identity `informCounts.dropped (10191) + built dropped (2501) == 12692`, 96 ticks `2018-01-01T00:00:00 → T23:45:00`, first transfer at tick 2, judge means 3.03/4.05/4.46 over n=100, clean-seed spread `{1: 0.8248454685184583, 7: 0.7963806526129852}`, defector draw 6 houses at seed 7, and the served-load identity `servedFracCum[-1]` reproducing `0.6725764138021589` **exactly** (verified unrounded before pinning the 5e-5 test tolerance, which exists only for the 4-dp storage rounding). One design note: `state.jsonl` logs no battery capacity, so `build_ticks` takes an optional `capacities=` override — `export_cell` passes the re-derived exact capacities; the argument-free fallback (INFORM `soc_capacity`, floored by max observed SoC) keeps the function testable on raw artifacts alone. Measured payload (CLI dry-run into a scratch dir, immediately deleted): clean 3.77 MB / defectors 3.16 MB / noise 3.54 MB / comm 1.54 MB ≈ **11.7 MB total**, under the plan's ~12.3 MB estimate and every file well under the 15 MB ceiling. **Verify (repo 3.12.12 `.venv`):** `pytest tests/test_export_demo_data.py -q` 9 ✓, `ruff check sim tests scripts` clean, `mypy` clean (38 source files, +1), full `pytest` **416 ✓** (407→416), `python -m scripts.figures --check` green — all committed live numbers byte-identical to their golden pin, no pin moved. |
| 2026-07-20 | **Phase 4 planning batch — spec + 2 executor plans (4a paper draft, 4b web demo) + kickoff prompts ($0, docs only)** | _(this commit)_ | 407 ✓ (unchanged) | Per the roadmap's standing rule (spec + TDD plan before execution), this session produced the full Phase 4 document batch under `docs/superpowers/`: **spec** `specs/2026-07-20-phase4-paper-demo-design.md`, **Plan 4a** `plans/2026-07-20-phase4a-paper-draft.md` (12 tasks: `docs/paper/paper.md` draft v1, Markdown-first per Leo's call — venue-agnostic until the advisor's CHI/ICLR/WWW decision; Sovacool reading task gated BEFORE fairness prose with tenet provenance correctly split McCauley/Jenkins vs Sovacool & Dworkin; related-work pass with a hard no-unverified-citation rule; number rule = COPY from `phase3_results.md`/`phase3_tables.md`/committed summary.json only; advisor constraints as verify greps — `byzantine|adversar|attack|threat` → 0, Gini never alone, unhedged no-deployment bullet; numeral-by-numeral audit task + `claims_audit.md` traceability; wrap drafts the venue email via /advisormeeting, never sends; NO tag — `paper-v1` at submission), **Plan 4b** `plans/2026-07-20-phase4b-web-demo.md` (11 tasks: `scripts/export_demo_data.py` TDD → committed ~12.3 MB four-cell JSON [clean@23/defectors@7/noise@23/comm@23; messages downsampled keep-iff `perf!=INFORM or templated:false` with per-tick INFORM aggregates preserved — that aggregate IS the comm story]; `tests/test_demo_data_pins.py` asserts exported numbers == `EXPECTED_LIVE` exact-float so demo can never drift from the paper; greenfield `web/` SvelteKit adapter-static + TS + plain CSS, overview + `/run/[cell]` replay [viridis SoC grid, scrubber, transfer arrows, circle overlay, message panel with drop reasons, per-house why-panel with tick-level judge joins + honesty note — per-message pairing ambiguous 83/99 in committed evals]; separate CI `web` job; Vercel via Git integration rooted at `web/`, deploy step Leo-gated, zero tokens), and **kickoff prompts** `plans/2026-07-20-phase4-kickoff-prompts.md` (2 blocks, subagent-driven-development, 4a→4b). **Leo's 3 decisions this session:** SvelteKit (not Next.js); Markdown-first paper (LaTeX post-venue); VT/AZ cells dropped from Phase 4. Grounding: 3 read-only explorers (paper raw material / demo data substrate / house conventions) + 2 Plan agents; reuse surface verified against code (`sample_households` `sim/engine.py:42`, `assign_defectors` `failure_modes.py:99`, `figures.py` `LIVE_CELLS`/`EXPECTED_LIVE`/`collect_cell`/`regen_baselines`/`cell_served_gap_closed`; seed-spread pins 0.8248…/0.7963… match `EXPECTED_LIVE` byte-exact). Pinned-literal rule in 4b: any plan test constant that reds at execution is re-derived by measurement, never force-fitted. Placeholder grep over all 4 docs → 0 (the one `TBD` hit is itself a verify-grep command). No `sim/`/`scripts/`/`tests/` change; suite untouched. |
| 2026-07-20 | **OSS-polish batch executed (13/13) — README staleness, pyproject OSS metadata, I-1 emission-vs-settlement caveat + 3 minor honesty notes** ✅ | _(this commit)_ | 407 ✓ (unchanged) | Executes `docs/plans/2026-07-20-oss-polish-plan.md` (the 2026-07-20 review's polish plan). **Docs + packaging metadata + comment/docstring caveats only — zero logic, zero prompt-string/cache-key, zero event-kind renames**, per fix-batch-E precedent (no TDD red step for prose; the frozen-pin verify still gates the commit). **README:** test count `364 as of 2026-07-19` → `407 as of 2026-07-20`; added the CI badge (`.github/workflows/ci.yml`, name `CI`) at the top and repointed the previously-phantom "see CI badge" phrase at it; scripts listing now headlines `figures.py` (the reproduce-everything entry point) and adds `dose_check.py`; new "Why the checkout is large" paragraph under Install. **Corrected the plan's own estimate while writing it** — the plan guessed "~35k files / ~13 MB"; measured, `reference_runs/` is **35,522 tracked files / ~266 MB apparent** (~35.5k prompt-cache JSONs ≈ 102 MB, balance = frozen run artifacts, mostly `messages.jsonl`), so the README ships the measured numbers. **pyproject:** `readme`/`license = {text = "MIT"}`/`authors`/3 `classifiers` + a `[project.urls] Repository` block. **I-1 (the review's one new substantive finding):** commitment fulfillment is bookkept at EMISSION, not settlement — `sim/agents/agent.py:830` does `c.kwh_remaining -= kw * dt_hours` on the *requested* kW, while `sim/network.py::settle_transfers` independently scales the transfer down against sender/receiver/bus caps and nothing feeds the result back (`MemoryKind`'s `transfer_outcome` slot is declared and never written — grep-confirmed, 1 hit repo-wide). Re-counted the evidence myself from `clean__seed23/events.jsonl`: **906 `sender_dod_floor` clips against 1,632 `transfer_executed`** — exact. **No published number is affected** (served/Gini/Jain all come from settled physics); the caveat lands in `scripts/figures.py::render_tables` prose (→ regenerated `docs/phase3_tables.md`, **+2 lines, the only generated-artifact diff**; `docs/figures/` untouched) and as a `docs/phase3_results.md` §6 bullet that explicitly re-qualifies §4's noise-cell "committed less, fulfilled more reliably" aside as a statement about *promising*, not delivered energy. Closing the loop is a Phase-4 candidate — it changes prompts/cache keys and would re-pay every committed cell. **Three minor notes:** M-4 `SENDER_DOD_FLOOR` is a misnomer that fires on ANY sender-cap clip — re-derived from `sim/engine.py::_transfer_caps` before asserting it (`sender_cap = (solar − load) + rate/DoD-bounded battery + grid`, so own-load deficit and rate limit both trip it) — with an explicit DO-NOT-RENAME warning (committed `events.jsonl` + replay pins depend on the literal string); M-1 `NRELSolar.get_kw` returns 0.0 before the first sample while `ResStockLoad.get_kw` raises, so a mis-set horizon silently runs dark (intentional asymmetry, now flagged for scenario authors); M-3 the LP row's Gini is whichever alternate optimum the solver picked (served-load objective, no fairness tiebreak) — surfaced on `render_fairness_panel` so figure readers see it. **Verify (twice, repo 3.12.12 `.venv`):** ruff clean, mypy clean (37 files), pytest 407 ✓, `figures --check` green (all 6 committed cells byte-identical to golden pins), `--tables` regeneration idempotent. **pyproject change → mandatory clean-install dry-run, run on two fresh venvs:** 3.14 (metadata pickup verified — `pip show` resolves Author/License) and a **CI-parity 3.12** venv (407 ✓ / ruff / mypy 37 files / `figures --check` green from a from-scratch `pip install -e ".[dev,data]"`). Security grep over staged diff → 0. **ONE UNPLANNED CHANGE, Leo-authorized mid-task: `.pre-commit-config.yaml` mirrors-mypy `rev: v1.11.2` → `v2.3.0`.** The commit was blocked by the pre-commit mypy hook erroring on `sim/strategies/lp_optimal.py:113` (`Unsupported target for indexed assignment` on `np.zeros(n)`) — a file this batch never touches. Diagnosed rather than bypassed: the hook pins **mypy 1.11.2** while the repo/CI resolve **mypy 2.3.0** (`mypy>=1.11`), and 1.11.2 false-positives against numpy 2.5.1's stubs. Proved it by running both cached pre-commit envs directly — the 3.12/numpy-2.5.1 env FAILS, the 3.14/numpy-2.4.4 env passes, and repo mypy 2.3.0 passes — so the variable is the **mypy version, not numpy** (my first hypothesis, that it was a numpy-stub mismatch, was wrong and is recorded here as such). This is escalated finding **C8-2** in a new form: last session flagged the hook as a weaker local gate but couldn't validate a fix because `pre-commit` wasn't installed; the rebuilt 3.12 `.venv` ships it, so the fix is validated here (`pre-commit run mypy --all-files` → Passed). A comment above the rev now says to keep it in step with CI's mypy. **C8-2's other half — the hook inheriting mirrors-mypy's default `--ignore-missing-imports` — is still open and still escalated, deliberately not touched here.** No `--no-verify`, no `SKIP=`. |
| 2026-07-20 | **Independent OSS-readiness review (3 reviewers) — verdict: near-optimal; security CLEAN; no published number wrong; polish plan written** | _(this commit)_ | 407 ✓ (unchanged) | Leo asked whether the repo is the best it can be for open source (architecture/accuracy/security/organization/conciseness), report-only. Method: fresh baseline (407 ✓ / ruff / mypy 37 files / CI green / `figures --check` green, all on the rebuilt **3.12.12** `.venv` — ENV-1 resolved) → 3 independent read-only reviewers briefed on the 07-18 known-limitations record → every surviving claim re-verified by the controlling session. **Security: clean, safe public** — 0 key-shaped strings in tree AND full history, cache artifacts hold no auth material, all `safe_load`, no injection surfaces, CI exposes nothing to forks. **Accuracy: no published number wrong**; one NEW finding **I-1** — commitment fulfillment bookkept at emission (`agent.py:828` decrements by requested kWh), settlement scales down separately (906 sender-cap clips vs 1,632 transfers in clean@23) and never feeds back (`transfer_outcome` declared, never written) → promise-side counters ≠ delivered kWh; physics/served/Gini unaffected. **Organization: professional**; 4 shallow staleness items (README test count 364→407, phantom CI-badge phrase, scripts listing missing `figures.py`/`dose_check.py`, thin pyproject metadata). All findings + fix tasks → **`docs/plans/2026-07-20-oss-polish-plan.md`** (docs+metadata only, zero logic/cache-key changes; I-1 loop-closing deferred to Phase 4 — it would change prompts/cache keys). No code touched this session. |
| 2026-07-19 | **Full-repo adversarial review (Plan C, batch #4 of 4) — WRAP: 29 findings + ENV-1, all 22 C/H/M skeptic-confirmed, 20 fixed, no published number moved** ✅ | _(this commit)_ | 407 ✓ (+43) | Closes the 4-plan 2026-07-18 maintenance batch. **Method:** 8 read-only reviewer subagents (physics / LP+metrics / agent / messaging / LLM / scripts / tests / config+docs) → 8 independent skeptic verifiers (default-REFUTE) on every CRITICAL/HIGH/MED → triage → TDD fixes gated on a 3.12 verify + frozen pins. Record: `docs/superpowers/archive/2026-07-18-full-repo-review.md`. **Raw:** 3 CRITICAL, 7 HIGH, 12 MED, 7 LOW = 29, + **ENV-1**. All 22 C/H/M **CONFIRMED, 0 refuted** (prior reviews refuted ~⅓; this batch's reviewers pre-analyzed reachability so yield held); severity corrections C4-1 HIGH→MED (inert on comm@23), C5-2 upgraded (418 real empty cache entries in the Sonnet cell), C3-3 mechanism (crash not corruption). Mutation check: all 5 core-formula tests (conservation/gini/transfer-cap/commitment-serve/gap_closed) bite. **No committed `reference_runs/` number is wrong** — every published figure/table value re-derived exactly; the one wrong *published claim* was a README aside (C8-1), docs-only. **20 findings fixed across 5 TDD batches (364→407 tests, +43):** A scripts (`6f20d471`, +19 — figures --check now asserts vs a golden pin instead of only printing; `apply_overrides` rejects typo'd dict-leaf keys; baseline/config-drift + seed-dir + rubric-pairing + paid-artifact guards), B LLM (`880d9b01`, +12 — fable/mythos cost rows, refuse-to-cache empty `stop_reason=max_tokens` responses [gates put() only → committed replay byte-identical], cache-key field-isolation test), C sim-validation (`5522d0fa`, +10 — critical_load_frac/defector_fraction bounds, NaN/Inf policy weight, manual-defector membership, round_robin zero-guard), D agent-behavior pin-gated (`5fcc07da`, +2 — commitment retraction by identity not value [C4-1], OFFER recipient dedup consistent with REQUEST fan-out [C3-1]; **comm@23 pin + replay byte-identical, independently verified**), E docs (`667f5102` — README cross-population fix + defector-provenance/cache/LP docstring caveats). **KNOWN LIMITATIONS (documented, not fixed):** C2-1 (LP self-pair over-schedule — visualization-only, `optimal_metrics` unaffected), C2-2 (LP per-group bus cap — dormant full-island), C3-2 (grid-connected orphan commitments — partial-island only), C1-2/C2-4/C5-5/C6-7/C6-8 (latent/mitigated). **ESCALATE to Leo:** (1) **ENV-1** — the repo `.venv` is Python 3.14/numpy 2.4.4 and non-deterministically trips a strict-mode conservation assert (preflight got a lucky hash seed); under a fresh **Python 3.12** venv (numpy 2.5.1 — the pinned/CI version) the suite is green + deterministic (6/6 random seeds + HASHSEED=0). ALL published results generated under 3.12 → unaffected. Rebuild local `.venv` on 3.12; decide `requires-python` (`<3.14`?) or investigate the numpy float-ordering sensitivity. (2) **C8-2** pre-commit mypy hook inherits `--ignore-missing-imports` (local gate weaker than CI) — NOT fixed: removing it exposes a numpy-stub-version false positive that would break every local commit unless numpy is pinned to CI's, and `pre-commit` isn't installed here to validate; CI's bare `mypy` already catches the bug class. (3) README framing (C8-1, now the self-consistent `__llm` seed-23 population / 14.2-pt control→LP) + Phase-4 latent bugs (C2-1/C2-2/C3-2, C3-1 dedup semantic defaulted to max). **Verify (twice, 3.12 venv):** ruff clean, mypy clean (37 files), pytest 407 ✓, `figures --check` green (all committed cells byte-identical to golden pins), `phase3_tables.md` byte-identical. `docs/plans/README.md` batch-#4 → done. |
| 2026-07-19 | **Fix batch E — README cross-population claim + 5 docstring/comment honesty fixes (C8-1, C8-3, C2-3, C5-3, C5-4, C2-1)** | _(this commit)_ | 407 ✓ (unchanged) | Docs/comment only, no logic — no TDD, no red step; verified the suite + golden pins stay green. **C8-1 (CRITICAL):** README's "solar showcase" table mixed two different household populations — subtracted the base `haves_havenots_solar.yaml` scenario's baselines (no_coordination 0.4280, round_robin 0.7666, lp_optimal 0.9711) against the live `__llm` cell's control (0.6269) and claimed "34 points of headroom," a number that doesn't mean anything across populations. Replaced the whole block with the one jointly-comparable `__llm` seed-23 population (no_coordination 0.3336 / control 0.6269 / round_robin 0.6444 / lp_optimal 0.7684 — matches `docs/phase3_results.md`'s seed-23 row exactly): 12.4-point rr→LP gap, 14.2-point control→LP headroom, of which live Haiku closes ~⅓ (32.3%). Grepped the full file after: zero remaining references to the old numbers or "34 points" framing anywhere else in README.md. **C8-3:** `docs/phase3_results.md` §6 gains a bullet noting the committed `defectors@7` artifact predates the C3 provenance fix — its `summary.json`'s `defector_house_ids` reads `[]` despite 6 defectors (33.6% dose) actually realized; the dose/ids are independently pinned by `scripts/dose_check.py`/`tests/test_run_provenance.py`, not by that stale field. No number wrong, provenance caveat only. **C2-3:** `sim/logging.py`'s `defector_house_ids` docstring claimed "present ⇒ realized; absent ⇒ configured-only" — false, the field is a dataclass default always emitted by `asdict`, never literally absent for any strategy; rewritten to describe the real (non-empty vs empty, disambiguate via `defector_fraction` + strategy name) signal. **C5-3:** `sim/agents/llm.py`'s temperature-omission comment overclaimed "committed run caches stay valid" — true only in the narrow cache-KEY sense; tightened to note a from-scratch call for a temperature-omitted (non-Haiku) model is non-deterministic and silently overwrites the cached entry, fine today (Haiku-only, always sends temperature=0.0) but any future non-Haiku live cell is non-reproducible-from-scratch by design. **C5-4:** `_reference_cache_dir`'s docstring ("both real layouts sit exactly three levels below the repo root") implied a resume-safety symmetry that doesn't hold; added a CAUTION that only `--reference-cell` runs (stable path) replay on re-invocation — a plain `runs/<ts>-<pid>` cell gets a fresh empty-cache dir every time and isn't automatically resumable on crash. **C2-1:** `_schedule_from_solution`'s docstring gains a caveat that its self-pair renormalization can push a sender's total scheduled obligation beyond its own LP `send` bound — visualization-only; `optimal_metrics()` (the published-ceiling function) never calls this path. Full verify: `ruff check sim tests scripts` clean, `mypy` clean (37 files), `pytest` 407 ✓ (unchanged — no new tests), `scripts.figures --check` green, every committed cell byte-identical to its golden pin. No pin moved. |
| 2026-07-19 | **Fix batch D — commitment retraction by identity + OFFER recipient dedup (C4-1, C3-1)** | _(this commit)_ | 407 ✓ (+2) | TDD fixes for 2 review findings that touch AGENT NEGOTIATION BEHAVIOR — both confirmed real by their skeptic passes but proven physics-inert on every committed live cell, so both were FIX NOW (not ESCALATE) under the frozen-pin gate. **C4-1:** `LLMAgent.retract_commitment` (`sim/agents/agent.py`) removed a `Commitment` via `list.remove(c)` — value-based, not identity-based. `Commitment` is a plain value-equal dataclass, so two commitments minted in the same `react_to_pending()` batch to the same recipient with equal amount+expiry collide by `==`; skeptic-C found this fired for real, twice, in the committed `comm@23` artifact (a bus-refused reply's retraction removed the wrong, already-delivered commitment instead), proven inert both times only by luck (adjacent append order in one case, the agent never reaching the serving loop in the other). Fix: remove by identity (`[x for x in self.commitments if x is not c]`), counter increment guarded on the list actually shrinking — `Commitment`'s `__eq__` left untouched, fix stays local to `retract_commitment`. **C3-1:** `_candidate_recipients` (`sim/agents/agent.py`) returned one `(peer, circle, weight)` tuple per circle with no dedup, so `act()`'s discretionary OFFER block double-counted a peer reachable via 2+ overlapping trust circles into two `Transfer`/`OFFER` objects, while `_emit_requests` (same input) already deduped via its own `best_by_target` (max-weight circle per target) — the two consumers disagreed on semantics. Skeptic-D programmatically checked all 7 shipped `haves_havenots_solar*.yaml` variants against the real `build_overlay_neighborhood` output: 0 overlapping-circle pairs, so genuinely unreachable in any committed cell. Fix: hoisted the max-weight-per-target dedup into `_candidate_recipients` itself (both consumers now agree); `_emit_requests`'s own dedup is now a harmless no-op over an already-deduped list, left untouched. 2 new red-then-green tests: `tests/test_commitment_fixes.py::test_retract_removes_by_identity_not_value` (3 commitments, 2 value-equal with a differently-valued one interposed; retracts the second by correlation id, asserts by `is` that the first survives — failed red against unmodified code, removing the wrong one) and `tests/test_agent.py::test_act_dedups_dual_circle_recipient_to_one_transfer` (dual-circle peer via geographic+owner both weight 1.0 vs a single-circle peer; asserts exactly one Transfer to the dual-circle peer with equal kw — failed red at `2 == 1`, old code produced 2 separate 0.7 kW transfers). **Verified no-op on committed cells: comm@23 pin + replay byte-identical** — `python -m scripts.figures --check` reprints every committed live cell (clean@23/1/7, comm@23, defectors@7, noise@23) to 6 decimals, all match their golden pin exactly (comm@23 served 0.494087, matching the `0.49408665834713517` pin); `test_llm_agent_replay.py`, `test_golden_numbers.py`, `test_figures.py` all pass unchanged; `test_agent.py`, `test_commitment_fixes.py` (incl. the mock clean-cell characterization pin, unchanged — that cell is zero-drop and has no overlapping circles, so both fixes are structural no-ops there too), `test_protocol.py` all green. Full verify (twice): `ruff check sim tests scripts` clean, `mypy` clean (37 files), `pytest` 407 ✓ (405→407). No pin moved, no fix dropped or escalated. |
| 2026-07-19 | **Fix batch C — sim input-validation hardening (critical_load_frac/defector_fraction bounds, NaN policy weight, manual-defector membership, round_robin zero-guard)** | _(this commit)_ | 405 ✓ (+10) | TDD fixes for 5 review findings (C1-1, C3-3, C4-2, C4-3, C2-4), all latent input-validation gaps — no shipped scenario YAML reaches any of them, no published number moves. **C1-1:** `sim/engine.py`'s `crit_range` shape check only asserted a 2-element list, not `0.0 <= lo <= hi <= 1.0` — an out-of-range upper bound (e.g. `[0.2, 1.4]`) would silently deflate `served_critical_load_fraction` downstream (`sim/logging.py`) rather than crash. Extended the existing check. **C3-3:** `sim/agents/policy.py`'s `recipient_priority` weight check was a bare `< 0`, which both `float('nan') < 0` and `float('inf') < 0` evaluate `False` for — now `not math.isfinite(w) or w < 0`, mirroring the pattern already used for `share_min_soc_frac`/`max_share_kw_per_tick`. Skeptic-corrected impact: `Transfer.__post_init__` already raises on non-finite `kw`, so the un-fixed gap was a crash, not silent corruption — fix is unchanged, it just stops the crash at the validation boundary instead. Reachable only via the free-text YAML fallback (not the live forced-tool-use path). **C4-2:** `FailureModeConfig.from_dict` now validates `0.0 <= defector_fraction <= 1.0` (a typo'd `1.5` used to surface as a confusing `random.sample` stdlib error deep in `assign_defectors`). **C4-3:** `assign_defectors`'s manual branch now asserts `defector_house_ids ⊆ house_ids`, raising a `ValueError` listing unknown ids (previously silently no-op'd into a zero-defector clean cell with no error). **C2-4:** `round_robin.py`/`round_robin_overlay.py`'s `soc_kwh / battery_kwh` had no zero-guard — `battery_kwh <= 0` now maps to fraction `0.0` (degenerate-house-is-neutral convention) instead of raising `ZeroDivisionError`. 10 new tests across `tests/test_engine.py` (+2), `tests/test_policy.py` (+3), `tests/test_failure_modes.py` (+3), `tests/test_strategies.py` (+2). Full verify (twice): `ruff check sim tests scripts` clean, `mypy` clean (37 files), `pytest` 405 ✓ (395→405), `scripts.figures --check` green. **Frozen-pin gate reconfirmed byte-identical:** `test_golden_numbers.py` (8), `test_figures.py` (17), `test_llm_agent_replay.py` (3) all pass unchanged; `figures --check` reprints every committed live cell's served/gini to 6 decimals, all match their golden pin, `git status` clean under `docs/figures/`/`docs/phase3_tables.md`. No pin moved, no fix reverted. |
| 2026-07-19 | **Fix batch B — LLM-layer hardening (fable/mythos cost rows, refuse-to-cache empty responses, cache-key field-isolation test)** | _(this commit)_ | 395 ✓ (+12) | TDD fixes for 3 review findings (C5-1, C5-2, C7-1). **C5-1:** `_PRICE_PER_MTOK` (`sim/strategies/llm_agent.py`) was missing rows for the `fable`/`mythos` model families that `sim/agents/llm.py`'s `_TEMPERATURE_DEPRECATED_PREFIXES` already treats as real, live-callable models — a live run on either would have silently reported `$0.00` regardless of actual spend (the exact bug class the project already fixed once, 2026-07-07). Added opus-tier estimate rows (`(5.0, 25.0)`, commented as an estimate pending a real posted rate) plus a test parametrized over the five `_TEMPERATURE_DEPRECATED_PREFIXES` entries asserting cost `> 0`. **C5-2:** `LLMClient.call()` (`sim/agents/llm.py`) unconditionally cached every response, including a `stop_reason=max_tokens` thinking-exhaustion reply (empty text, no tool_input) — the skeptic pass found this had already happened for real, 418 times, in the committed Sonnet-ablation cell. Fix: before `cache.put(...)`, refuse to persist when `resp.text == "" and resp.tool_input is None`; the response is still *returned* so the caller's fallback path engages. **C5-2 gates `put()` only, never `get()`** — committed caches with pre-existing empty entries keep replaying byte-identically (`test_llm_agent_replay.py` reconfirmed green, unchanged). **C7-1 (test-only, no production change):** `tests/test_cache.py` gained `test_cache_key_changes_when_any_hashed_field_varies`, parametrized over `model`/`system`/`temperature`/`max_tokens`/`tools_schema` — the prior test only isolated `user`, leaving 4 of `cache_key()`'s 5 other hashed fields unguarded against a silent-drop regression (real risk: this code churned twice in the last two weeks — temperature-omission fix, judge token-budget fix). Verification (fresh 3.12 venv): `ruff`/`mypy --strict` clean, `pytest` 395 ✓ (383 + 12: 5 params C5-1 + 2 tests C5-2 + 5 params C7-1), `scripts.figures --check` green. Frozen-pin gate reconfirmed: `test_golden_numbers.py`, `test_figures.py`, `test_llm_agent_replay.py` all pass unchanged — no published number moved. |
| 2026-07-19 | **Fix batch — Cluster 6 script-layer hardening (figures --check, override typo rejection, baseline/config drift + seed-dir + rubric-pairing + paid-artifact guards)** | _(this commit)_ | 383 ✓ (+19) | TDD fixes for 6 review findings (C6-1..C6-6), all latent/no-op-today per the skeptic pass — no published number was ever wrong. `scripts/figures.py --check` now actually asserts committed served/gini against a golden pin (`EXPECTED_LIVE`) instead of only printing (was a total no-op). `sim/overrides.py` rejects a typo'd FINAL leaf key inside dict-typed Scenario fields (`llm`/`household_sampling`/`data_paths`) instead of silently adding a dead key — reuses `_KNOWN_LLM_KEYS` for `llm`, falls back to "already present" for the mode-dependent `household_sampling`/`data_paths`. `regen_baselines` now asserts the current scenario YAML matches the committed live cell's frozen `config.json` on determinism-relevant fields before regenerating baselines (armed landmine, not yet triggered). `scripts/run.py`'s `--reference-cell` dir suffix now derives from the scenario's actual final seed (catches `--set seed=...`, not just `--seed`), plus a `--force`-gated refusal to overwrite a non-empty reference-cell dir. `_variant_agreement`'s `(sender, t_sent)` pairing (80.4% collision rate on real data) is tightened with `correlation_id` (now stored in `eval_explanations.py`'s sample rows); `.get(...)` keeps it a no-op on pre-fix artifacts. `evaluate_run` gains an existence guard (`force=`/`--force`) before writing, so a same-variant re-run can't silently clobber a paid judging artifact. **No published number moved**: `docs/phase3_tables.md` byte-identical after `scripts.figures --all`; frozen pins (`test_golden_numbers.py`, `test_figures.py`, `test_llm_agent_replay.py`) unchanged. (PNG re-render differs byte-for-byte from committed in this fresh venv's matplotlib — confirmed via A/B against unmodified base code that this is a pre-existing environment/version artifact, not caused by this batch; `docs/figures/` left untouched.) ruff + mypy --strict clean. |
| 2026-07-19 | **Repo-Cleanup Plan Task 6 — WRAP: counts synced, figures byte-verified, public-repo hygiene pass** ✅ | _(this commit)_ | 364 ✓ | Closes the 8-item repo-cleanup batch (batch #3 of 4). Full verify twice: `ruff check sim tests scripts` clean, `mypy` clean (37 source files — plotter deleted in Task 3), `pytest` 364 ✓ both runs. `python -m scripts.figures --check` + `--all` → `git status` clean for `docs/figures/` + `docs/phase3_tables.md` (byte-identical regeneration proves this batch moved no published number). Test-count sync: README status line "367 as of 2026-07-19" → **364** (the only forward-looking count claim; CLAUDE.md `367 ✓` progress rows are historical, left verbatim). **Public-repo hygiene:** full-tree key scan `git grep -IlE 'sk-ant-(api\|oat)[0-9a-zA-Z_-]{15,}'` → 0 (34,158+ tracked files); no `.env*` tracked; `.gitignore` still covers `runs/`/`data/`/regenerable `reference_runs/` baselines; `LICENSE` (MIT) intact + referenced from README; README already front-door-first (what/headline/install/reproduce → history → license — no reorder needed). `docs/plans/README.md` batch-#3 row flipped **pending → done**. No `sim/`/test change. |
| 2026-07-19 | **Repo-Cleanup Plan Task 5 — README: Phase 3 section labelled complete** | _(this commit)_ | 364 ✓ (unchanged) | Structural label fix. `## Phase 3 — Benchmark & Experiments` header `(in progress, 2026-07-07)` → `(complete)`; the section's status line updated from "infrastructure complete; live runs budget-gated" to "benchmark + live runs + analysis all complete (`phase3-infra-complete` → `phase3.3-complete`)" with a pointer to `docs/phase3_results.md`. The README top status line, archived spec/plan links (Task 2), and plotter-free architecture listing (Task 3) were already current. No `sim/`/test change; 364 tests unchanged. |
| 2026-07-19 | **Repo-Cleanup Plan Task 4 — condense CLAUDE.md (single Status section, rows ≤07-12 archived, pointers fixed)** | _(this commit)_ | 364 ✓ (unchanged) | Collapsed the five per-phase status blocks into one `## Status` section (headline numbers carried; full story in `docs/phase3_results.md`); moved 53 progress rows dated 2026-07-06 → 2026-07-12 (Phases 2.9 → 3.1) verbatim to `docs/progress_log_archive.md` (now spans → 2026-07-12); rewrote the Critical-files table (roadmap first, `docs/plans/`, phase3 results/tables/figures, archive dir); session-start steps 2-3 now point at the roadmap + active `docs/plans/` batch; deleted the superseded struck-through repo-private warning line; four-phase table Phase-3 row → complete. **≤35 KB gate WAIVED by Leo** — kept all ≥07-15 rows verbatim (they + the verbatim-preserved decision sections floor the file near ~87 KB, down from 132 KB); recent live-run detail stays in-file over the numeric target. No `sim/`/test change; 364 tests unchanged. |
| 2026-07-19 | **Repo-Cleanup Plan Task 3 — remove verified-dead code (4 funcs + phase-1 plotter)** | _(this commit)_ | 364 ✓ (−3) | Deleted `default_affiliations` (sim/network.py), `policy_to_yaml` (sim/agents/policy.py), `optimal_served_fraction` (sim/strategies/lp_optimal.py), `MemoryStream.from_jsonl` (sim/agents/memory.py) — all zero non-test call sites (controller-verified) — plus `scripts/plot_phase1_results.py`. Test fallout resolved: 2 `default_affiliations` tests + the `MemoryStream` jsonl round-trip test deleted (−3); `test_policy_round_trip_yaml`→`test_policy_from_yaml_parses` reworked to a parse-only `policy_from_yaml` test with a captured YAML literal (keeps YAML-parse coverage, drops the serialize leg); the 2 real LP tests migrated `optimal_served_fraction(...)`→`optimal_metrics(...)["served_load_fraction"]` (behavior identical). lp_optimal docstrings that named the deleted accessor updated. `MemoryStream.write_jsonl` (also unused, but plan-scoped as keep) left untouched. Deleting `default_affiliations` left `numpy` unused in `sim/network.py` — import removed (ruff-flagged). pyarrow `[data]` extra annotated (pd.read_parquet engine — no direct import). README scripts-listing plotter row removed. Symbol sweep → 0 hits; ruff + mypy --strict clean (37 source files); clean-install dry-run green; net 364 tests (367 baseline − 3). |
| 2026-07-19 | **Repo-Cleanup Plan Task 2 — archive completed-phase specs/plans + one-off docs** | _(this commit)_ | 367 ✓ (unchanged) | `git mv` 7 specs + 8 plans → `docs/superpowers/archive/{specs,plans}/`, plus the opus-3.2 execution prompt and `phase1_real_data_result.png` → `docs/superpowers/archive/`. Roadmap (`2026-07-12-remaining-phases-roadmap.md`) STAYS in specs/ (live Phase-4 pointer). New `archive/README.md` index. Reference updates: README.md link line + the two phase3.2-playbook pointers repointed to `/archive/` paths; roadmap references zero moved paths (verified). CLAUDE.md path references left for Task 4's condensation; `docs/plans/` batch-record refs and archived-file internal refs left as historical records. Dangling-ref gate `grep -rnE "superpowers/(specs\|plans)/2026-0" --include="*.md" . \| grep -v /archive/` → only CLAUDE.md + docs/plans/ hits remain. No `sim/`/test change; 367 tests unchanged. |
| 2026-07-19 | **Repo-Cleanup Plan Task 1 — true up phase3.2 plan's stale Stage 3/4 checkboxes before archiving** | _(this commit)_ | 367 ✓ (unchanged) | Docs-only. Checked the 16 real stale checkboxes (`- [ ]`→`- [x]`) in `docs/superpowers/plans/2026-07-12-phase3.2-live-runs.md` — Stage 3 (Sonnet ablation, done 2026-07-18), Stage 4 (judging, done 2026-07-17), credential-gate + Stage-2 gate decisions — and added a dated trued-up banner under the title. Anchored verify `grep -cE '^- \[ \]'` → 0 (the brief's unanchored grep returns 1 on line 3's prose `- [ ]`, which is inside backticks and correctly left untouched). No `sim/`/test change; 367 tests unchanged. |
| 2026-07-19 | **Test Suite Accuracy Plan Task 8 — WRAP: docs sync (test count + real mypy scope); tag n/a (maintenance batch)** ✅ | _(this commit)_ | 367 ✓ (unchanged) | Docs-only wrap closing the 8-task Test-Accuracy hardening plan (batch #2 of the 2026-07-18 maintenance batch). `README.md` status line: "Run `pytest` for the current test count" → adds "(367 as of 2026-07-19)" and "mypy --strict clean" → "mypy --strict (sim/ + scripts/) clean" (Task 5 widened the real scope). `CLAUDE.md` Conventions: "`mypy --strict` on `sim/`" → "on `sim/` + `scripts/`" with a dated pointer to Task 5. `docs/plans/README.md`: flipped the batch-#2 row from **pending** → **done**. Historical phase-completion status blocks (which quote their own then-current mypy/test claims) left verbatim per the no-rewrite rule — only the two live/forward-looking tooling statements updated. **Full verify (twice):** `ruff check sim tests scripts` clean, `mypy` clean (38 files, sim + scripts), `pytest` 367 ✓ both runs. Security grep over the staged diff (`grep -cE 'sk-ant-(api\|oat)[0-9a-zA-Z_-]{15,}'`) → 0 (Task 3's `sk-ant-oat01-FAKE` fixture is deliberately below the 15-char run, does not match). No `sim/`/test change. **Plan complete: 8/8 tasks, whole-branch review clean.** |
| 2026-07-19 | **Test Suite Accuracy Plan Task 7 — pin shipped scenario-YAML count** ✅ | _(this commit)_ | 367 ✓ (+1) | New test `test_shipped_scenario_count_is_pinned` in `tests/test_scenario.py` pins `len(sorted(Path("configs/scenarios").glob("*.yaml"))) == 21` using the IDENTICAL glob as the strict-load parametrize at line 297, so the data-driven test total can't drift silently when YAMLs are added/removed. Mutation check: temporarily changed assertion to `== 20` — test failed and listed all 21 names, confirming the assertion bites; reverted. Full suite 367 ✓ (+1 from 366); ruff + mypy --strict clean. Zero `sim/` changes; characterization pin with no red step (value already correct). |
| 2026-07-19 | **Test Suite Accuracy Plan Task 6 — exact pins on e2e served-load and failure-axis effects (test-only)** ✅ | _(this commit)_ | 366 ✓ (unchanged) | Tightened range-only / inequality-only e2e assertions into exact numeric pins, per the same rationale as `tests/test_golden_numbers.py` (byte-determinism makes exact pins free and catches an in-range physics regression the old bounds couldn't). **`tests/test_engine.py`:** `test_run_smoke_no_coordination`, `test_run_resstock_path_end_to_end`, `test_run_real_data_path_end_to_end` each gain `assert summary["served_load_fraction"] == pytest.approx(1.0, abs=5e-7)` above the existing `0.0 <= … <= 1.0` bound (all three of these short smoke fixtures serve 100% of load — kept as a second line of defense, not redundant, since it still catches a partial-serve regression). **`tests/test_llm_agent_integration.py`:** pinned the mock-pipeline `served_load_fraction == 0.5189926422777774` and `len(msgs) == 10988` (kept the `> 0` lines). **`tests/test_llm_agent_failure_axes.py`:** each of the three failure-axis tests (`test_defector_wrapper_corruption_changes_outcomes`, `test_noise_changes_outcomes_vs_clean`, `test_comm_constraint_reduces_delivery_AND_changes_outcomes`) gains an exact pin of the TREATED cell's `served_load_fraction` (dirty 0.5227893278851087, noisy 0.5204974940507625, constrained 0.4915447847393646) — the existing `!= clean` OR-assertions are kept as-is (they prove the causal effect *exists*; the new pins freeze its *size*). All values derived by running each test's exact code path twice outside pytest and confirming byte-identical `repr()` output before pinning (never invented). Added `import pytest` to the two `test_llm_agent_*` files (`test_engine.py` already had it). **Zero `sim/` changes.** Stability gate: full suite run twice (366 ✓ both times) + the 3 touched files run a third time standalone (18 ✓) — all three identical, confirming byte-determinism holds. ruff + mypy --strict clean. |
| 2026-07-19 | **Plan batch Task 5 — mypy covers `scripts/` (config + annotation fixes)** ✅ | _(this commit)_ | 366 ✓ | Mypy now covers both `sim/` and `scripts/` with `--strict`. Changes: (1) `pyproject.toml` line 38: `files = ["sim"]` → `files = ["sim", "scripts"]`; added matplotlib override block for untyped module; (2) `.pre-commit-config.yaml` mypy hook scope: `^sim/.*\.py$` → `^(sim|scripts)/.*\.py$`; (3) `scripts/plot_phase1_results.py:36` annotation fix: bare `dict` → `dict[str, Any]`, added `from typing import Any` import; kept existing `# type: ignore[no-untyped-def]` on untyped strategy param. Config-only, zero runtime behavior change. Full verification: mypy clean (38 source files), ruff clean, pytest 366 ✓, clean-install dry-run green (fresh venv, pip install -e ".[dev,data]", pytest 366 ✓). |
| 2026-07-19 | **Test Suite Accuracy Plan Task 4 — e2e reference-cell resume test: zero provider calls on replay (test-only)** ✅ | _(this commit)_ | 366 ✓ (+1) | New `test_reference_cell_resume_replays_with_zero_provider_calls` in `tests/test_llm_agent_replay.py`, modeled on `test_two_runs_with_same_mock_are_byte_identical`. Run 1 populates a reference cell (`tmp_path/reference_runs/scenA/llm_agent/clean/llm_cache`) via a `_make_llm_client` factory that mirrors the real one (`PromptCache(local_dir=run_dir/"llm_cache", reference_dir=llm_strat._reference_cache_dir(run_dir))`); run 2 resumes from a `tmp_path/runs/scenA/llm_agent/<ts>` dir (same parents[3]-is-repo-root layout `_reference_cache_dir` expects) using a `_TrapLLMClient(MockLLMClient)` subclass whose `_call_provider` raises `AssertionError("paid call attempted — cache resume failed")` — proving every `LLMClient.call()` in run 2 is served by the reference tier, never falling through to a "paid" call. Asserts run 2's `summary.json` macro fields (`served_load_fraction`, `gini_welfare`, `transfer_count`, `min_house_served_fraction`, `jains_index`, `served_critical_load_fraction`) equal run 1's exactly. **Mutation check:** temporarily made `sim/strategies/llm_agent.py`'s `_reference_cache_dir` return `None` unconditionally — the new test FAILED with the trap's `AssertionError` (reference_dir resolved to `None`, run 2's empty local cache missed, `_call_provider` was reached), proving the test actually exercises the resume path; reverted, `git diff --stat -- sim/` confirmed empty. Full suite 366 ✓ (365→366, +1); ruff + mypy --strict clean. |
| 2026-07-19 | **Test Suite Accuracy Plan Task 3 — pin retry exhaustion, connection/server-error retries, OAuth ctor routing (test-only)** ✅ | _(this commit)_ | 365 ✓ (+3) | Three new tests appended to `tests/test_llm_client.py`, no `sim/` changes: `test_anthropic_client_raises_after_retry_exhaustion` (every attempt fails → `_call_provider` re-raises `last_exc` after exactly `max_retries` attempts and sleeps), `test_anthropic_client_retries_on_connection_and_server_errors` (`APIConnectionError` then `InternalServerError` then success — both non-rate-limit exception types are caught by the same except-tuple and retried), `test_anthropic_client_oauth_key_routes_to_auth_token` (`sk-ant-oat…` keys construct the SDK client with `auth_token=`/`api_key=""`/`max_retries=0`; other keys use `api_key=` with no `auth_token` kwarg). No contingency needed — `anthropic.APIConnectionError(request=MagicMock())` constructs fine (verified directly), so the brief's httpx.Request fallback wasn't used. **Mutation check (the point of pinning):** temporarily changed `sim/agents/llm.py`'s `raise last_exc` to `return None`, ran the exhaustion test — it failed (an `AttributeError` inside `LLMClient.call` on the now-`None` response, since `pytest.raises(RateLimitError)` no longer caught anything), confirming the test actually exercises the re-raise; reverted via `git checkout -- sim/agents/llm.py` (`git diff --stat -- sim/` empty, confirmed clean). Full suite 365 ✓ (362→365, +3); ruff + mypy --strict clean. |
| 2026-07-19 | **Test Suite Accuracy Plan Task 2 — TZ-independence test refactored to monkeypatch** ✅ | _(this commit)_ | 362 ✓ | `test_nrel_noise_is_timezone_independent` refactored from manual `os.environ` try/finally to pytest `monkeypatch` fixture. Env restoration now owned by pytest even on hard failure. Test function signature unchanged, behavior identical, passes in isolation. All 362 tests pass, ruff + mypy clean. |
| 2026-07-19 | **P-A Task 1 — make `test_negotiation_counters_reach_summary` self-sufficient** ✅ | _(this commit)_ | 362 ✓ | Test passed in isolation without module-global dependence; fix makes it explicit. Replaced the module-global `_REGISTRY` reference with an explicitly-prepared registry via `_prepare_with_failure()`, passing `decide.registry` to `update_summary_with_counts()`. Test now fully self-contained and verifiable in isolation. TDD: test already passed (no red step triggered), but the fix correctly eliminates the module-global side-effect. ruff + mypy clean, all 362 tests pass. |
| 2026-07-19 | **P3-accuracy Plan B Task 8 — comm@23 live re-run post C1/C2/C3: bugs, not fragility, drove most of the comm loss** ✅ | _(this commit)_ | 362 ✓ | The one authorized re-spend of the batch (Leo authorized ~$4–7, hard cap $8; **actual $3.96**, 292 cache hits / 1,506 fresh Haiku calls, 66 min). Re-ran `haves_havenots_solar__comm` @ seed 23 via `--reference-cell comm --seed 23` (writes the committed dir in place; the 1,662-response cache replayed early ticks free until the first C1/C2 divergence, then paid fresh). **served 0.4565 → 0.4941** (+3.8 pts); control 0.5027 unchanged → live-vs-control gap **−4.6 → −0.86 pts** (gap-closed control→LP −17.4% → −3.2%); gini_welfare 0.4803 → 0.4531, Jain 0.5665 → 0.5959, served-critical 0.6412. **C1 verified in the field:** `commitments_retracted = 202`, matching **exactly** the 202 budget/comm-dropped ACCEPT/COUNTER replies (kwh>0) in `messages.jsonl` — every retraction undoes a promise the requester never saw. **C3 honest:** `defector_house_ids = []` (no-defector cell). Verification block all-pass (sanity 0.3340 ≤ 0.4941 ≤ LP 0.7684; plan-parse 25/655 = 3.8% < 5%; react_amount_defaulted 0). **Finding (pre-registered outcome, HIT):** two commitment-integrity bugs accounted for most of the original −4.6 comm loss; fixing them recovers the cell to within −0.86 of the control, and the residual is the untouched send-order bandwidth channel (11,194 of 16,954 messages budget-dropped) — so the "negotiation costs bandwidth under scarcity" framing survives but at a **much smaller magnitude**, and most of the original apparent loss was measurement bugs, not LLM fragility. n=1 seed. Downstream regenerated deterministically (`scripts/figures --all`): headline/fairness/scatter/failure-axis PNGs + `phase3_tables.md` comm rows move; `figures --check` green. Docs updated: `phase3_results.md` §2/§3/§4 comm numbers + narrative, 3.2 plan Addendum 2 (post-fix record appended, pre-fix record untouched), CLAUDE.md status. Key grep over staged diff + full tree → 0 (the 1,506 new cache files store prompt/response/model/tokens only). |
| 2026-07-19 | **P3-accuracy Plan B Task 6 — wire Sonnet capability ablation into tables/results; purge stale stage-3/4 stubs ($0 docs)** | _(this commit)_ | 362 ✓ (+2) | The committed Stage-3 Sonnet ablation artifact (`reference_runs/haves_havenots_solar__llm_sonnet/llm_agent/clean_sonnet__seed23/summary.json`) appeared nowhere in `scripts/figures.py`. Added `ABLATION_CELLS` beside `LIVE_CELLS` (kept separate on purpose — the four-cell figures stay 4-cell and tell the failure-axis story; the ablation is a capability data point, not a fifth failure axis) + a `CELL_LABEL` entry `"clean (Sonnet)"`; `read_live_summary` now resolves through `{**LIVE_CELLS, **ABLATION_CELLS}` (unknown cell/seed still raises `KeyError`); `_live_runs()` appends the ablation row after the four failure-axis rows so `render_tables`'s negotiation table picks it up automatically. Negotiation-table prose gains two sentences: naming the ablation row, and flagging that its `commitments_expired` predates the Task-5 C1/C2 fixes (verified via `git log`: the ablation run `0e64e6e6` was committed before `d3b5ff14`/`0fdc9424`) so its expiry percentage isn't comparable to the post-fix Haiku rows. Fixed the stale `render_tables` docstring ("stays a pending Stage 4 stub" — Stage 4 has been done since 2026-07-17) to describe the populated behavior; the empty-artifact branch stays as the honest no-artifact fallback. `docs/phase3_results.md` gains a new "§5.5 Capability ablation (Stage 3)" block (Sonnet 0.6685 vs Haiku 0.6726 — a statistical tie, both clear control/rr by comparable margins, gap-closed 29.4% vs 32.3%; finding: model capability doesn't move clean-cell coordination, corroborates the P2.8 ceiling + advisor's scenario-design guidance; caveats: n=1, non-deterministic since `temperature` is omitted for Sonnet, higher parse-noise absorbed by the negotiation machinery) — inserted as §5.5 rather than renumbering §6/§7, since the already-committed Task-5 progress-log row references "§6 Limitations" and a renumber would make that historical entry misleading. §7 "What's next" updated: Stage 3 moves from "optional add-on" to done; remaining deferred = VT/AZ cells + `all`@7 (confounded comm component). `README.md` status line + two body sections (`Fairness + explainability substrate`, `Budget-gated next steps`) updated: Stage 3/Stage 4 done with headline numbers inlined, "pending advisor sign-off" and "stubbed pending" claims removed. Regenerated `docs/phase3_tables.md` via `python -m scripts.figures --tables` — `git status` confirms it is the ONLY regenerated-artifact diff (no `docs/figures/*.png` touched); `python -m scripts.figures --check` green, unchanged (it still walks only `LIVE_CELLS`, which is untouched — figures stay 4-cell as designed). TDD: 2 new red-then-green tests (`test_ablation_cell_reads_committed_sonnet_summary`, `test_tables_include_capability_ablation_row`) confirmed red before the `ABLATION_CELLS`/label existed. Full suite 362 ✓ (360→362, +2); the frozen 4-cell `LIVE_CELLS` pin and the `0.6725764138021589` served pin at `tests/test_figures.py:59-66` are untouched — verified byte-identical. ruff + mypy --strict clean. No `sim/` change; no API calls, $0. |
| 2026-07-19 | **P3-accuracy Plan B Task 5 — annotate pre-fix live cells + expiry-metric semantics ($0 docs)** | _(this commit)_ | 360 ✓ (unchanged) | Docs-only honesty pass so the committed pre-fix live artifacts can't be silently misread after C1/C2. `docs/phase3_results.md` §6 Limitations gains three bullets: (a) the clean/defectors/noise live cells were produced **before** C1 (`d3b5ff14`) / C2 (`0fdc9424`) — pre-batch baseline `cf18b13d` — so they are frozen measurements of the pre-fix protocol and **NOT re-derivable from cache with current code** (a naive replay re-pays instead of hitting the prompt cache); this is concrete, not hypothetical — the clean seed-23 solar run's own 914-expired-of-3,343-made proves the below-threshold-committer population C2 changes exists in that run; (b) `commitments_expired` changed definition under C2 (now also counts promises that die while HELD below the committer's own `share_min_soc_frac`) → pre-fix vs post-fix expiry percentages (clean@23 27.3%, defectors@7 17.8%, noise@23 5.7%) are not directly comparable; (c) expiry is only counted while islanded (pre-existing scope note). Appended a dated addendum to `docs/superpowers/plans/2026-07-12-phase3.2-live-runs.md` carrying the same two notes — **append-only, the pre-fix run-log record is left verbatim** per the no-rewrite rule. **No published served/gini/Jain number is wrong** — these are honest caveats, not corrections. `docs/phase3_tables.md` left untouched (generated; its caveat lands in Task 6's `render_tables` prose). No `sim/`/test change; full suite 360 ✓ unchanged, ruff + mypy --strict clean. |
| 2026-07-19 | **P3-accuracy Plan B Task 4 — C2: hold (don't serve) commitments below own share_min_soc_frac** | _(this commit)_ | 360 ✓ (+3) | A committed transfer bypassed the below-mean discretionary filter (a promise is a promise) but was ALSO bypassing the agent's own `share_min_soc_frac` safety floor — an agent that had since fallen below its own threshold could still export committed energy, exactly the safety violation the floor exists to prevent. Fix is **hold, don't serve**: `sim/agents/agent.py`'s commitment-serving region in `act()` now runs a **hoisted expiry sweep FIRST, unconditionally** (a held promise still ages out on its original TTL, counted via `n_commitments_expired`, whether or not the agent may currently serve) — `self.commitments` is rebuilt from only the non-expired entries; the serving loop is then gated `if self.commitments and soc_frac >= self.policy.share_min_soc_frac:`, with the keep-condition inside simplified to `if c.kwh_remaining > 1e-9` since expired entries are already gone before the loop runs. Above-threshold behavior (budget_kw computation, partial-serve math, OFFER `payload={"kwh": …, "committed": True}` construction) is otherwise byte-identical to before. The gate reads the noised `soc_frac` already in scope (decide-on-what-you-see, unchanged design) — no engine-truth peek, no new RNG consumer, no prompt/cache-string edit. `Commitment`'s docstring updated to state the promise is held (not dropped) below the agent's own floor, keeping "bypasses the below-mean belief filter" but no longer implying it bypasses the agent's own threshold. TDD: 2 new red-then-green tests — `test_below_threshold_holds_commitment_then_serves_after_recovery` (nothing ships at 0.20 frac, ledger keeps the promise, still REQUESTs; after recovering to 0.80 frac it serves and clears) and `test_below_threshold_commitment_expires_while_held` (stays at 0.20 frac for 3 ticks with a TTL of 1: transfers always empty, `n_commitments_expired == 1` — proving the hoisted sweep counts an expiry that was never serviceable) — plus a 3rd regression test, `test_above_threshold_commitments_still_bypass_below_mean_filter`, a verbatim-adapted copy of `test_agent.py::test_commitment_produces_transfer_bypassing_belief_filter` (SoC 0.8 ≥ 0.30 threshold) proving above-threshold serving is untouched. **Deviation from the task brief's example code:** the brief's first test used `Commitment(recipient="r0c0", ...)` on an agent whose own `house_id` is also `"r0c0"` — a self-transfer, which `sim.types.Transfer.__post_init__` rejects unconditionally; changed the recipient to `"r0c1"` (matching the sibling regression test and its `test_agent.py` original) so the recovery-and-serve half of the test can actually construct the transfer it asserts on. **The batch's only authorized pin move** — the Task-1 mock clean-cell characterization pin shifted (below-threshold agents stop over-exporting and instead serve more of their own load): `served_load_fraction` 0.519560067167591 → 0.5232789139715761 (up — held reserves serve the holder's own load better than exporting them did), `transfer_count` 664 → 302, `commitments_made` 2287 → 2023; `commitments_retracted` stays 0 (zero-drop cell, C1 no-op preserved). Confirmed deterministic over 2 fresh runs before pinning. Full suite 360 ✓ (357→360, +3); all frozen gates reconfirmed green: `test_golden_numbers.py`, `test_figures.py`, `test_llm_agent_replay.py` (byte-identity — the shipped replay fixtures are above-threshold committers, so C2 is a true no-op there), `test_agent.py` in full (46/46, incl. the SoC 0.8-0.9 commitment tests at 914-982, the T11 partial-fulfillment/list-order/expiry pins at 1242-1282, and the react-prompt pin at ~1041), all `test_protocol.py`; ruff + mypy --strict clean. |
| 2026-07-19 | **P3-accuracy Plan B Task 3 — C1: retract commitments whose reply the bus refused** | _(this commit)_ | 357 ✓ (+5) | The bus could REFUSE a react reply (budget overflow / comm drop under `comm@23`-style cells), but the agent had already registered the commitment behind that reply — energy would still ship against a promise the requester never saw. Fix is **register-then-retract, not defer-registration** (deferring would change the `open_kwh` line rendered into same-tick subsequent react prompts even in zero-drop cells → cache-key drift → committed cells stop replaying and silently re-pay money): `sim/agents/protocol.py` `MessageBus.send()` return type `None`→`bool` (True = entered the queue, False = refused; each of the three existing drop paths just gains a `return False`, the success path a `return True` — no other logic, no RNG/budget-accounting change). `sim/agents/agent.py`: the ACCEPT/COUNTER registration block now also stashes the `Commitment` in a new `_reply_commitments: dict[correlation_id -> Commitment]` (provisional); new `n_commitments_retracted` counter (made stays "promise uttered", never decremented — retracted is the new C1 observable); `react_to_pending` clears `_reply_commitments` at the top of every tick; new `retract_commitment(reply)` pops by correlation_id, removes from `self.commitments`, increments the counter — idempotent, no-op for REJECTs/unknown ids. `sim/strategies/llm_agent.py`'s reply loop: `if not reg.bus.send(...): agent.retract_commitment(m)` (only the reply loop — the outbox/INFORM send sites are untouched, matching the brief). Counter plumbing (`commitments_retracted`) threaded through the zero-dict + `current_call_counts` into `llm_call_counts_detailed`. **Zero prompt-text edits** (`_PLAN_SYSTEM_PROMPT_*`/`_REACT_SYSTEM_PROMPT_*`/f-strings/`_POLICY_TOOL_SCHEMA` untouched) — cache keys for every committed live cell are unaffected. TDD: 5 new tests red-then-green — `test_send_reports_acceptance_and_refusal` (protocol), `test_retract_commitment_undoes_the_refused_reply` + `test_retract_is_a_noop_for_reject_replies` (unit), `test_budget_dropped_reply_leaves_no_commitment` + `test_no_drop_run_retracts_nothing` (new facade helper `_prepare_1x3_with_budget`, modeled on `test_strategy_llm_agent.py::_prepare_with_failure`, seeding two REQUESTs to a middle house through the bus and ticking once — grid-CONNECTED so `act()` returns early and doesn't itself consume the commitments the same tick, isolating the react/retract bookkeeping under test). **The Task-1 characterization pin's three original values (served=0.519560067167591, transfers=664, made=2287) are byte-for-byte UNCHANGED** — a 4th assertion (`commitments_retracted == 0`) was appended, proving C1 is a true no-op on the zero-drop mock cell. Full suite 357 ✓ (352→357, +5); all frozen gates reconfirmed green: `test_golden_numbers.py`, `test_figures.py`, `test_llm_agent_replay.py` (both text and tool_input replay modes — proves the reply-loop change is behavior-neutral in zero-drop cells), `test_agent.py` (incl. the SoC 0.8-0.9 commitment tests and the react-prompt pin), all pre-existing `test_protocol.py` tests; ruff + mypy --strict clean. |
| 2026-07-18 | **P3-accuracy Plan B Task 2 — C3: realized defector house ids recorded in summary.json** | _(this commit)_ | 352 ✓ | `assign_defectors` computed the random defector draw but nobody persisted it — every committed `defectors` cell's `summary.json` carried `failure_modes_active.defector_house_ids: []` (the CONFIGURED tuple, empty for `defector_assignment: random`), making the realized draw invisible in every artifact. Purely additive provenance, no engine/physics/RNG/message change: `_AgentRegistry` gains a defaulted `defector_house_ids: tuple[str, ...] = ()` field, populated in `prepare()` as `tuple(sorted(defectors))`; `update_summary_with_counts` hoists `reg = registry or _REGISTRY` and, when `reg is not None`, writes `list(reg.defector_house_ids)` into `failure_modes_active.defector_house_ids` (never a raw `set` — deterministic sorted order). `sim/logging.py` gains a one-line caveat: field present ⇒ realized (post-run, llm_agent/llm_fallback only), absent ⇒ configured-only (round_robin/lp_optimal never realize an assignment). Two new red-then-green tests in `tests/test_run_provenance.py`: the defectors cell now round-trips `sorted(assign_defectors(...))` exactly (6 of 30 houses at `defector_fraction=0.20`), the clean cell stays `[]`. All frozen pins (`test_golden_numbers.py`, `test_figures.py`, `test_llm_agent_replay.py`, `test_agent.py`, `test_protocol.py`) unchanged and green — the replay byte-identity test compares state/events/messages.jsonl, not summary.json. Committed pre-fix `reference_runs/` artifacts are NOT rewritten. Full suite 352 ✓ (+2); ruff + mypy --strict clean. |
| 2026-07-18 | **P3-accuracy Plan B Task 1 — characterization pin for mock clean cell (pre-C1/C2)** | _(this commit)_ | 350 ✓ | New `tests/test_commitment_fixes.py`: a characterization pin freezing the current, unmodified mock-clean-cell (`haves_havenots__llm.yaml`) outcome — `served_load_fraction=0.519560067167591`, `transfer_count=664`, `llm_call_counts_detailed.commitments_made=2287` (verified deterministic over repeated runs before pinning). Plus the shared `_agent`/`_own`/`_request`/`_run_mock` helpers Tasks 3 (C1: retract commitments whose reply the bus refused) and 4 (C2: hold below-threshold commitments instead of exporting) build on, modeled on `tests/test_agent.py`'s and `tests/test_llm_agent_failure_axes.py`'s existing patterns. Green on unmodified code — no `sim/` changes in this task. Full suite 350 ✓; ruff + mypy --strict clean. |
| 2026-07-18 | **Plan batch: repo audit + cleanup — 4 executor-ready plans ($0 planning)** | _(this commit)_ | 349 ✓ (unchanged) | Leo's brief (review all code / tests accurate / clean unused planning files / condense CLAUDE.md / clean unused code / Phase-3 max accuracy — verbatim in `docs/notes/2026-07-18-audit-cleanup-brief.md`, later addition: repo is public+open-source, verify no key exposure) → 4-agent read-only audit + 4 plans under **`docs/plans/`** (new maintenance-batch home; see its README; execution order **B→A→D→C**, one fresh Opus session each). **Audit verdicts:** every published Phase-3 number recomputes exactly from committed artifacts (no numeric corrections anywhere); test suite honest (0 skips/xfails, 349 count reconciles = 320 defs + 29 parametrize); 0 key-shaped strings across all 34,158 tracked files, no `.env`. **Plan B (first, money-gated):** C1 register-then-retract for bus-refused ACCEPTs (deliberately NOT defer-registration — that changes prompt bytes in zero-drop cells → cache re-pay), C2 hold-don't-serve commitments below own `share_min_soc_frac` (hoisted expiry keeps aging visible), C3 realized `defector_house_ids` into summary.json, Sonnet-cell wiring via `ABLATION_CELLS`, stale Stage-3/4 doc claims purged, then **comm@23 live re-run (Leo authorized ~$4–7, hard cap $8)** vs same-seed control with pre-registered expectation (residual loss expected — send-order bandwidth channel untouched). Committed clean/defectors/noise cells stay as annotated pre-C2 artifacts. **Plan A:** order-dep `_REGISTRY` test fix, TZ via monkeypatch, retry-exhaustion/conn-error/OAuth-ctor pins, reference-resume e2e ($0-replay proof), mypy→`scripts/`, exact pins on range-only e2e + failure-axis tests, scenario-count pin. **Plan D:** archive completed-phase specs/plans + one-off docs to `docs/superpowers/archive/`, true-up 16 stale 3.2-playbook boxes, delete 4 dead functions (`default_affiliations`/`policy_to_yaml`/`optimal_served_fraction`/`MemoryStream.from_jsonl`) + phase-1 plotter, CLAUDE.md 101→~30 KB (rows ≤07-12 archived), public-repo hygiene pass. **Plan C:** full fresh adversarial re-review (Leo's pick over targeted), 8 clusters + skeptic verification + ESCALATE rule for anything touching published-run physics. |
| 2026-07-18 | **P3.2 Stage 3 — Sonnet capability ablation: model capability does NOT move clean-cell coordination** ✅ | _(this commit)_ | 349 ✓ | Ran `haves_havenots_solar__llm_sonnet` (`claude-sonnet-5` agents) clean@23. **served 0.6685 / gini 0.269 / Jain 0.7944 / served-critical 0.8347** — a statistical TIE with the Haiku clean@23 (0.6726 / 0.2704 / 0.7936 / 0.8347), in fact **0.4 pts BELOW** Haiku. Both beat the zero-LLM control 0.6269 (Sonnet **+4.2**, Haiku +4.6) and round_robin 0.6444 (Sonnet **+2.4**, Haiku +2.8); Sonnet closes **29.4%** of the control→LP gap (LP 0.7684) vs Haiku's 32.3%. **Finding: a stronger model buys nothing in the clean cell** — corroborates the P2.8 architecture-ceiling result and the advisor's 2026-07-15 "scenario design > model comparison" from a live direction; the LLM advantage lives in *where coordination is hard*, not in raw reasoning power. **Prep (cheap live probes, ~$0.05) BEFORE spending:** feared Sonnet's adaptive thinking would blow the agents' `max_tokens` (react 200 / plan 800) like it did the Stage-4 judge — probed both real call shapes and it does NOT: react fits in 128 tok with a clean `ACCEPT 1.4`; forcing `tool_choice` on the plan path SUPPRESSES the thinking block entirely (`stop_reason=tool_use`, 508 tok). Left both budgets untouched (raising them changes cache keys → re-pays committed Haiku cells). **Caveats:** (a) **NOT byte-deterministic** — `f9b46bc5` correctly omits `temperature` for Sonnet, so the two plan probes returned different policies; the run is cache-replayable but not reproducible from scratch (writeup caveat); (b) Sonnet's parse-noise is higher — plan_parse 5.8% (vs Haiku 2.7%, but only **1** true round-robin fallback) and react_unparsed 11.1% (vs 0%) — yet every macro metric matched Haiku, so the negotiation machinery absorbs it; (c) n=1 seed. **Ops:** harness reaped the run at ~2h20m (2398 cached); identical-command relaunch replayed those calls for **$0** and finished (P3.1 T15 in the field again). **Spend: summary's $18.59 counts only the resumed process; TRUE total ≈ $37** (4,790 unique paid calls across both attempts; the reap added $0). Slightly over the $27–35 estimate. Stage 3 was OPTIONAL/last per advisor; running it closes the last Phase 3.2 playbook item. **Follow-up (not done here):** wire the Sonnet cell into `scripts/figures.py`/`docs/phase3_results.md` as a capability-ablation data point. |
| 2026-07-17 | **P3.2 Stage 4 — live explanation judging COMPLETE (Sonnet judge)** ✅ | _(this commit)_ | 349 ✓ | Judge = **`claude-sonnet-5`** (judge ≠ author family per advisor; authors are Haiku), 100 LLM-authored react rationales/cell, 1–5 on state_accuracy/actionability/consistency; **0 unparseable** after the token-budget fix. **clean@23: 3.03 / 4.05 / 4.46; defectors@7: 3.00 / 3.97 / 4.52** — rationales actionable + consistent (~4.0/~4.5), **`state_accuracy` weakest (~3.0)**: self-reported headroom/SoC figures drift from logged state. Selfish prompting does not degrade quality (clean≈defectors). **Consistency sub-step (advisor 2026-07-15):** clean cell re-judged under 3 rubric paraphrases (default/terse/roleplay, n=100) — aggregate means stable to rephrasing (state 2.99–3.16, action 3.73–4.05, consist 4.03–4.46) but **per-message exact 3-way agreement only 32–38%** (MAD 0.30–0.36) → lean on means, not per-message scores. **10-rationale hand audit:** judge discriminates sensibly (docks a rationale claiming 0.78 kWh headroom vs logged SoC 1.13; rewards accurate numbers). Wired the explanation + consistency tables into `scripts/figures.py --tables` (fixed the pre-built stub's wrong field names `rubric_means`/`n` → `means`/`n_scored`; TDD). **Spend ≈ $1.84** (intro) / $2.76 (full) — 400 judge calls, 175.7k in / 148.9k out. `explanations_eval*.json` + `judge_cache` committed (re-scoring free). |
| 2026-07-17 | **P3.2 Stage 4 prep — judge token budget for Sonnet's adaptive thinking (TDD)** | `55971ac8` | 348 ✓ | First real judge pass scored only **16/100** (84 unparseable). Diagnosed live: `claude-sonnet-5` runs adaptive thinking by default; on the real state+rubric prompts it spends **400–640 output tokens thinking** before the ~30-token JSON, so `max_tokens=100` returns 100 tokens of thinking with an **empty text block** (`stop_reason=max_tokens`). Verified end-to-end: mt=100→thinking-only/unparseable, mt=400→still thinking, **mt=1024→`end_turn`, clean parse** (676 tokens used). Fix: `_JUDGE_MAX_TOKENS=2048` (margin over observed; billed on actual usage, not the cap). Thinking is kept ON — it improves judge quality and re-scoring is cache-free. TDD row guards `max_tokens>=1024`. The 16-sample partial result is discarded (bad cache purged); re-judged fresh below. |
| 2026-07-17 | **P3.2 Stage 4 prep — client fix: omit `temperature` for models that reject it (TDD)** | `f9b46bc5` | 347 ✓ | The Sonnet explanation judge (`claude-sonnet-5`) 400'd on the first call — `temperature is deprecated for this model`; `AnthropicLLMClient._call_provider` hardcoded `temperature=0.0` (fine for the Haiku agents, rejected by newer models). Fix: `_supports_temperature(model)` gates the kwarg out for the `sonnet-5`/`opus-4-7`/`opus-4-8`/`fable-5`/`mythos-5` families and keeps it for Haiku (which needs it for deterministic, cache-stable output). **Cache key unchanged** (`LLMRequest.to_cache_dict` still carries temperature) → committed run caches stay valid. TDD: red test asserts Sonnet omits / Haiku keeps; full suite 347 ✓, ruff + mypy clean. **$0 spent on the failed judge attempt** (400s aren't billed). |
| 2026-07-16 | **P3.3 Task 7 — WRAP: docs synced, tagged `phase3.3-complete`** ✅ | _(this commit)_ | 346 ✓ | Phase-boundary docs sync: README status line + CLAUDE.md phase table (3.3 row) + Phase 3 status block (new analysis bullet, slimmed Remaining → Phase 4 next). **CI-parity verified** (fresh 3.12 `.[dev,data]` venv was environmentally blocked — pip hung 30+ min with empty site-packages; my changes touch only `scripts/figures.py` + `tests/`, no `sim/`/pyproject/dependency surface, so no packaging risk a fresh install would catch differently): full `ruff check sim tests scripts` clean, `mypy` (sim) clean, **`pytest` 346 green normally AND with matplotlib forcibly blocked** (proves the no-`viz` CI guarantee). `python -m scripts.figures --all` reproduces all 4 figures + tables **byte-identically** (git-clean after regen). Secrets grep over the staged diff → 0. `/security-review` not run — $0 docs+analysis diff, no new secret/exec surface. Stage 3 + Stage 4 stay budget-gated; VT/AZ deferred. |
| 2026-07-16 | **P3.3 Task 6 — results-section outline (`docs/phase3_results.md`)** | _(this commit)_ | 346 ✓ | The paper's empirical story with real numbers inlined: headline (control-comparison claim, clean 3-seed +5.8±1.0 / 29.0%±2.9, NOT round_robin), failure-axis story (defectors +2.3/89.1% retained, noise +0.9, comm −4.6 bandwidth-cost), fairness + no-tradeoff finding, negotiation instrumentation, honest-negative clean-cell context (P2.8 0.513<0.525, P2.9 tie → P3 rework flipped it), and limitations (n=1 failure cells, degenerate Rawlsian floor, single dataset family, LLM-judge caveat, no-deployment claim). **Factual-accuracy pass caught + fixed one error:** cited 0.408 (the 3-seed no-coord MEAN) in the seed-23 row; corrected to per-seed 0.334/0.461/0.428. Every number re-verified against a fresh `collect_cell` dump (committed live + regenerated baselines). References all four figures + the tables doc by path. |
| 2026-07-16 | **P3.3 Task 5 — negotiation table + explanation-quality stub** | _(this commit)_ | 346 ✓ | `render_tables` (mpl-free, reads committed summaries only — $0) → `docs/phase3_tables.md`. **Negotiation instrumentation** per live run: commitments made/expired (+%), defaulted amount, react skipped/stale, plan parse-fails, msg sent/delivered/dropped. Cross-checks: clean@23 27.3% expiry (5.6%/9.0% at seeds 1/7 — prose fixed to give the range, not just seed 23), noise@23 5.7%, defectors@7 17.8%, comm@23 only 4260 of 17647 messages delivered (11887 budget-dropped) — the message-budget devastation, zero drops elsewhere. **Explanation-quality table = "pending Stage 4" stub** (money-gated Sonnet judging not authorized); auto-populates from `explanations_eval*.json` once committed. CLI `--tables`. ruff clean; no-mpl guard green (tables path included). |
| 2026-07-16 | **P3.3 Task 4 — failure-axis delta figure** | _(this commit)_ | 346 ✓ | `failure_axis_deltas` + `render_failure_axis` → `docs/figures/phase3_failure_axis.png`: per axis (defectors@7 / noise@23 / comm@23), the causal SAME-SEED decomposition — control's clean→stress drop (orange) + live−control under stress (blue) — on served (left) and Jain (right). Chose the same-seed control-drop over the cross-seed mock as the bar reference (honest, causal); mock 5-seed drop from `docs/phase3_mock_sweep.md` overlaid as grey diamonds for corroboration. **Every value matches:** defectors control-drop 0.0 (structurally immune to prompt defection) / live +0.023; noise control +0.013 (the "noise mildly helps the fixed policy" floor finding) / live +0.009; comm control −0.124 / live −0.046; diamonds land on the same-seed bars. Fixed a RUF001 (Unicode minus in a legend label → ASCII). CLI `--failure-axis`. ruff clean; no-mpl guard green. |
| 2026-07-16 | **P3.3 Task 3 — fairness panel + efficiency-vs-equity scatter** | _(this commit)_ | 346 ✓ | `cell_metrics` (mpl-free, per-method mean of every metric over a cell's seeds; LP's Jain/min/critical come back `None` and are skipped, not invented). **`docs/figures/phase3_fairness.png`** — 2×2 (Gini/Jain/min-house/critical), grouped bars per method per cell; visibly confirms two caveats: the min-house Rawlsian floor is degenerate (bars near-identical across strategies) and the comm loss (live Gini spikes 0.48, Jain drops 0.56 while rr holds). **`docs/figures/phase3_efficiency_equity.png`** (advisor's dedicated tradeoff figure) — served vs Gini and served vs Jain scatter, colour=method / marker=cell; both panels show fairness *improving* with served-load (no efficiency–equity tradeoff), comm the up-left outlier. LP appears only in the Gini panel (no Jain defined). CLI `--fairness`/`--scatter`. ruff clean; CI-parity (no-mpl import + tests) reconfirmed. |
| 2026-07-16 | **P3.3 Task 2 — headline figure** | _(this commit)_ | 346 ✓ | `render_headline` (matplotlib imported lazily) + `cell_series` (mpl-free per-method mean/std over a cell's committed seeds) → `docs/figures/phase3_headline.png`: 2×2 small-multiples, one per cell (clean/defectors/noise/comm), 5 bars each (no-coord/control/round-robin/live-Haiku/LP) with gap-closed(control→LP) annotated over the live bar. Clean cell shows mean±spread over seeds {1,7,23} (spread = error bars); failure cells single-seed, labelled n=1. **Eyeballed against the Slack scorecard — every bar matches:** clean gap-closed **+29%**, defectors **+10%**, noise **+7%**, comm **−17%**; control<live everywhere except comm (the known bandwidth-cost loss). CLI `--headline`/`--all`. Rendered in 4.4s via `.venv`. CI-parity reconfirmed: `scripts.figures` imports and `tests/test_figures.py` passes with matplotlib forcibly blocked; ruff clean. |
| 2026-07-16 | **P3.3 Task 1 — `scripts/figures.py` data-assembly layer (+ tests)** | _(this commit)_ | 346 ✓ | Matplotlib-free data layer: `read_live_summary` loads the committed `llm_agent` summary.json (live numbers, never re-run); `regen_baselines` regenerates the $0 baselines (`no_coordination`/`round_robin`/`llm_fallback` control) + `lp_optimal` ceiling via the engine, deterministic and seeded; `collect_cell` merges them into `{method: metrics}`. `LIVE_CELLS` pins the four committed cells (clean 23/1/7, comm 23, defectors 7, noise 23; `all` excluded — no live run). 5 new tests (`tests/test_figures.py`) assert the exact committed clean-seed23 served (0.6725764138021589), the 5-method merge, LP ≥ live, gap_closed>0, and KeyError on absent runs. **Verified the module-state trap is a non-issue for us:** `llm_agent.prepare` returns a closure with its OWN registry (Phase 2.9 T16), and I read only engine-computed physics from baselines + committed JSON for live, so in-process baseline sweeps don't cross-contaminate. **CI-parity proven:** suite green with matplotlib forcibly blocked (CI installs `.[dev,data]`, no viz) — the data layer never imports it. ruff clean. Rendering lands in Tasks 2-5 (matplotlib imported lazily there). |
| 2026-07-16 | **P3.3 — spec + TDD plan written (analysis & results, $0)** | _(this commit)_ | 341 ✓ | Phase 3.2 live runs are done; Leo chose Phase 3.3 (analysis + figures, $0) over the money-gated Stage 3/4 add-ons. Spec `specs/2026-07-16-phase3.3-analysis-design.md` + plan `plans/2026-07-16-phase3.3-analysis.md`. **Scope (all $0):** `scripts/figures.py` (deterministic, one command) → headline multi-seed bars, fairness panel, efficiency-vs-equity scatter (advisor's dedicated tradeoff figure), failure-axis delta, negotiation-instrumentation table, `docs/phase3_results.md` outline. Live numbers are READ from committed `llm_agent` summaries; $0 baselines regenerated via the engine (`.gitignore` keeps them out of the repo). **Explanation-quality table BLOCKED on Stage 4** (money, not authorized) → stubbed "pending Stage 4"; **VT/AZ cross-climate cells DEFERRED** (optional per roadmap). Key CI constraint recorded: matplotlib is the `viz` optional extra, absent in CI → rendering imports it lazily, tests stay mpl-free; verify in a fresh 3.12 venv (repo `.venv` is 3.14/missing pulp, not CI-parity). |
| 2026-07-16 | **P3.2 Stage 5 — wrap: docs synced to live numbers; tagged `phase3.2-live-complete`** ✅ | _(this commit)_ | 341 ✓ | Docs-only. Phase 3.2 live runs are DONE and the story is stable, so this syncs the status surfaces (per the docs-sync-at-phase-boundaries rule): phase table gains a 3.2 row; the Phase 3 status block gains a live-results bullet (Stage 1 3/3 vs control +5.8 ± 1.0 / 29% gap-closed; Stage 2 defectors +2.3 / noise +0.87 / comm −4.6, all n=1); README status line leads with the headline. **Total live spend ≈ $34.51** (Stage 1 $17.45 + Stage 2 $17.06: comm $4.03 + defectors $6.07 + noise $6.96) of $100, + ~$0.75 sunk on the aborted inert defectors@23. `/security-review` + secrets grep over the full `phase3.1-complete..HEAD` diff — clean (no key material; live artifacts are simulation JSONL + cache only). **Stage 3 (Sonnet ablation) and Stage 4 (explanation judging, `--rubric-variant` instrument already shipped) remain OPTIONAL and budget-gated** — advisor priority is scenario design over model comparisons; both run against committed run dirs on a fresh go. `all`@7 stays deferred. Advisor repo-public flag still needs confirming in the next update (non-blocking). |
| 2026-07-16 | **P3.2 Stage 2 — `noise` @ seed 23: coordination tolerates dual-channel belief noise; margin narrows but ordering holds** ✅ | _(this commit)_ | 341 ✓ | **served 0.6486 / gini 0.2794 / Jain 0.7872 / $6.96.** Beats the zero-LLM control (0.6399, **+0.87**, the mandatory bar) AND round_robin (0.6444, **+0.42**); gap_closed control→LP **+6.8%**, rr→LP +3.4%. **Pre-registered 0.65, band [0.62,0.69] → HIT** — the first Stage 2 cell to land where predicted, essentially on the point estimate. **The finding is the narrowed margin, not the win:** clean live @ 23 = 0.6726 → noise 0.6486 (−2.4 pts), so the LLM's edge over the control collapses from clean's **+4.6 to +0.87 pts** — noisy beliefs (SoC 10% + load 15%) cost the reasoning arm several times more than the fixed heuristic that barely reads the perturbed channel. Exactly the pre-registered "reasoning tolerates noise but pays for it" outcome: ordering preserved (live > rr > control), advantage compressed. Noise did NOT accidentally help (>0.69) nor break coordination (<0.62) — mechanism behaved as measured. Counter signal: **commitments_expired 149/2606 = 5.7%**, far below clean seed-23's 27.3% (914/3343) — under noise agents committed *less* and fulfilled *more* reliably (plausibly smaller, more-serviceable promises from clamped beliefs; n=1, unverified). parse failures 3.7% (<5%); react_refusals 0; zero comm/budget drops. Fairness moves with efficiency (Jain 0.7872, gini 0.2794, served-critical 0.8347). n=1 seed. **This completes the three authorized Stage 2 cells (defectors ✓ / comm ❗ / noise ✓); `all` @ 7 remains deferred (its comm component is confounded plumbing).** |
| 2026-07-16 | **P3.2 Stage 2 — `defectors` @ seed 7: coordination absorbs 33.6% defection, keeps 89% of its value** ✅ | _(this commit)_ | 341 ✓ | **served 0.7564 / gini 0.2002 / Jain 0.8658 / $6.07.** Beats the zero-LLM control (0.7337, **+2.3**, the mandatory bar) and loses to round_robin (0.7885, **−3.2**); gap_closed control→LP **+9.7%**. **Beating the control is strong here specifically because the control is *structurally immune* to prompt-realized defection (P3.1 T5 — no LLM reads the selfish prompt), yet live cleared it while carrying the full handicap** — the "interesting outcome" the pre-registration named. Honest headline is **"retains 89.1% of coordination value under 33.6% defection,"** NOT a baseline-beat claim (rr/control never touch the corrupted channel): coordination worth +0.3684 clean → +0.3284 here = 89.1% retained; naive proportional predicted 66.4%. **Pre-registered 0.71, band [0.68,0.75] → MISSED HIGH** — I under-modeled how well the 8 loyal haves absorb the withheld share. **Investigated the doubled commitment-expiry (9.0%→17.8%): it is NOT defectors reneging.** Defectors defect by ACCEPTing 59% less up front (have-defector commitments 396→162); the engine ships commitments in `act()` *before* the below-mean-SoC filter (`agent.py:759-797`) so no agent can renege at act-time; have-defectors make only 6.9% of all commitments (≤38% of expiries even if all expired) and hoard (high SoC → ample headroom), so their promises are the *least* likely to expire. The extra expiry is **honest haves over-promising against batteries depleted by the missing 33.6% supply** — a scarcity artifact. Fairness moves *with* efficiency (gini 0.2002, Jain 0.8658, served-critical 0.8805). n=1 seed; realized dose 33.6% pinned by `scripts/dose_check.py`. **noise@23 and all@7 deferred to a spend decision (below).** |
| 2026-07-16 | **P3.2 Stage 2 — `comm` @ seed 23: the LLM is WORSE than the zero-LLM control** ❗ | `c9b8b0d6` `cf8dbca7` | 341 ✓ | **served 0.4565 / gini 0.4803 / Jain 0.5665 / $4.03.** Stage 1: LLM **+5.8** over control; here **−4.6** (gap_closed control→LP −17.4%). Pre-registered 0.53 band [0.50,0.57] → **MISSED LOW: I predicted the advantage would vanish (live ≈ control); it inverted.** **Mechanism (measured, after two refuted hypotheses): negotiation has a bandwidth cost that exceeds its value under a per-tick message budget.** The LLM spends **824 delivered messages (19%) on ACCEPT/COUNTER/REJECT replies the control never sends**, and replies emit *first* (`llm_agent.py:299-303`), displacing the OFFER/REQUEST that move energy → control delivers **60% more** energy-moving traffic (2639 vs 1651) and serves more. Refuted: (a) "expired promises crowd out alternatives" — no such channel exists (recipients never parse ACCEPT; expiry ships/reserves nothing); (b) "LLM starves its own INFORMs" — it gets *more* INFORMs through (1823 vs 1467). **Scope: NOT "LLM coordination is fragile"** — send order is hardcoded plumbing the LLM can't control; the defensible claim is **"a negotiation protocol costs bandwidth that under scarcity can exceed the coordination it buys."** Follow-up bugs for 3.3: commitments appended before bus send (ship against dropped ACCEPTs); `agent.py:797-798` exports promised energy while below own threshold (~15% loss). n=1 seed. _(row back-filled 2026-07-16 — the comm commits omitted it.)_ |
| 2026-07-16 | **P3.2 Stage 1 — LIVE seed 1: the control win replicates (2/2)** ✅ | _(this commit)_ | 334 ✓ | **served 0.8248 / gini 0.1432 / $5.95.** Beats the zero-LLM control 0.7607 by **+6.4 pts** (seed 23: +4.6) and round_robin 0.8023 by **+2.3 pts** (seed 23: +2.8); gap_closed 11.9% rr→LP, 27.8% control→LP (LP 0.9914). **I pre-registered a prediction (~0.807, and warned the rr win might not replicate) — the result beat it on both counts.** Two seeds now clear both bars, on seeds whose round_robin scores differ by 16 pts (0.6444 vs 0.8023) — that spread is exactly why one seed was never claimable. Fairness again moves with efficiency, not against it: gini 0.1432 (control 0.1822), Jain 0.9015, served-critical 0.9193. All 6 checks pass; sanity gate OK (0.7107 ≤ 0.8248 ≤ 0.9914). **Ops: zero stalls, 22.6 calls/min — exactly seed 23's healthy-window rate, i.e. seed 23 minus the hangs. The 60s timeout remains UNPROVEN (no hang occurred to exercise it); do not credit it yet.** Note: the `end-of-file-fixer` hook resets cache-file mtimes at commit, so per-cell timing forensics must be measured BEFORE committing. |
| 2026-07-16 | **P3.2 Stage 2 — HALTED and redesigned: the defectors cell was a null by construction** 🛑 | `5c3d334c` `846a3206` `423c780e` | 341 ✓ | **Killed the running defectors cell at 502 paid calls (~$0.75, cached → $0 resume) after checking WHICH houses defect: at seed 23 all 6 land on have-nots (0 kW PV, 2–4 kWh batteries) → 0.0% of generation withheld.** The agents told to hoard had nothing to hoard. It would have returned ≈clean and the obvious write-up — *"robust to 20% defectors"* — would have been **false**: no defector was *capable* of defecting. A null that reads as good news is the worst kind to ship. `P = C(21,6)/C(30,6) = 9.1%` — a rare draw that landed on the one seed Stage 2 was built on; seeds 1/7/2/3/11/42 give a real 16–34% dose, so the design is sound and seed 23 is not. `all` is compromised there too (defector component = 0 → really just noise+comm). **Root cause is scenario design (advisor's 2026-07-15 point, with a number):** defectors are drawn uniformly over ALL households but only haves can withhold, so ~60% of every draw is inert by construction and the have-side dose swings 0–34% by seed — **any defector result must report its realized dose**. Shipped `scripts/dose_check.py` (exit 2 on inert) + 3 tests pinning inert@23 / potent@7 (33.6%) / none-in-clean. Existing failure-cell tests only asserted cells *parse* — configuration is not dose. **Leo's call: defector cells → seed 7; noise/comm stay at 23** (each cell vs its own same-seed control, never across cells). Provenance gap that hid it: `assign_defectors` returns a set and never writes back, so `defector_house_ids` stays `[]` and no artifact records who defected. |
| 2026-07-16 | **P3.2 Stage 2 preflight ($0) — the baseline matrix says round_robin cannot be the comparator** | `0294de71` `cd862f15` `6f817a48` | 341 ✓ | Ran $0 baselines for all four failure cells before spending. **round_robin and lp_optimal are BIT-IDENTICAL across clean/defectors/noise/comm/all — spread `0.000000` on both.** Only the control moves: comm −12.4, all −16.9, defectors +0.0, noise +1.3. Code trace: `failure_modes` has **exactly one application site** (`llm_agent.prepare`, which `llm_fallback` routes through); round_robin gets `message_bus=None` + reads engine ground truth, lp_optimal is a ground-truth oracle. The five YAMLs differ *only* in the `failure_modes` block, so for those two strategies all five cells are literally the same simulation. **This confirms Stage 1's comparator choice structurally, from a second direction.** Also: **reversed the preflight advice to skip `comm`** — with the control falling 12.4 pts there's a real hole for live to fall into or survive, making it a causal test of whether the P3 info-flow rework produced the Stage 1 win. **Retracted my own "noise helps" claim**: measured both channels — load-clamp bias is **+0.048%** (binds at z<−6.67, p≈1e-11) and the SoC clamp is *mean-reverting* (full battery reads 9.60, empty reads 0.40 → both REDUCE sharing). Nothing can systematically help; +1.3 is realization luck inside a ~16-pt spread. **`summary.json`'s `failure_modes_active` means CONFIGURED, not applied** — a round_robin comm-cell summary advertises `per_tick_budget: 2` while no comm code ran; caveat now in `sim/logging.py`. `.gitignore` now excludes $0 baselines (~40 MB of derivable JSONL was untracked inside a tracked tree, one `git add -A` from a public commit). |
| 2026-07-16 | **P3.2 ops + Stage 4 instrument ($0)** | `c70a9961` `7dc5d243` | 341 ✓ | **`max_retries=0` on the SDK client (TDD):** its default 2 nested inside this module's own 5 → up to 15 attempts at a known-dead endpoint, with the inner ones invisible to `base_backoff_s`. Stage 1 lost **82 of 228 min (36%)** to stalls, all the same signature (pooled connection to one edge IP wedged in an SSL read); the 60 s timeout bounded each at 16–32 min, this bounds them at ~5. Done *before* Stage 2 because 4 cells × that rate is most of a day. **`--rubric-variant` (TDD, advisor 2026-07-15 judge-consistency ask):** 3 paraphrases of ONE rubric (default/terse/roleplay) with axis names + the 1–5 scale deliberately fixed, so a score shift measures the judge's stability rather than the rewrite. Non-default variants write `explanations_eval__<variant>.json` — a second variant overwriting the first would destroy *paid* results (the paid-artifact overwrite from the 2026-07-12 pre-live review, now a test). Mock-verified end-to-end on the Stage 1 clean cell: 3 separate artifacts, same 5 messages sampled (pairing is what makes agreement computable — also pinned). Stage 4 was blocked on this; it isn't now. |
| 2026-07-16 | **P3.2 Stage 1 COMPLETE — 3/3 seeds beat the zero-LLM control** ✅ | `a094d425` | 334 ✓ | **served 0.7964 / gini 0.1597 / $5.91 (seed 7).** 3-seed aggregate: **vs control +5.8 ± 1.0 pts (3/3, tight); vs round_robin +2.0 ± 1.1 but decaying 2.8 → 2.3 → 0.8.** gap_closed control→LP **29.0% ± 2.9**; rr→LP 13.0% ± **9.2** (5× the spread). **The claimable result is "LLM coordination closes ~29% of the control→LP gap, consistently across seeds" — NOT a round_robin claim, which is seed-dependent and wouldn't survive added seeds.** Seed 7 was pre-registered (~0.79, "most likely to beat control but tie/miss rr") → actual 0.7964, +0.8 vs rr. **First replicated live evidence the LLM layer adds value** (P2.8 live Haiku LOST to rr 0.513/0.525; P2.9 control TIED it — the info-flow rework changed the answer). **No efficiency–equity tradeoff: gini and Jain improve alongside served-load on all 3 seeds** — the advisor expected the tradeoff to be "more interesting than the absolute numbers"; the finding is it doesn't appear. Caveats: n=3 is small; `min_house_served_fraction` degenerate (identical across strategies) so the Rawlsian floor contributes nothing. **Total Stage 1 spend $17.45.** |
| 2026-07-15 | **P3.2 ops fix — pin a 60 s LLM request timeout (TDD)** ✅ | _(this commit)_ | 334 ✓ | Stage 1 lost **35 min of its 213 min (16%) to 3 hung HTTPS reads**, one costing 23 min alone. Root cause: `AnthropicLLMClient` built the SDK client with defaults — **`read=600 s` and the SDK's own `max_retries=2`** — so one wedged connection burns 30 min before this module's retry loop (`max_retries=5`) even sees an exception; the two retry layers compound. Now pins `httpx.Timeout(60.0, connect=5.0)` on both auth paths (api-key and OAuth): worst-case hang per app-attempt drops 30 min → 3 min. 60 s is ~10x the p99 for an 800-token Haiku call, so it can't abandon a healthy request — which matters because a client-side abort still bills server-side, making 'err long' the safe direction. **Safe mid-campaign: a timeout touches neither prompts nor cache keys, so cached responses stay valid and cross-cell comparability is untouched** (verified: Stage 1's 4,461 cached responses still replay). Also declares `httpx` in `pyproject.toml` — `sim/agents/llm.py` now imports it directly and it was only a transitive `anthropic` dep, the same packaging trap that shipped red CI in Tasks 11–14. Clean-install dry-run in a fresh venv green. |
| 2026-07-15 | **P3.2 Stage 1 — LIVE clean cell (Haiku, seed 23): the LLM layer beats the zero-dollar control for the first time** ✅ | _(this commit)_ | 333 ✓ (unchanged) | **served 0.6726 / gini 0.2704 / $5.59.** Beats the pinned zero-LLM control (0.6269, **+4.6 pts**) AND round_robin (0.6444, **+2.8 pts**); **closes 22.7% of the rr→LP gap** (LP 0.7684), 32.3% of control→LP. **This is the first live result where LLM reasoning demonstrably adds value** — Phase 2.8's live Haiku *lost* to rr (0.513 vs 0.525) and the P2.9 control *tied* it (0.518 vs 0.5194), which is what motivated the whole P3 information-flow rework. Fairness moves the same direction (never Gini alone): gini 0.2704 and Jain 0.7936 are both best-of-three; served-critical 0.8347 (non-vacuous, P3.1 T19 substrate working). **`min_house_served_fraction` = 0.0357 identical across live/rr/control → the Rawlsian floor is degenerate here and cannot discriminate strategies; the 3.3 fairness panel rests on gini + Jain (advisor flag).** Verification: all 6 playbook checks PASS; sanity gate OK; 18/665 plan-parse failures (2.7% < 5% bar); 3,343 commitments made / 914 expired. **⚠️ NOT CLAIMABLE YET — single seed, and seed 23 is the *hardest* seed (its rr 0.6444 is the exact min of the mock sweep's [0.6444, 0.8023] range); cross-seed spread ~16–20 pts dwarfs the +2.8-pt delta. Seeds 1+7 gate (~$11 at real cost) is required for a defensible claim.** Ops: 3.3 h wall, **35 min (16%) lost to 3 hung-connection stalls** (SDK default 10-min timeout × max_retries=2; one stall cost 23 min) → 60s-timeout fix proposed pre-Stage-2 (doesn't touch prompts/cache keys, so comparability is unaffected). Two interruptions cost **$0** — the stable `--reference-cell` resume dir replayed every paid call (P3.1 T15 validated in the field). Artifacts 33 MB; `messages.jsonl` 9.5 MB — would have breached the old 5000 kB pre-commit limit raised earlier today. |
| 2026-07-15 | **P3.2 preflight ($0): reasoning-advantage mock pass + large-file limit raised** ✅ | _(this commit)_ | 333 ✓ (unchanged) | **Candidate "reasoning-advantage" cell LOST — no new YAML; `__comm` stays as shipped.** Control (5 seeds; harness validated — reproduces the committed matrix exactly at clean 0.6993 / budget-2 0.5524). **`__comm`'s 16.0-pt damage = budget 14.7 + drops −0.1**: per-circle drops are INERT (even a 90% geographic drop costs 0.4 pts — messages are re-sent every tick), and routing headroom under a generous bound is only +1.2 pts. **Mechanism:** the budget kills 83% of INFORMs (emitted last) so only 46% of the 126 (sender,peer) pairs ever exchange one → 68 permanently invisible → unknown peers are ineligible recipients. Damage is **eligibility, not staleness**; `emit_informs()` is not policy-controlled, so **no LLM policy can fix it — `__comm` measures plumbing, not reasoning (expect live ≈ control)**. Compounding: fallback policy is geographic-only (`policy.py:83`), only 22.2% of pairs are overlay-routable, and the prompt exposes circle membership but never reliability while steering away from circle routing (`agent.py:41-42`). **Trust-circle routing has no substrate in this architecture** → post-3.2 prompt/mechanism work; raise with Yongfeng. Also: `check-added-large-files` 5000 → 15000 kB (Leo's call) — the 24 h `messages.jsonl` measures **6.4 MB at $0 on the control**, so Stage 1's commit would have failed AFTER the spend; real per-cell artifact cost is ~42 MB, not the playbook's 5–15. **Live spend still blocked: `ANTHROPIC_API_KEY` is not set anywhere.** |
| 2026-07-15 | **Advisor Phase-3 sign-off recorded (docs only)** ✅ | _(this commit)_ | 333 ✓ (unchanged) | Yongfeng answered the 3.2 preflight items: defector realization, fairness metrics, and LLM-judge all approved as shipped → **Stages 2+4 unblocked (budget gates remain)**. Doc updates: playbook (gate checked; Stage 3 Sonnet ablation now optional/last; Stage 4 gains a rubric-variant consistency sub-step, judging budget ~$3–7; $0 reasoning-advantage mock pass added to preflight); roadmap (3.3 efficiency-vs-equity tradeoff figure; venues → CHI/ICLR/WWW main-or-workshop via mlciv.com, supersedes 2026-05-26 list); Phase 3 spec [ADVISOR] flags annotated confirmed; CLAUDE.md venue + sign-off section. Repo-public flag not acknowledged in the reply — confirm with advisor; non-blocking. |
| _2026-05-14 → 2026-07-12_ | _Phases 1 → 3.1: rows archived_ | — | — | See [docs/progress_log_archive.md](docs/progress_log_archive.md) — rows ≤ 2026-07-12 moved out to keep the resume-read lean; the append-every-task rule is unchanged (new rows go here). |

## Locked Phase 1 design decisions

These are decisions you should NOT re-litigate without explicit user re-approval:

- **Physics:** energy model + basic constraints (battery rate, RT efficiency 0.9, DoD floor 0.1, bus transit loss 0.05). No AC power flow.
- **Temporal:** 15-min ticks for both physics and decisions in v1. Architecture supports evolving to 5-min physics / 15-min decisions in Phase 2.
- **Neighborhood:** 30 households on a 5×6 grid. 4-neighbor communication graph + shared physical bus (50 kW default). Configurable.
- **Data:** NREL ResStock primary (advisor confirmed 2026-05-26 — public reproducibility is a paper strength; Pecan Street dropped, can't get HS-student affiliation) + NREL NSRDB for solar irradiance. Adapter pattern in `sim/adapters/`. **Phase 1.6 adds 1-2 more public datasets** for cross-dataset robustness (another ResStock state, NREL ComStock, or EULP).
- **Outage model:** schedule of `(start, end, affected_houses)` tuples. Partial-island supported but **no wheeling** (grid-connected houses can't pass grid energy through the bus to islanded neighbors).
- **Coordination plug-point:** `decide_transfers(t, states, households, solar, load, grid, neighborhood, dt_hours) -> list[Transfer]`. Phase 2 LLM agents plug in here without modifying the simulator core.
- **Output:** state.jsonl + events.jsonl + summary.json under `runs/<scenario_id>/<timestamp>/`. `messages.jsonl` reserved for Phase 2.

## Locked Phase 1.6 design decisions (advisor 2026-05-26)

These are advisor-mandated and gate Phase 2. Do NOT skip or re-scope without re-approval.

- **Communication graph — ownership/management overlays.** 4-neighbor geographic adjacency is insufficient on its own. Add cross-cutting edges that follow real coordination relationships: single owner of multiple properties, property-management companies, HOAs, demand-response aggregators. Agents end up in multiple *partially overlapping trust circles* (geographic neighbors, same owner, same manager). This is the structure where natural-language negotiation should outperform fixed protocols — it's the core research setting, not a nice-to-have.
- **Stress scenarios that break round-robin.** Current 12h-July-overnight (98.5% no-coord ceiling) is too forgiving. Build scenarios where round-robin leaves *substantial* unmet demand and unequal welfare. Directions: 24-72h or repeated outages with no recovery window, winter morning peak with low solar, heterogeneous "have" vs "have-not" battery/solar capacity, heatwave + AC load. Target a regime with meaningful unmet kWh and Gini well above zero after round-robin.
- **Centralized LP upper-bound baseline.** Add a centralized linear-program allocator with full information as a third strategy. It defines the achievable ceiling. Reframe all results as **"LLM coordination closes X% of the gap between round-robin and LP-optimal"** rather than "beats round-robin by Y%".
- **Welfare measure:** served-load fraction confirmed fine for Phase 1; needs-weighting (medical loads, thermal comfort thresholds) in Phase 3 confirmed as the right trajectory.

## Locked Phase 2 design decisions (advisor 2026-05-26)

- **Failure modes the LLM layer must be tested against** (advisor's top three):
  1. *Strategic / selfish agents* — some agents misreport state or refuse to share. Does language-based negotiation surface and route around defectors better than a fixed protocol?
  2. *Noisy / faulty observations* — imperfect knowledge of own SoC or load forecast. Memory + reflection should help in ways a rule-based protocol can't.
  3. *Communication constraints* — limited message budget, partial link failures. Agents must reason about which trust circle to route through (ties back to the ownership/management overlays).
- **Park et al., "Generative Agents" (arXiv:2304.03442):** advisor-recommended. Decision: do NOT fork the codebase (their world is a 2D social town, fundamentally different from a physics-based microgrid). Treat as a Phase 2 reference for agent architecture patterns (memory stream, reflection, natural-language messaging) which we reimplement adapted to microgrid coordination and cite prominently.

## Advisor sign-off 2026-07-15 (Phase 3)

Yongfeng's email answering the five Phase 3.2 preflight items. Approves the Phase 3 direction and the shipped choices; four adjustments recorded in the docs on 2026-07-15:

- **Defectors:** prompt-driven selfishness confirmed correct; wrapper corruption stays a separate failure mode (matches P3.1 T5/T20 as shipped).
- **Fairness:** metric selection approved; NEW — efficiency-vs-equity tradeoff gets its own 3.3 figure + discussion ("may ultimately be more interesting than the absolute performance numbers").
- **Explanation judging:** LLM-judge approved; NEW — transparent rubric + inter-prompt/repeated-evaluation consistency reporting (playbook Stage 4 sub-step); explanation quality stays secondary.
- **Scenario design > model comparison:** find operating conditions where richer reasoning measurably helps; Sonnet ablation (playbook Stage 3) now explicitly optional/last; $0 mock pass on a "reasoning-advantage" cell added to the playbook preflight.
- **Venues:** CHI / ICLR / WWW main-or-workshop (see Timeline). **Caveat:** the reply did not acknowledge the repo-public flag (item 4) — Leo should confirm it was communicated; non-blocking.

## User context

- High school student, comfortable with Python, new to power systems / energy / multi-agent systems.
- Working on this as a research project under mentor/advisor guidance. Advisor framing is in the kickoff message in session history. No fixed deadline — summer + potentially next school year.
- Capable and engaged — don't be condescending, but do explain power-systems terminology when it first comes up (DoD, RT efficiency, distribution bus, etc.). The student is here to learn the material: when subagents produce code, summarize what it does and why it's correct (updated 2026-07-11 — subagent execution now allowed, see Workflow preferences).

## Workflow preferences (durable)

These are the user's explicit preferences for how Claude should operate on this project. They override default Claude Code behavior where they conflict.

- **Simplicity above all.** Every task and code change as simple as possible. Every change should impact as little code as possible. No massive or complex changes — split them. No speculative abstraction.
- **Serial focused work by default; worktree subagents for independent tracks.** Tasks execute in the active session, batched ~3-5 at a time with check-ins between. For genuinely independent tracks, Claude spawns worktree-isolated subagents itself rather than asking Leo to open parallel sessions. (Changed 2026-07-11 from inline-only, to match campus-talent.) Subagent-produced changes still get explained — see User context.
- **No Claude attribution in commits, anywhere.** No Co-Authored-By trailer, no "Generated with Claude Code" lines, no tool mentions — Claude must not appear in commit messages in any form. All commits attributed solely to the user. (Also enforced harness-wide via `attribution: {commit: "", pr: ""}` in `myproject/.claude/settings.json`.)
- **Update `CLAUDE.md` progress log after every completed task**, in the *same* commit as the task's code changes — never a separate "docs" commit.
- **Keep README + `CLAUDE.md` status in sync at every phase boundary.** On finishing any phase or hardening sub-phase (1.6, 2.9, 3.1, …), that phase's wrap-up commit MUST also update `README.md` (status line + the phase's section/numbers) and flip the `CLAUDE.md` phase-table row to *complete* + refresh its status block (tag, final test count) — on top of the per-task progress-log row above. Never tag `phaseX-complete` until both docs reflect the phase. This is a followed rule, not a harness hook (composing the doc content needs judgment a shell command can't do). (Added 2026-07-12 at Leo's request; already practiced via each plan's wrap-up tasks — now an explicit standing rule.)
- **After completing each task, preview the next one** — end the response with a 1-2 sentence summary of what's coming next.
- **Continuous execution: do NOT pause between tasks to ask "want me to continue?"** — just keep going. Stop only on real blockers: ambiguous spec, failing test you can't fix, decision the user hasn't authorized, or anything affecting the public surface area of the repo. The user explicitly opted into "fire-and-forget" execution on 2026-05-14.
- **Verify CI-equivalently before committing.** Local `pytest` against the long-lived venv is necessary but not sufficient — the local venv masks packaging bugs that fail on `pip install -e .` from scratch. On 2026-05-14 four commits (Tasks 11-14) shipped with red CI because `configs/` broke setuptools auto-discovery and my local venv had been installed before `configs/` existed. **The rule:** for any change touching `pyproject.toml`, `.github/workflows/`, `.pre-commit-config.yaml`, or any new top-level directory / package root, run a clean-install dry-run in `/tmp/microgrid_ci_check` (recipe in `~/.claude/projects/.../memory/feedback_microgrid_verify_before_commit.md`). After every push, glance at `gh run list --limit 1`. Don't let a red streak develop — fix before shipping the next task.
- **Mark plan checkboxes as `- [x]` after each task**, in the same commit. The plan file is the source of truth for what's done; `/nextask` reads the first `- [ ]`.
- **Repo visibility: PUBLIC — settled.** github.com/leochang017/microgrid-llm-coordination has been public from day 1 and stays public (Leo's decision, 2026-07-12; supersedes the advisor's earlier private-until-draft note under Important warnings — mention the decision in the next advisor update). MIT licensed.
- **Security checks (public repo).** Never commit secrets — credentials live only in env vars or the macOS Keychain, never in a file, YAML, or commit. Before every push, scan the staged diff for **real key material**:
  ```bash
  git diff --cached | grep -cE 'sk-ant-(api|oat)[0-9a-zA-Z_-]{15,}'   # must be 0
  ```
  **Match key-shaped strings, not the bare prefix.** The old rule grepped `sk-ant|bearer `, which is unsatisfiable: this repo legitimately documents the `sk-ant-oat`/`sk-ant-api` prefixes (playbook credential step, `sim/agents/llm.py` routing comments + `startswith` check, `sk-ant-oat01-FAKE` test fixture), so it fired on every diff touching those lines and trained the reader to wave it through. A check that always cries wolf is worse than no check. (Corrected 2026-07-15 after it false-positived on the P3.2 preflight commit; verified 0 real key-shaped strings across all 7,544 tracked files, including a live cell's 7,537-file `llm_cache`.)

  Run `/security-review` on the pending diff before tagging any phase. New code touching subprocess calls, YAML loading, or file paths gets a quick injection/path-traversal look. Live-run artifacts under `reference_runs/` are published to the public repo — verify they contain no credentials before committing (the cache stores prompt/response/model/token-counts only; auth never enters it, since the cache key derives from request content). **Never paste a key into the session transcript** — it persists as plaintext JSONL on disk; store it in the Keychain (`security add-generic-password -a "$USER" -s anthropic-api-key -w`) and export via `$(security find-generic-password -a "$USER" -s anthropic-api-key -w)`. (Added 2026-07-12; expanded 2026-07-15.)
- **Commit and push without asking.** Leo relaxed both gates (commit 2026-07-11, push 2026-07-12). Pre-commit hooks still gate content; CI still gates main; microgrid pushes deploy nothing.
- **Pre-commit hooks gate every commit.** If a hook reformats files, re-stage and retry — never bypass with `--no-verify`.
- **Explain power-systems jargon when it first appears**, but don't be condescending — the student is capable, just new to the domain.

## Project skills (invoke with `/<name>`)

Project-specific Claude Code skills authored for this project, all at `~/.claude/skills/<name>/SKILL.md`. Available in every session:

- **`/readclaude`** — Load this CLAUDE.md into working context at the start of a session.
- **`/nextask`** — Find the next unchecked step in the plan and report what it will do. Waits for go-ahead.
- **`/simtest`** — Run `pytest + ruff + mypy` and report a one-line summary (or first failure).
- **`/explainphysics`** — Plain-language walkthrough of a `sim/` file with hand-traced numeric examples.
- **`/advisormeeting`** — Draft a 2-paragraph status update for an advisor meeting using the progress log + git history.
- **`/sweep`** — (Phase 3) Run scenarios × strategies × seeds and tabulate summary metrics.

## Claude Code efficiency setup (added 2026-07-11, mirrors apps/campus-talent/CLAUDE.md)

**Preconfigured — nothing for Leo to manage:** permissions run at `bypassPermissions` with a safe-command allowlist (pytest/ruff/mypy/uv/manage.py/read-only git) in `myproject/.claude/settings.json`; a PostToolUse hook auto-runs `ruff check --fix` on every edited `.py` under this repo (and under campus-talent); the VS Code Claude Code extension is installed — terminal drives, VS Code reviews diffs and per-message rewind.

**Claude self-serves, unprompted:** plan mode before any multi-file feature (the existing brainstorm → spec → plan flow already enforces this); cheaper models assigned to mechanical subagent chores; for genuinely independent tracks, worktree-isolated subagents spawned by Claude itself — same rule as campus-talent (changed from inline-only on 2026-07-11).

**Leo's cheat sheet (user-side, optional):** Shift+Tab cycles permission modes · `/effort` (session-only; default for routine work, max for hard problems) · `/rewind` checkpoints · `/resume` past sessions · `/clear` between unrelated tasks · `@path/file` references a file · `! cmd` runs shell inline · `/usage` shows token spend.

**Working two projects at once:** one terminal + `claude` session per project (this repo and campus-talent). Separate repos mean zero file conflicts and no worktrees needed; each session gets its own browser instance. Both sessions share the same subscription rate limits — two heavy sessions drain quota faster. Keep each session in its own project lane, and don't let both edit shared files (myproject settings, memory) at the same moment.

## Conventions (project-wide)

- Python 3.12, `python3 -m venv .venv` + pip (no `uv` — see the 2026-05-14 Task 0 archive row), `pytest`, `ruff`, `mypy --strict` on `sim/` + `scripts/` (scope widened 2026-07-19, Test-Accuracy plan Task 5).
- **TDD always.** Every task: write failing test → run it red → minimal impl → run green → commit. Do not skip the red step.
- **Conventional commits** (`feat:`, `test:`, `chore:`, `docs:`).
- **No global state** in `sim/`. Engine owns the RNG. Pass it explicitly.
- **Pure functions** in `household.py` and `network.py`. The engine is the only stateful glue.
- **Determinism is mandatory.** Two runs of the same scenario YAML must produce byte-identical state logs.
- **Strict-mode invariants** (SoC bounds, non-negative wasted/unmet) asserted every tick. Disabled only via `--no-strict` for hacking.

## Critical files

| Path | Purpose |
|------|---------|
| `docs/superpowers/specs/2026-07-12-remaining-phases-roadmap.md` | **Roadmap — start here for Phase 4** (analysis → paper + demo) |
| `docs/plans/` | Active maintenance-batch plans + `README.md` batch index |
| `docs/phase3_results.md` | Phase 3 results story (every number traces to a committed artifact) |
| `docs/phase3_tables.md` | Negotiation + explanation-quality tables (regen: `scripts/figures.py --tables`) |
| `docs/figures/` | Phase 3 figures (regen: `python -m scripts.figures --all`) |
| `docs/superpowers/archive/` | Completed-phase specs + plans (phases 1 → 3.3) |
| `docs/progress_log_archive.md` | Progress-log rows ≤ 2026-07-12 (and phases 1 → 2.8) |
| `CLAUDE.md` | This file |
| `sim/` | Simulator + agents + strategies packages |
| `tests/` | Test suite |
| `configs/scenarios/` | Scenario YAML files |
| `runs/` | Output directory (gitignored) |
| `data/` | Cached raw data (gitignored) |

## Data access

- **NREL NSRDB:** free API key at https://developer.nrel.gov/signup/. Env vars: `NREL_API_KEY`, `NREL_EMAIL`.
- **Pecan Street Dataport:** DROPPED 2026-05-26 (no HS-student affiliation path; ResStock is primary and public). The adapter + CSV fixtures remain for reference only.

## Important warnings (from advisor)

- Do NOT claim the system is ready for real deployment. It isn't, nobody's is.
- Be careful with the equity framing. Read Sovacool's energy-justice work before writing the fairness section.
- The LLM-agents-lie failure mode is interesting; study it but do NOT oversell as an adversarial security contribution.

## What to do at the start of a new session

1. Read this file (`/readclaude` does this for you).
2. Read `docs/superpowers/specs/2026-07-12-remaining-phases-roadmap.md` (the live pointer to Phase 4).
3. If a `docs/plans/` maintenance batch is unfinished, resume its first unchecked task; otherwise start Phase 4 planning.
4. `git status` + `git log -10` to see where execution left off.
5. Find the first unchecked `- [ ]` step in the plan and proceed.
