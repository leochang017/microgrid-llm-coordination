# Execution prompt — Phase 3.2 live runs (for a fresh Claude session)

> Leo pastes this (or `Read docs/2026-07-15-opus-phase3.2-execution-prompt.md and follow it`) into a fresh session to execute Phase 3.2. Written 2026-07-15 at commit `a46442b`.

## Who you are working for and what this is

You are executing **Phase 3.2 (live LLM runs)** of the microgrid research project at `/Users/leochang/myproject/academic/microgrid`. This phase spends **real money** on Anthropic API calls and produces the paper's headline results. Everything is already built and hardened; your job is disciplined operation, verification, and record-keeping — not new architecture.

## Read these first, in order (do not skip)

1. `CLAUDE.md` — authoritative project context: research question, phase status, **Workflow preferences (durable)** section (binding rules), and the new **"Advisor sign-off 2026-07-15 (Phase 3)"** section.
2. `docs/superpowers/plans/2026-07-12-phase3.2-live-runs.md` — **the active plan.** Launch pattern, budget table, per-stage checkboxes, post-run verification block, failure playbook. Execute it top-to-bottom from the first unchecked `- [ ]`.
3. `docs/superpowers/specs/2026-07-12-remaining-phases-roadmap.md` — where 3.2 fits; what Phase 3.3 needs from your outputs.
4. `git log -5 --oneline` + `git status` — confirm you're at/past `a46442b` on a clean `main` with CI green.

## Current state (2026-07-15)

- Tag `phase3.1-complete` exists; 333 tests; ruff + mypy --strict clean; CI green.
- **Advisor sign-off received 2026-07-15** — the playbook's advisor [GATE] is already checked. Stages 2 (failure cells) and 4 (judging) are unblocked. Defector cells use `prompt` realization (advisor-confirmed). Sonnet ablation (Stage 3) is **optional/lowest priority** — run last or skip.
- Zero-LLM control (`llm_fallback`) and round_robin baselines are $0 and pinned by golden tests; every live result is compared against them (that comparison IS the paper question).
- No live run has happened yet under the new physics. The run-log table in the playbook is empty — you fill it.

## Hard rules (violating any of these is a stop-the-line failure)

1. **Money gates:** every `- [ ]` marked **[GATE]** in the playbook requires Leo's explicit "go" **in your session** before proceeding. Never infer approval. The first gate is Stage 1 (clean cell seed 23, ~$9–12). $0 work (preflight checks, mock sweeps, baselines, analysis) needs no gate.
2. **One live cell at a time.** No parallel live runs, ever.
3. **Stable resume dirs:** always launch live runs with `--reference-cell <name> --seed <N>`, never a bare timestamped out-dir. On crash/429: rerun the IDENTICAL command (cache replays paid calls at $0). See the playbook's failure playbook before improvising.
4. **Post-run verification block after EVERY cell** (it's in the playbook), then commit that cell + a `CLAUDE.md` progress-log row in the same commit, before starting the next cell. Record served/gini/cost in the playbook's run-log table too, and tick the checkbox.
5. **Public repo security:** before every push, `git diff --cached | grep -icE 'sk-ant|bearer '` must be 0. `ANTHROPIC_API_KEY` lives in the shell env only. Verify committed `reference_runs/` artifacts contain no credentials.
6. **No Claude attribution in commits, in any form.** Conventional commits (`feat:`/`fix:`/`docs:`/`chore:`).
7. **Numbers sanity:** if a live cell's served < control − 0.05 or > LP, STOP spending and follow the playbook's "numbers look insane" procedure. Do not rationalize and continue.
8. **Any code change is TDD** (red test → minimal impl → green), e.g. the Stage 4 `--rubric-variant` flag for `scripts/eval_explanations.py`. Run `/simtest` (pytest + ruff + mypy --strict) before committing code. Docs/artifact commits don't need the suite.
9. **Don't re-litigate locked decisions** (CLAUDE.md's locked-decision sections + the 3.1 spec). Simplicity above all: smallest possible changes.

## Execution order

1. **Remaining preflight ($0):** suite green + clean git; credential check; crash-resume plumbing check (`_reference_cache_dir` must not return None); disk/large-file hygiene note; **the reasoning-advantage mock pass** (advisor 2026-07-15): one mock sweep over a comm-scarce + trust-circle-routing candidate cell vs the current `__comm` cell — report the comparison to Leo, commit nothing unless it wins.
2. **[GATE → Leo] Stage 1:** clean cell, Haiku, seed 23. Then the paired $0 comparison vs round_robin + llm_fallback. Report; [GATE] seeds 1 and 7.
3. **Stage 2:** failure cells `defectors` / `noise` / `comm` / `all`, seed 23 each, verification + $0 baselines per cell. [GATE] replicate seeds only where there's a live-vs-control delta worth error bars.
4. **Stage 4:** live explanation judging (Sonnet judge, ~200 samples) + the **consistency sub-step** (TDD `--rubric-variant`, ~30-message subsample, 2–3 rubric paraphrases, report per-axis agreement). Hand-audit 10 rationales.
5. **Stage 3 (optional, last, [GATE]):** Sonnet clean-cell ablation — only if Leo wants it after seeing failure-cell results.
6. **Stage 5 wrap:** README + CLAUDE.md status sync (mandatory at phase boundary), `/security-review`, tag `phase3.2-live-complete`, push, CI green, total-spend log row.

## Working style (from CLAUDE.md — binding)

- Continuous execution between $0 tasks; never pause to ask "continue?" — but money gates always stop for Leo.
- During long runs (each live cell takes 45–90 min wall), post progress to the Slack main channel at cell boundaries and check it for Leo's replies/approvals — Leo may approve the next gate there.
- After each completed task, end your message with a 1–2 sentence preview of the next task.
- Explain what results mean in plain language — Leo is learning the domain; define jargon on first use.
- Expected outputs Leo cares about: does live Haiku beat the 0-dollar control at all, per cell? How much of the control→LP gap closes? Fairness metrics alongside served-load, never Gini alone.

## What NOT to do

- No prompt-engineering iterations on the agent prompts mid-campaign (any prompt/YAML change invalidates cross-cell comparability — see the playbook's mid-campaign rule; prompt work is a post-3.2 advisor decision).
- No new metrics, mechanisms, or scenario redesigns beyond the preflight mock pass.
- No parallel subagents for live runs; no browser tools needed here at all.
- Don't touch `reference_runs/` from Phase 2.8 (old-physics; kept for provenance).
