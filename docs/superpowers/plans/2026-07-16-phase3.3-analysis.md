# Phase 3.3 — Analysis & results (TDD plan)

**Spec:** `docs/superpowers/specs/2026-07-16-phase3.3-analysis-design.md` · **Gate:** `phase3.2-live-complete` (met) · **Cost:** $0 (no API calls).

**Working rules:** smallest-possible diffs; one commit per task (Conventional Commits, no Claude attribution); append a Progress-log row to `CLAUDE.md` in the SAME commit as the task's artifacts; verify CI-equivalently (fresh 3.12 venv, `.[dev,data]`, `ruff check sim tests scripts` + `pytest`) before each commit; the repo `.venv` (3.14, missing pulp) is not trusted. Interpreter for CI-parity work: a fresh venv built in the scratchpad.

**Data-assembly must stay matplotlib-free** (CI has no `viz` extra). Rendering imports `matplotlib` lazily. Tests only ever import the data-assembly functions.

---

## Task 0 — CI-parity venv (once, no commit)

1. Build a fresh Python 3.12 venv in the scratchpad; `pip install -e ".[dev,data,viz]"` (viz so figures render locally; CI still tests without it).
2. Confirm baseline: `ruff check sim tests scripts` clean, `pytest` green, record the pass count.
3. **Verify:** all three green. This is the interpreter every later "verify" step uses.

## Task 1 — `scripts/figures.py` data-assembly layer (+ tests)

1. New `scripts/figures.py`. Add a matplotlib-free data layer:
   - `LIVE_CELLS`: mapping of cell dir → {seed → live run path}, the four committed cells (clean 23/1/7, comm 23, defectors 7, noise 23). `all` excluded (no live run).
   - `read_live_summary(cell, seed) -> dict` — loads the committed `llm_agent/.../summary.json`.
   - `regen_baselines(scenario_path, seed) -> dict[str, dict]` — thin reuse of `scripts.compare._collect` for `no_coordination, round_robin, llm_fallback` + `lp_optimal` ceiling. (Import from compare; do not duplicate the LP math.)
   - `collect_cell(cell, seed) -> dict[str, dict]` — merges regenerated baselines + read live summary into one `{method: metrics}` record with the full metric set (served, gini, jain, min_house, critical, and the negotiation counters for the live method).
   - Reuse `scripts.compare.gap_closed` for annotations (do not re-derive).
2. New `tests/test_figures.py` (matplotlib-free): assert `read_live_summary("haves_havenots_solar__llm", 23)["served_load_fraction"]` == the committed 0.6725764138021589 (exact); assert `collect_cell` returns all five methods for the clean cell with `gap_closed(control→LP)` matching a hand-computed value; assert a cell with no live run for a seed raises a clear error.
3. **Verify:** `pytest tests/test_figures.py` green in the CI-parity venv; `ruff check scripts` clean.
4. **Commit** (+ Progress-log row).

## Task 2 — Headline figure

1. In `figures.py`, add `render_headline(out_dir)` (matplotlib imported lazily): grouped bars per cell, methods in canonical order `no_coordination / llm_fallback / round_robin / llm_agent / lp_optimal`; clean cell shows mean±spread over seeds {23,1,7} (spread = error bars); failure cells single-seed, annotated `n=1`; gap-closed(control→LP) printed over the live bar. Writes `docs/figures/phase3_headline.png`.
2. Add a CLI: `python -m scripts.figures --headline` (and `--all`). Deterministic (fresh Figure, explicit close).
3. **Verify:** run it in the CI-parity venv; PNG is produced; eyeball that the clean bars match the Slack-reported live numbers (0.6726/0.8248/0.7964). `ruff check scripts` clean; `pytest` still green.
4. **Commit** (figure + code + Progress row).

## Task 3 — Fairness panel + efficiency-vs-equity scatter

1. `render_fairness_panel(out_dir)` — small-multiples of Gini, `min_house_served_fraction`, Jain, `served_critical_load_fraction`, per method per cell → `docs/figures/phase3_fairness.png`. Gini never alone (it's one panel of four).
2. `render_efficiency_equity(out_dir)` — scatter of served-load (x) vs fairness (y: one panel Gini, one panel Jain), one marker per method per cell → `docs/figures/phase3_efficiency_equity.png`. This is the advisor's dedicated tradeoff figure.
3. Wire both into `--all`.
4. **Verify:** both PNGs render; spot-check the clean-cell point shows Gini/Jain improving alongside served (the "no tradeoff here" finding). ruff + pytest green.
5. **Commit** (+ Progress row).

## Task 4 — Failure-axis delta figure

1. `render_failure_axis(out_dir)` — per axis (defectors@7 / noise@23 / comm@23): live-minus-control Δserved and Δfairness bars, with the mock floor from `docs/phase3_mock_sweep.md` drawn as a reference line/marker. → `docs/figures/phase3_failure_axis.png`.
2. Read the mock floor values from `docs/phase3_mock_sweep.md` (parse or hardcode with a cited comment — pick whichever is simpler and less brittle; a hardcoded constant with the source line cited is acceptable per repo style).
3. **Verify:** PNG renders; deltas match the Slack scorecard (defectors +2.3, noise +0.87, comm −4.6 vs control). ruff + pytest green.
4. **Commit** (+ Progress row).

## Task 5 — Negotiation table + explanation-quality stub

1. `render_tables(out_dir)` writes `docs/phase3_tables.md`:
   - **Negotiation instrumentation table** — per live cell: commitments_made, commitments_expired (and %), react_amount_defaulted, react_skipped, react_dropped_stale, message sent/delivered/dropped. All from `llm_call_counts_detailed` + `message_counts`.
   - **Explanation-quality table — stub:** if any `explanations_eval*.json` exists under the run dirs, render rubric means; else emit a single "pending Stage 4 (money-gated judging not yet authorized)" row. No spend.
2. **Verify:** markdown renders; numbers match committed summaries (e.g. clean seed23 commitments_made 3343 / expired 914 = 27.3%). ruff + pytest green.
3. **Commit** (+ Progress row).

## Task 6 — Results-section outline

1. Write `docs/phase3_results.md`: the paper's empirical story with real numbers inlined — headline (control-comparison claim, not round_robin, per Stage 1 reasoning), failure-axis story, fairness/efficiency-equity discussion, negotiation instrumentation, honest-negative clean-cell finding, limitations (n=1 failure cells, degenerate min_house floor, single dataset family, explanation table pending Stage 4). Reference each figure by its committed path.
2. No new figures — this is prose + the tables/figures already generated.
3. **Verify:** every number in the doc traces to a committed summary or a regenerated baseline; no invented figures. (Factual-accuracy mandate.)
4. **Commit** (+ Progress row).

## Task 7 — Phase 3.3 wrap

1. Regenerate everything from one command (`python -m scripts.figures --all`) in the CI-parity venv; confirm all figures + tables reproduce.
2. Docs sync (phase-boundary rule): update `README.md` status line + `CLAUDE.md` Phase 3 status block to reference Phase 3.3 results/figures; add the wrap Progress-log row.
3. Final CI-parity verify: fresh check `ruff check sim tests scripts` + `pytest` green.
4. `/security-review` is not required for a $0 docs+analysis diff with no new secret surface, but run the secrets grep over the staged diff (`git diff --cached | grep -cE 'sk-ant-(api|oat)[0-9a-zA-Z_-]{15,}'` → 0) as standing practice before push.
5. Tag decision: Phase 3.3 is analysis, not a new run campaign — tag `phase3.3-complete` at wrap if the results doc + figures are all in. Commit, push, Slack summary + next-task preview (Phase 4 spec, or Stage 3/4 if Leo reopens spend).

---

**Blocked/deferred, tracked here so they aren't silently dropped:** explanation-quality *numbers* (Stage 4 money), VT/AZ cross-climate cells (optional, Leo+advisor decision). Both are flagged in `phase3_results.md` limitations, not quietly omitted.
