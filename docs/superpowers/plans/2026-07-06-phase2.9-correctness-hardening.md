# Phase 2.9 Correctness Hardening — Implementation Plan

> **For agentic workers:** Execution mode for this project is INLINE (per CLAUDE.md conventions) —
> tasks executed in-session, TDD, one commit per task, progress-log row + checkbox tick in the
> same commit. Spec: `docs/superpowers/specs/2026-07-06-phase2.9-correctness-hardening.md`.

**Goal:** Fix every verified correctness bug from the 2026-07-06 review so Phase 3 starts from
physically-sound, reproducible, pinned numbers.

**Architecture:** No new subsystems. Surgical fixes to `engine.py` caps + strict invariants,
`network.py` settlement, `lp_optimal.py` relaxations, the three data adapters, plus test hardening
and experiment tooling (`--seed`, multi-seed compare, zero-LLM control, provenance/cost logging).

**Tech stack:** unchanged (Python 3.12, pytest, ruff, mypy --strict, scipy HiGHS, pandas).

## Global constraints

- TDD: failing test → red → minimal fix → green → commit. Determinism mandatory.
- `mypy --strict sim/` and `ruff` clean at every commit; pre-commit hooks never bypassed.
- Conventional commits, no Co-Authored-By trailer.
- Every task's commit updates CLAUDE.md progress log + this file's checkboxes.

---

### Task 1: Docs catch-up — Phase 2.8 exists
- [x] CLAUDE.md: add progress rows for `e4d8ff3`, `e56941d`, `61daf17`; update Phase 2 status block (phase2.8-complete tag, clean-cell ceiling finding 0.513, messages +66 %, react_skipped 616).
- [x] README: Phase 2.8 paragraph + correct test count (188).
- [x] `configs/scenarios/haves_havenots.yaml:1` header comment: "24 h" → "12 h".
- [x] Commit `docs: catch CLAUDE.md/README up to Phase 2.8`.

### Task 2: Household physics test hardening (pins existing-correct behavior)
- [x] Rewrite `test_rt_efficiency_full_cycle` to assert on the DISCHARGE step's actual output (end soc == floor, served == η·charged via unmet accounting), not test-side arithmetic.
- [x] Add `test_step_receives_energy_charges_battery_with_sqrt_eta` (negative `desired_net_export_kw`, η=0.9: stored == recv·dt·√η).
- [x] Add `test_step_export_shortfall_cuts_export_before_load` hitting `household.py:116-123` (achieved < desired, remainder unmet).
- [x] Run: `pytest tests/test_household.py -v` → all pass (these pin correct behavior; red-check by inspection of old vacuous assert).
- [x] Commit `test: pin discharge sqrt(eta), receive leg, and export-shortfall branches`.

### Task 3: Engine conservation fix (THE bug)
- [x] Failing test `tests/test_engine_conservation.py`: scripted 2-house night transfer where sender load+send exceeds deliverable; assert every state row has `achieved_net_export_kw == desired` and receiver credit == sender debit·(1−loss). Red under old caps.
- [x] Fix `engine.py` caps to the spec formulas (load/solar-aware, √η on the whole battery term, grid term for connected senders; load-serving term for receivers).
- [x] Add strict-mode assert `abs(achieved − desired) ≤ 1e-9` after `step()`.
- [x] Green + full suite triage (expected failures move to Task 4).
- [x] Commit `fix: engine transfer caps now conserve energy; strict mode asserts achieved==desired export`.

### Task 4: Re-derive scenario expectations post-fix
- [x] Re-run `compare.py` on `haves_havenots`, `overnight_outage_hard`, `24h_uniform`, `synthetic_lp_smoke`; record new numbers.
- [x] Replace `test_integration.py`'s ≥10 kWh phantom assertion with honest post-fix expectations (+ comment explaining the 2026-07-06 correction).
- [x] Update any stress/integration numbers that shifted; update README/CLAUDE.md claims in the same commit.
- [x] Commit `fix: re-derive scenario expectations after conservation fix`.

### Task 5: LP hardening — curtailment slack + send/recv bounds
- [x] Failing test: LP on a solar-rich islanded mini-scenario (pv 12 kW, rate 2 kW, load 1 kW) currently raises `RuntimeError: infeasible`.
- [x] Add `curt` var (power-balance slack, zero cost); add per-tick bounds `send ≤ min(bus, solar + rate·√η + grid)`, `recv ≤ min(bus, load + rate)`.
- [x] Regression: `optimal_served_fraction(haves_havenots)` pinned to pre-change value (0.5294…, 6 dp); existing LP-dominance tests stay green.
- [x] Commit `fix: LP gains curtailment slack + engine-feasibility bounds`.

### Task 6: Settlement + event minors
- [x] `settle_transfers`: aggregate duplicate (sender,receiver) pairs instead of dict-clobbering (test: two same-pair transfers sum).
- [x] `_schedule_from_solution`: exclude self-pairs from the sender pool and renormalize (add gross-conservation assert to an existing LP e2e test).
- [x] `engine.run`: initialize `last_grid_status` to `True` so outages active at t=0 emit `OUTAGE_STARTED` (test on a scenario islanded from the first tick).
- [x] Commit `fix: settlement duplicate-pair aggregation, LP self-pair renormalize, t=0 outage event`.

### Task 7: NRELSolar — timezone + TZ-independent noise
- [x] Noise seed from calendar fields (`int(t.strftime('%Y%m%d%H%M'))`), test pins the exact draw for a fixed (seed, t) as a hardcoded constant.
- [x] `NRELSolar(tz_offset_hours=…)` shifts the index at load; new `Scenario.solar_tz_offset_hours` field plumbed through `_build_data`; `24h_resstock*.yaml` + `24h_real.yaml` get `solar_tz_offset_hours: -6`.
- [x] Loud validation in `NRELSolar.__init__`: mean-GHI-by-hour argmax must fall in 10:00–16:00, else ValueError naming the UTC pitfall (test with a UTC-shaped frame).
- [x] ~~`fetch_data.py`: request `utc=false`~~ — kept `utc=true` as the single convention (CSVs are UTC; scenarios declare `solar_tz_offset_hours` explicitly) so existing data files stay valid and no double-shift is possible; validation makes misuse loud.
- [x] Commit `fix: NREL solar timezone alignment + machine-independent noise seed`.

### Task 8: ResStock — native interval + period-ending timestamps
- [ ] Convert kWh→kW with the file's own median timestamp delta (not scenario `dt_hours`); failing test: 15-min fixture + `dt_hours=0.5` must return unchanged kW.
- [ ] Detect period-ending files (first sample at 00:15-style offset) and shift the index back one interval; new fixture `resstock_period_ending_sample.csv`; test alignment at 00:00.
- [ ] Commit `fix: ResStock native-interval conversion + period-ending timestamp alignment`.

### Task 9: Scenario outage validation
- [ ] `load_scenario` raises on `affected_houses` ids not in the grid and on outage windows that don't intersect [start, end).
- [ ] Tests for both rejects + all shipped YAMLs still load.
- [ ] Commit `feat: validate outage windows and affected-house ids at scenario load`.

### Task 10: Metric + baseline numeric pins
- [ ] `_gini` unit tests: [1,1,1]→0, [0,1]→0.5, [0,0,1]→2/3, empty→0, all-zero, 4-value hand case.
- [ ] round_robin pin: exact per-target kW ((soc−floor)·0.05/dt/n_targets = 1.165 on the existing fixture), above-mean receives nothing, empty when nobody islanded; same math for round_robin_overlay across an owner edge.
- [ ] Commit `test: pin Gini and round_robin baseline numerics`.

### Task 11: Golden regression numbers
- [ ] `tests/test_golden_numbers.py`: post-fix `haves_havenots` served/unmet/gini for no_coord, round_robin, lp ceiling pinned to 6 dp (byte-determinism makes this free; update only on intentional physics change).
- [ ] Commit `test: golden-pin showcase scenario metrics`.

### Task 12: Sweep tooling — seeds + honest gap_closed
- [ ] `run.py --seed N` (dataclasses.replace).
- [ ] `compare.py`: `--seeds a,b,c` → per-seed tables + mean±range aggregate; `gap_closed` unclamped (negative values print as negative; NaN when gap ≈ 0); `--strategies` extension point including `llm_fallback`.
- [ ] Update `test_compare.py` for unclamped semantics.
- [ ] Commit `feat: multi-seed compare + seed override + unclamped gap_closed`.

### Task 13: Zero-LLM control strategy
- [ ] `sim/strategies/llm_fallback.py`: llm_agent machinery with LLM calls disabled (agents hold their initial tuned fallback policy; act() runs pure-Python) — the control that bounds the LLM's marginal contribution. Plus `llm: messaging: off` flag suppressing bus sends (mock-tested).
- [ ] Run it on `haves_havenots__llm` and record served/gini vs round_robin + LLM reference.
- [ ] Commit `feat: zero-LLM control strategy (llm_fallback) + messaging-off ablation flag`.

### Task 14: Wire dead knobs + run provenance + real cost accounting
- [ ] `llm_agent.prepare` consumes `scenario.llm`'s `react_max_per_tick` (and drops/documents dead keys); config.json gains `llm` + `failure_modes`; summary `failure_modes_active` filled from scenario.
- [ ] `AnthropicLLMClient` accumulates usage tokens; `update_summary_with_counts` writes real `llm_cost_usd_estimated` (Haiku $1/$5 per MTok; 0 for cache hits/mocks).
- [ ] Commit `feat: wire llm scenario knobs, run provenance, and real cost accounting`.

### Task 15: React queue — fix the silent 19.5 % drop
- [ ] `observe()` EXTENDS `pending_react` (no clobber); entries age out after 2 ticks with a counted `n_react_dropped_stale`; docstring + CLAUDE.md claim corrected.
- [ ] Tests: skipped message answered next tick; stale drop counted.
- [ ] Commit `fix: react queue survives across ticks with age-out instead of silent clobber`.

### Task 16: Registry handle (sweep-safe counters)
- [ ] `prepare()` attaches the registry to the returned closure; `update_summary_with_counts(run_dir, registry=None)` prefers the handle; module global kept for back-compat; subprocess-per-cell constraint documented in compare.py.
- [ ] Commit `refactor: llm_agent counters travel with the prepared closure`.

### Task 17: Tool-use path test parity
- [ ] `MockLLMClient` supports canned `tool_input` dicts; replay-determinism + integration tests parametrized over text and tool paths; malformed-tool_input 3-strikes unit test.
- [ ] Commit `test: mock the tool-use path the live runs actually take`.

### Task 18: Wrap-up
- [ ] Full suite + ruff + mypy + clean-install dry-run in fresh venv.
- [ ] Re-run real-data numbers (`24h_resstock_outage`) + regenerate `docs/figures/phase1_real_data_result.png`; update README + CLAUDE.md numbers and Phase 2.9 section; deferred-items table verbatim from spec.
- [ ] Tag `phase2.9-complete`; push; check `gh run list`.
